# 商品视频 Agent：竞品参考与产品决策

调研日期：2026-09-09。范围为官方产品页、帮助文档与 API 文档；未登录这些产品执行同素材付费实测。下文区分产品公开能力与本项目的设计判断，不将厂商宣传的质量、收益或效率视为实测结论。

## 1. 同类型产品如何组织工作

| 产品 | 官方公开的设计 | 对本项目的借鉴 | 暂不照搬 |
| --- | --- | --- | --- |
| Invideo Editor | 自然语言编辑与可手动操作的多轨时间轴结合，生成素材进入可编辑项目 | “生成完成”后继续改同一份作品；镜头、素材、版本互相对应 | 首期不构建完整调色、混音和多人 NLE；这些不是当前领域闭环的前提 |
| Invideo Vision | 先形成九宫格分镜；统一人物/风格，再从选中帧进入视频生成 | 先稳定商品主视觉、色调和参考帧，再生成动态镜头；失败时重做单镜头 | 九张不是固定标准；镜头数由故事与时长决定 |
| Creatify | 商品页到广告、行业创意模板、批量变体，围绕商品广告组织应用 | 维护商品资产与品牌包，做开场/卖点/CTA 的可比较变体 | 不承诺自动提升转化率；投放效果要通过真实 A/B 数据验证 |
| Runway Workflows | 将文本、图像生成、图生视频节点连接，节点有历史输出，可替换当前结果 | 后台按镜头建立资源依赖与候选历史，替换参考图仅失效其下游视频 | 不把复杂节点图强加给普通商家；前台仍是一段话加素材 |
| Descript Underlord | 在编辑器内部执行任务，覆盖按时间/时长编辑和图像/视频生成；显示执行计划 | 使用具体修改意图和镜头级操作，展示可理解的进度与结果 | 长访谈的文本剪辑模式可留到视频处理阶段，不作为商品广告的默认入口 |

依据：[Invideo Editor](https://invideo.io/make/agentic-video-editor/)、[Invideo 分镜帮助](https://help.invideo.io/en/articles/14754413-creating-storyboards-with-invideo)、[Creatify](https://creatify.ai/)、[Runway Workflows 帮助](https://help.runwayml.com/hc/en-us/articles/45769159004691-Building-your-first-Workflows)、[Descript 更新说明](https://feedback.descript.com/changelog/announcingunderlord-v2)。以上是公开能力摘要，表中“借鉴”与“不照搬”为本项目判断。

## 2. 取长补短后的定位

不把竞争点定为“接入最多模型”，而是让商品材料在不同模型之间保持可用，最后交付一个能继续修改的工程。先服务一种明确工作：同一商品的介绍、卖点、活动和版本迭代。

产品结构应是：简洁输入入口 → 镜头工作区 → 可复用素材与品牌约束 → HyperFrames 工程 → 成片及版本。模型在每个镜头背后工作；用户主要决定画面、文案、节奏和目标，必要时才看生成候选与预算。

本轮落地镜头列表、真实长时长、7 种版式、最多 12 张输入图片、镜头级素材选择和时长/顺序编辑。它们先解除“所有任务只能套三幕”的结构限制。

## 3. 如何让效果继续变好

### 从长幻灯片到有内容的镜头序列

延长成片只解决容量，不解决叙事。正式策划需为每个镜头指定目的：吸引注意、建立场景、完整商品、细节、使用过程、事实说明、收尾行动。每个新镜头应增加一条信息、一个视角或一个必要节奏变化。

建议的默认节奏由内容决定：开场快、关键演示留足时间、文案停留时间匹配阅读量。不能把同一张图循环二十次，称为二十个新拍摄镜头。当前 Demo 的镜头变化主要是编排、裁切与动效，真正的新场景需要更多实拍或生成素材。

### 从一次全量生成到按镜头迭代

每镜保留指令、参考图、候选图/视频、选中版本和模型任务记录。先选定关键画面，再让视频模型负责运动；文案、Logo、CTA 在 HyperFrames 中单独叠加。这样改文字不触发生视频，换运动可以沿用商品参考图，镜头重做不会丢失整条作品。

### 从同一种演示到可比较的案例

后续应使用同一商品材料制作：30 秒广告、60 秒说明、120 秒产品故事，以及“仅改第二个镜头”的版本对照。保持材料和目标相同，再比较工具操作成本、改动准确性、商品一致性与最终可交接性；不要只比较不同材料下的宣传样片。

## 4. 接入顺序的判断

先补充输入媒体、参考图与真实镜头素材，再扩展模型数量。OpenAI GPT Image 可作为首批生图适配对象，其官方文档区分直接 Image API 与可连续编辑的 Responses 工作流。[OpenAI 图像文档](https://developers.openai.com/api/docs/guides/image-generation)

生视频优先分别验证 Runway 与 Veo 的单镜头任务、参考资产、异步状态与结果收取。不同提供方的时长、画幅和输入限制需要独立校验，不能靠一个通用参数原样透传。[Runway API](https://docs.dev.runwayml.com/api/)、[Veo 文档](https://ai.google.dev/gemini-api/docs/veo?hl=en)

截至调研日，OpenAI 官方标记 Sora API 将于 2026-09-24 永久停用。因此它不进入新系统的默认视频提供方名单；后续若有新接口，应重新核验再接入。[OpenAI 公告所在 API 页面](https://developers.openai.com/api/reference/typescript/resources/videos/methods/create)

## 5. 下一轮比较的最小任务集

1. 相同商品图与事实，制作 30 秒、至少 5 个镜头的广告。
2. 用素材扩充到 60 秒，检查是否增加内容，而非延长停留。
3. 在 120 秒版本中替换一个镜头，核对其余镜头和音轨是否保持。
4. 给出商品包装参考，生成环境图再生成短视频，人工检查 Logo、形状与颜色。
5. 断开一个模型提供方，确认已完成镜头保留、任务有明确结束状态且费用可追溯。

记录首轮成功率、人工修改次数、总耗时、额外生成次数、实际费用，以及对照组的盲评结果。尚未完成的付费实测应保留为空，不填写估算成功率。
