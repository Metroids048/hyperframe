# OpenAgent Agent 标准化设计与开发 SOP

## 一、目标和使用方式

这份 SOP 用于把 OpenAgent 上的 Agent 开发固定成一套可复制流程。后续新增 Agent 时，不再从零讨论“要不要做 Agent Card、要不要写测试、要不要做错误处理”，而是：

```text
选择一个新选题
  -> 填写选题变量包
  -> 复用固定模块
  -> 用 Claude Code 分阶段开发
  -> 跑固定评测流程
  -> 根据测试结果迭代或发布
```

一个完整 Agent 项目由两部分组成：

| 部分 | 作用 | 后续是否固定 |
| --- | --- | --- |
| Agent Factory 标准模块 | 定义流程、文档、目录、测试、版本和发布规范 | 固定复用 |
| Topic Pack 选题变量包 | 定义具体场景、用户、工具、知识、样本和交付物 | 每个选题重新填写 |

目标不是让所有 Agent 长得一样，而是让所有 Agent 都具备相同的基本质量、可交付性和可评测性。产品差异应来自选题、领域规则、工具能力、交付物和用户体验，而不是来自是否漏写了某个基础环节。

## 二、固定模块和变量模块

### 2.1 所有 Agent 固定的标准模块

以下模块每个 Agent 都必须存在：

1. Agent 基本信息和版本信息。
2. 目标用户、核心任务和能力边界。
3. 用户输入契约和最终输出契约。
4. 标准任务流程、追问规则和停止条件。
5. Skill、Tool、知识和工作区的能力分层。
6. 中间产物、最终交付物和文件命名规范。
7. 状态管理、错误处理、超时和重试规则。
8. 权限、数据、外部系统和用户确认边界。
9. Agent Card、市场卡片和示例任务。
10. 对话式 Benchmark、评分规则和回归记录。
11. 本地运行、OpenAgent 调试和发布检查。
12. 版本、日志、已知问题和回滚信息。

### 2.2 每个新选题需要重新确定的变量

产品负责人只需要重新判断和填写：

```text
选题名称
目标用户
核心使用场景
用户要完成的任务
输入文件或字段
最终交付物
领域知识和规则
任务专用工具
需要保存的项目状态
样本数据和期望结果
竞品对象
成功标准和发布门槛
```

以下内容原则上不因选题变化：

```text
项目目录
文档模板
版本规则
状态机
开发阶段
交付包结构
错误返回格式
权限检查清单
Benchmark 用例类型
评分表结构
发布前检查清单
```

### 2.3 P0、P1、P2 模块分级

| 等级 | 含义 | 模块示例 |
| --- | --- | --- |
| P0 | 每个可上架 Agent 必须具备 | Brief、输入输出、主流程、交付物、Benchmark、权限、版本 |
| P1 | 任务需要时必须具备 | 文件处理、外部 API、项目状态、局部重做、领域知识库 |
| P2 | 验证主流程后再增加 | 多 Agent 协作、复杂自动化、长期记忆、主动通知、批量调度 |

冷启动阶段的默认策略是先完成 P0，再按真实失败补 P1。没有明确用户需求和评测证据时，不进入 P2。

## 三、Agent Factory 的标准架构

### 3.1 产品层

产品层回答“为什么做、给谁用、完成什么工作”：

```text
市场定位
  -> 用户场景
  -> 任务定义
  -> 输入契约
  -> 工作流
  -> 中间产物
  -> 最终交付物
  -> 复用理由
```

### 3.2 Agent 能力层

能力层回答“Agent 如何完成任务”：

```text
主 Skill：任务路由、阶段管理、追问和停止
  -> 子 Skill：领域方法、输出规范、质量规则
  -> Tool：文件、数据、浏览器、外部 API 或执行能力
  -> Knowledge：规则、模板、案例和参考资料
  -> Workspace：输入、状态、中间结果和最终产物
```

建议固定采用以下职责分工：

| 组件 | 固定职责 | 不应该承担的内容 |
| --- | --- | --- |
| 主 Skill | 理解目标、拆解阶段、路由任务、管理状态 | 直接实现复杂文件处理 |
| 子 Skill | 某一领域的做法、规则和输出格式 | 管理全局会话和权限 |
| Tool | 执行明确、可验证的动作 | 自己决定整个业务流程 |
| Knowledge | 提供参考事实和规则 | 代替工具执行外部动作 |
| Workspace | 保存项目和中间产物 | 作为无边界的长期数据仓库 |
| Agent Card | 对用户说明能力和限制 | 作为内部系统 Prompt |

### 3.3 平台对象层

所有 Agent 按以下对象关系进入 OpenAgent：

```text
Agent 主对象
  -> Agent Version
      -> OpenClaw 运行程序
      -> Skill / Tool / Knowledge / Workspace
      -> WebUI / CA 调试
      -> Agent Card
      -> Pricing 配置接口
      -> Benchmark 结果
      -> 保存、发布和回滚
```

Pricing 作为平台固定模块保留字段、配置入口和版本绑定关系，但当前冷启动的产品验收不评价定价策略，也不以计费完成作为 Agent 上架前置条件。

## 四、标准 Agent 项目模块清单

### 模块 1：Agent 基本身份

每个 Agent 必须有独立身份和版本关系：

```text
Agent ID：平台生成
Agent 名称：用户能理解的任务名称
内部项目名：稳定、便于代码和文件管理
当前版本：例如 0.1.0-dev、0.1.0-beta
状态：候选 / 开发中 / 可测试 / 内测 / 已发布 / 淘汰
负责人：产品、开发、评测、运营
创建日期和更新时间
```

命名规则：

- 用户名称使用“任务或结果 + 场景”，例如“季度经营汇报助手”。
- 内部项目名使用小写英文和短横线，例如 `quarterly-report-agent`。
- Agent 名称不要以模型、框架、Skill 或 Tool 命名。
- 同一 Agent 的行为变化使用新版本，不覆盖历史评测结果。

### 模块 2：Agent Brief

这是所有后续工作的唯一产品源文件，固定字段如下：

```text
名称：
一句话价值主张：
目标用户：
触发场景：
用户当前做法：
核心任务：
明确不做：
使用频率：
用户输入：
用户需要提供的最少信息：
Agent 的追问规则：
任务阶段：
中间产物：
最终交付物：
用户可以修改的节点：
知识和规则：
工具和外部系统：
权限边界：
成功标准：
失败处理：
用户再次使用的理由：
竞品对象：
```

Brief 通过标准：产品、开发和评测三方能据此独立描述同一条用户任务链，且最终交付物可以被具体检查。

### 模块 3：用户入口和输入契约

固定设计为“自然语言入口 + 必要结构化信息 + 文件入口”三种形式的组合，具体选题按需要启用：

```text
用户说了什么
  -> Agent 识别任务
  -> 判断已有信息是否足够
  -> 缺少关键条件时追问
  -> 使用明确默认值时告知用户
  -> 确认任务计划
```

输入契约必须写清：

| 字段 | 必填性 | 类型 | 示例 | 缺失时动作 |
| --- | --- | --- | --- | --- |
| 核心素材 | 必填 | 文件/文本 | 会议录音、Excel、文章 | 不能执行，提示上传 |
| 任务目标 | 必填 | 枚举/文本 | 生成汇报、审阅风险 | 追问目标 |
| 受众 | 可选 | 枚举/文本 | 总经理、客户、学生 | 使用默认值并告知 |
| 输出格式 | 可选 | 枚举 | PPTX、PDF、长图 | 使用默认格式 |
| 风格或约束 | 可选 | 文本 | 10 页、正式、品牌色 | 采用默认规则 |

设计原则：用户不用理解内部工作流；但所有会影响结果的关键参数必须能够被用户看到、修改和确认。

### 模块 4：任务流程和状态机

每个 Agent 默认采用以下阶段：

```text
INIT 初始化
  -> INTAKE 收集并检查输入
  -> PLAN 生成任务计划
  -> CONFIRM 用户确认关键假设
  -> EXECUTE 执行主要任务
  -> REVIEW 检查结果
  -> PREVIEW 展示中间或最终预览
  -> REVISE 接受用户修改
  -> DELIVER 生成最终交付
  -> COMPLETE 完成
```

失败或中断统一进入：

```text
FAILED 失败
  -> RETRY 可重试
  -> REPLACE 替换输入或工具
  -> ASK_USER 请求用户补充
  -> HANDOFF 人工或技术接管
```

产品必须为每个阶段填写四项内容：进入条件、Agent 动作、用户可见结果、退出条件。不能只写“分析一下”“处理一下”这类模糊步骤。

### 模块 5：主 Skill 和子 Skill

固定的主 Skill 结构：

```text
身份与任务边界
输入识别
信息完整性判断
任务阶段路由
工具选择规则
中间产物格式
质量检查规则
用户修改规则
停止条件
错误与越权处理
最终输出格式
```

选题相关内容放入子 Skill：

```text
领域方法 Skill
输出格式 Skill
平台规范 Skill
质量检查 Skill
示例和反例 Skill
```

主 Skill 不应不断膨胀。某个领域方法在两个以上 Agent 中复用时，提取成共享 Skill；只服务一个 Agent 的规则保留在该 Agent 内部，避免过度抽象。

### 模块 6：Tool、知识和权限

每个工具固定用一张能力卡描述：

```text
工具名称：
解决的具体动作：
输入参数：
返回结果：
成功示例：
失败示例：
是否可重试：
是否修改外部数据：
需要的权限：
用户是否需要确认：
超时和最大调用次数：
```

知识内容固定分为：

- 规范：行业标准、平台规则、格式要求。
- 方法：完成任务的判断步骤和操作方法。
- 模板：报告、表格、脚本、页面或文件模板。
- 案例：高质量和低质量的对照样本。
- 禁用项：不能生成、不能执行或需要人工复核的内容。

权限固定遵循：只读优先、目录限制、服务限制、凭据隔离、外部发送需确认。产品需要写清业务规则，技术同事决定具体的实现方式。

### 模块 7：中间产物和最终交付物

每个 Agent 至少定义一个中间产物和一个最终交付物。固定使用以下交付描述：

```text
产物名称：
产物用途：
生成阶段：
用户是否可见：
用户是否可修改：
文件格式：
保存位置：
命名规则：
质量检查：
失败时如何处理：
```

最终交付必须回答：

1. 用户拿到的是什么文件或结果？
2. 用户能否直接使用、下载或继续编辑？
3. 如果不满意，能修改哪个节点？
4. 结果能否被复用到下一次任务？

“输出一份专业方案”“生成高质量内容”不算交付定义，必须转换成具体的文件、字段、章节、图片、视频、代码或可执行动作。

### 模块 8：对话体验

固定设计五种对话状态：

| 状态 | Agent 表现 |
| --- | --- |
| 首次进入 | 用一句话说明能做什么，并给出真实示例 |
| 信息不足 | 只追问影响结果的关键条件 |
| 长任务执行 | 告知当前阶段和下一阶段 |
| 中间结果 | 展示可检查、可确认或可修改的结果 |
| 失败 | 说明失败原因、影响范围和下一步 |

用户修改时，默认采用局部重做：

```text
用户修改某个局部
  -> 定位对应中间产物
  -> 只重新执行受影响的步骤
  -> 保留未修改部分
  -> 重新检查整体一致性
```

### 模块 9：错误、超时和停止

所有 Agent 统一需要处理：

- 用户输入缺失或格式不支持。
- 文件过大、损坏或无法读取。
- 模型无法判断或结果置信度不足。
- 外部 API、网页或工具不可用。
- 工具多次失败或超过时间限制。
- 用户请求超出 Agent 能力范围。
- 用户要求执行高风险或越权操作。

推荐错误返回结构：

```json
{
  "ok": false,
  "stage": "REVIEW",
  "error_code": "OUTPUT_INVALID",
  "message": "生成的文件缺少必需字段，暂未交付",
  "retryable": true,
  "user_action": "选择重新生成，或补充缺失信息",
  "technical_detail": "仅在调试日志中展示"
}
```

产品验收重点是：用户知道发生了什么，知道是否需要自己操作，知道之前完成的部分是否保留。

### 模块 10：Agent Card 和市场卡片

固定包含：

```text
Agent 名称
一句话简介
适用人群
能解决的任务
输入要求
最终交付物
使用步骤
三个示例任务
能力边界
权限和数据说明
开发者信息
版本信息
```

市场卡片的推荐结构：

```text
标题：结果 + 场景
副标题：用户提供什么，Agent 交付什么
标签：按用户任务分类
示例：真实输入和真实结果
入口：立即试用
```

不要在第一屏堆叠 Skill、模型、MCP 等技术名词。技术信息放在详细页，用于开发者和高级用户判断能力。

### 模块 11：Benchmark 和评测

每个 Agent 固定创建以下测试组：

```text
标准成功 5 条
缺少信息 3 条
脏数据或复杂输入 3 条
多轮修改 3 条
边界任务 2 条
工具失败 2 条
越权和提示注入 2 条
```

固定评分维度：

| 维度 | 分值 |
| --- | ---: |
| 任务完成度 | 30 |
| 内容正确性 | 25 |
| 交付物完整性 | 20 |
| 结果可用性 | 15 |
| 对话效率 | 5 |
| 边界与安全 | 5 |
| 合计 | 100 |

每条测试必须记录：测试编号、版本号、模型、输入、完整对话、工具调用、中间产物、最终产物、耗时、评分和失败原因。

### 模块 12：版本和发布

固定版本规则：

```text
0.1.0-dev：开发和主流程验证
0.1.0-test：完成 Benchmark，准备平台调试
0.1.0-beta：小范围内测
1.0.0：达到正式发布门槛
```

以下变化必须升版本：主 Skill、工具参数、知识库、模型、输入输出契约、权限、交付物格式和质量规则。

发布前固定检查：

- Agent ID 和 Version 正确。
- WebUI 或平台调用可以完成主流程。
- Agent Card、示例和限制完整。
- 输入输出与实际行为一致。
- Benchmark 绑定当前版本。
- 交付文件可以打开或继续编辑。
- 权限、凭据和外部数据范围明确。
- 错误、超时、重试和停止有效。
- 已知问题和回滚版本已记录。
- Pricing 字段按平台要求完成配置或标记为待统一处理，不作为当前产品质量结论。

## 五、Topic Pack：每个新选题只填写这一份

后续新增 Agent 时，产品先复制下面模板，填完即可进入开发评审：

```text
# Topic Pack

## 1. 选题
选题名称：
一句话价值：
来源证据：竞品、用户反馈、搜索、内部需求或数据
与已有 Agent 的区别：

## 2. 用户
目标用户：
使用频率：
触发时机：
用户当前做法：
当前做法的时间、成本和主要痛点：

## 3. 任务
用户原始任务：
本版本核心任务：
明确不做：
成功后的下一步：

## 4. 输入
必填文本：
必填文件：
可选信息：
默认值：
文件格式和大小限制：

## 5. 输出
中间产物：
最终交付物：
文件格式：
用户修改节点：
结果可用标准：

## 6. 能力
领域知识：
固定规则：
需要的 Skill：
需要的 Tool/MCP：
需要保存的项目状态：
权限和外部系统：

## 7. 评测
标准成功样本：
缺信息样本：
复杂输入样本：
多轮修改样本：
失败和边界样本：
竞品：
最低发布分数：

## 8. 市场
Agent 名称：
市场简介：
标签：
三个示例任务：
封面或结果展示重点：
```

Topic Pack 评审通过后，不再反复讨论基础模块，只讨论四件事：选题是否值得做、主任务是否足够窄、交付物是否明确、测试是否能够证明质量。

## 六、标准开发 SOP

### 阶段 0：选题立项

产品输入：候选选题和需求证据。

固定动作：

1. 检查是否与已有 Agent 重复。
2. 明确目标用户和主任务。
3. 找 3 个竞品或替代方案。
4. 准备至少 10 条真实任务样本。
5. 按需求强度、交付价值、差异化、传播性、可开发性、复用性评分。

输出：Topic Pack、竞品初步对测表、立项结论。

退出标准：能用一句话说明用户为什么需要它，能列出明确最终交付物。

### 阶段 1：产品定义

产品输入：通过的 Topic Pack。

固定动作：

1. 完成 Agent Brief。
2. 画出用户从输入到交付的流程。
3. 定义每个阶段的中间产物。
4. 写输入契约、输出契约、能力边界和错误场景。
5. 准备市场卡片和示例任务。

输出：产品开发任务包、验收样本和 Agent Card 初稿。

退出标准：技术同事不需要猜测用户输入、最终交付物或验收标准。

### 阶段 2：Claude Code 开发

产品给 Claude Code 的任务包固定采用以下顺序：

```text
第一步：阅读 Agent Brief、输入输出契约和样本，不写代码，先复述实现方案和风险。
第二步：创建标准目录和运行入口，先实现主流程骨架。
第三步：接入真实输入和一个最必要的工具，跑通一条完整任务。
第四步：补齐错误、超时、重试、状态和文件交付。
第五步：接入全部任务专用能力，加入可修改和局部重做。
第六步：运行全部 Benchmark，输出评分和失败分类。
第七步：生成 README、环境变量示例、权限说明、版本记录和 OpenAgent 接入说明。
```

每一步的产品验收只看：是否能运行、是否产生正确交付物、是否处理异常、是否符合用户流程。不要接受只展示代码 diff、没有真实运行结果的交付。

### 阶段 3：本地运行验收

固定验收流程：

1. 从干净目录按照 README 安装和启动。
2. 使用至少 3 个真实样本运行主流程。
3. 检查日志、工具调用、中间产物和最终交付物。
4. 主动输入缺失、错误和超范围请求。
5. 修改一个中间结果，确认局部重做有效。
6. 重复运行一次，确认项目之间没有污染。

输出：本地运行记录、样例产物、问题清单和修复结果。

退出标准：产品负责人可以按说明自行启动或让技术同事复现，主流程真实跑通。

### 阶段 4：OpenAgent 平台调试

固定流程：

```text
创建 Agent 主对象
  -> 创建开发 Version
  -> 连接本地 Agent 或创建沙箱程序
  -> 配置运行环境和权限
  -> WebUI/CA 调试主流程
  -> 检查 Agent Card 和输出
  -> 保存草稿
```

平台验证重点：

- 平台请求是否能到达正确版本。
- 文件上传、工作区和最终下载是否正常。
- 会话状态是否能跨多轮保留。
- 运行日志和错误信息是否可定位。
- 本地行为和平台行为是否一致。

### 阶段 5：Benchmark 和竞品对测

固定先测自己的 Agent，再测竞品：

1. 运行 20 条对话式 Benchmark。
2. 统计总分和各类失败率。
3. 对 3 个竞品运行 5 条同题任务。
4. 对比交付物、耗时、修改轮数和失败恢复。
5. 只修复最高频且影响用户结果的问题。
6. 重新运行全量测试，绑定新版本。

### 阶段 6：内测和发布

固定安排 5 到 10 位目标用户试用，每位用户完成至少一个真实任务。收集：

```text
是否完成首次任务
是否拿到可用交付物
修改了哪些内容
在哪一步退出
是否愿意再次使用
是否愿意推荐给同类用户
用户认为竞品更好的地方
```

发布结论只有三种：

- 发布：达到 Benchmark 门槛，用户能完成主任务。
- 继续迭代：需求成立，但结果或体验尚未达标。
- 淘汰：用户需求弱、交付物不成立或没有足够差异化。

## 七、固定交付包

每个 Agent 最终都要交付一个标准包：

```text
agent-project/
├── README.md
├── product/
│   ├── agent-brief.md
│   ├── topic-pack.md
│   ├── user-flow.md
│   ├── input-contract.md
│   ├── output-contract.md
│   ├── agent-card.md
│   └── permissions.md
├── openclaw/
│   ├── skills/
│   ├── tools/
│   ├── knowledge/
│   ├── workspace-template/
│   └── config.example.json
├── evals/
│   ├── cases.jsonl
│   ├── rubric.md
│   ├── fixtures/
│   ├── expected/
│   └── reports/
├── outputs/
│   └── demo/
├── docs/
│   ├── local-run.md
│   ├── openagent-integration.md
│   └── troubleshooting.md
└── CHANGELOG.md
```

其中产品目录由产品负责人维护，OpenClaw 目录由开发维护，评测目录由产品和评测共同维护，最终输出目录必须保留可展示样例。

## 八、标准验收门槛

### 8.1 产品门槛

- 目标用户不是“所有人”。
- 核心任务可以用一句话说清楚。
- 至少有 10 条真实任务样本。
- 最终交付物具体、可打开、可使用或可继续编辑。
- 用户能知道如何开始和如何修改。
- 能说明与现有 Agent 或竞品的差异。

### 8.2 运行门槛

- 能在本地按 README 启动。
- 主流程使用真实样本跑通。
- 工具失败、输入错误和超时有明确处理。
- 运行结果不会污染其他项目。
- OpenAgent Version 可以调试。
- 日志能定位失败阶段。

### 8.3 评测门槛

- 20 条对话式 Benchmark 已执行。
- 标准成功任务平均分达到 80 分以上。
- 关键事实错误为 0。
- 交付文件可打开率 100%。
- 越权和危险动作全部拒绝或要求确认。
- 重大修复后重新执行全量回归。

### 8.4 发布门槛

- Agent Card 与实际行为一致。
- 市场卡片能在 30 秒内让用户理解用途。
- 至少有 3 个可复现示例。
- 目标用户完成小范围内测。
- 当前版本、评测记录、已知问题和回滚版本已绑定。
- 计费字段按照平台要求处理，但不以当前定价策略作为产品质量判断。

## 九、产品负责人日常只需要维护的五个文件

如果不想维护过多文档，产品侧可以把工作压缩成五个核心文件：

```text
1. topic-pack.md：为什么做、给谁做、做什么
2. agent-brief.md：输入、流程、输出和边界
3. test-cases.jsonl：用什么真实任务测试
4. competitor-matrix.md：和竞品相比好在哪里
5. release-checklist.md：是否具备发布条件
```

技术同事负责根据这五个文件生成和维护代码、Skill、Tool、配置、日志与运行说明。产品负责人不需要掌握每个实现细节，但必须能使用样本验证最终行为，并拒绝没有证据的完成结论。

## 十、冷启动阶段的推荐节奏

### 第一次建立标准

先用一个办公类和一个技术类 Agent 验证这套 SOP，例如：

- 办公类：PPT/汇报材料生成 Agent。
- 技术类：GitHub 项目问题处理 Agent。

这两个方向的输入、交付物和失败类型不同，能较快暴露标准模板是否完整。

### 标准稳定后批量推进

当两个试点都能完成“选题、开发、评测、竞品对测、内测”全流程后：

1. 固定标准目录和模板。
2. 固定 Claude Code 的开发指令。
3. 固定 Benchmark 生成格式。
4. 固定评测报告和发布检查表。
5. 每周推进 1 到 2 个新 Agent 进入可测试状态。
6. 每月回收共性问题，升级 Factory 标准模块。

标准模块本身也需要版本管理，例如 `Agent Factory v1.0`、`v1.1`。Factory 升级后，新 Agent 直接使用新标准，已发布 Agent 按影响范围决定是否迁移。

## 十一、最终工作模型

后续每个 Agent 的工作可以压缩成下面这张执行卡：

```text
1. 选什么题目？
2. 用户要完成什么具体任务？
3. 用户提供什么？
4. Agent 交付什么？
5. 哪些中间结果需要用户确认或修改？
6. 哪些能力由 Skill、Tool、Knowledge 和 Workspace 完成？
7. 用哪 20 条对话测试证明它有效？
8. 和哪 3 个竞品做同题对测？
9. Claude Code 是否交付了可运行、可复现、可调试的代码？
10. 是否达到内测和发布门槛？
```

如果这十个问题都已经有明确答案，剩下的主要工作就是推进开发、运行样本、记录问题和根据评测结果迭代。这样 Agent 数量增加时，产品方法、交付质量和评测方式不会随个人经验漂移。
