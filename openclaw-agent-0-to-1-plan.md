# OpenClaw Agent 从 0 到 1 工作方法（产品负责人版）

## 目标

建立一套适合产品负责人的方法，在 OpenAgent 平台上基于 OpenClaw 设计并推动完成一个可运行、可调试、可评估、可发布的 Agent。你的核心工作不是亲自编写运行代码，而是定义值得做的场景、设计完整的 Agent 方案、明确交付结果和验收标准，再和技术同事一起把方案变成可运行版本。

## 一、先建立正确的工作模型

把一个 Agent 看成六个部分：

```text
用户任务
  -> Agent Card（能力、输入、输出、权限、使用边界）
  -> OpenClaw Gateway（会话、状态、工具、事件）
  -> Skill / Plugin / MCP（工作流和工具）
  -> 模型与外部系统
  -> 结果校验与交付物
  -> 运行记录、评分和版本迭代
```

OpenClaw 是运行时和执行框架，不等于 Agent 本身。Agent 的差异主要来自：任务边界、工作流、工具、领域规则、数据源、结果校验和评估集。

当前平台的关键对象可以先按以下关系理解：

```text
Agent
  └── Agent Version
        ├── 本地 Agent 连接或沙箱 Agent 程序
        ├── WebUI / CA 调试入口
        ├── Agent Card
        ├── Pricing（后续由创建者配置）
        ├── 测试与运行记录
        └── 信息保存 / 信息发布
```

后续所有工作都围绕“一个 Agent、一个可发布版本、一个可重复验证的任务集合”展开。

## 二、第一阶段必须了解和熟悉的内容

### 1. OpenClaw 基础架构

需要弄清楚：

- Gateway 如何启动、连接和管理会话
- Agent 的工作区、配置、状态、记忆分别放在哪里
- Skill、Plugin、Tool、MCP server 的区别和边界
- 模型 provider 如何配置，模型调用如何流式返回
- 本地执行、远程执行、沙箱执行分别适用于什么场景
- 文件、浏览器、网络、命令、第三方 API 如何授权
- 任务失败、超时、重试、中断和人工接管如何表现
- 如何查看 Gateway 日志、工具调用记录和最终产物

建议优先阅读 OpenClaw 官方仓库的 README、Architecture、Tools、Skills、Plugins、Security 和 Sandboxing 文档：

- https://github.com/openclaw/openclaw
- https://docs.openclaw.ai

必须实际跑通一个最小示例：启动 Gateway，建立一次会话，调用一个无副作用工具，读取日志，修改配置后重新运行。

### 2. 本地部署能否用于创建和测试

可以。你可以在自己的 Windows、macOS 或 Linux 环境本地部署 OpenClaw，用本地 Gateway、工作区、Skill、工具和模型连接，先把 Agent 的实际效果跑出来。OpenClaw 官方项目支持 Windows，Windows 环境也可以通过 WSL2 使用；具体安装方式和版本要求以项目当前文档为准。

但要区分两个目标：

| 目标 | 本地 OpenClaw | OpenAgent 平台 |
| --- | --- | --- |
| 设计任务流程 | 适合 | 适合 |
| 编写和调试 Skill | 适合 | 适合 |
| 验证模型回答和工具调用 | 适合 | 适合 |
| 验证平台 Agent ID、版本和发布状态 | 不适合 | 必须在平台验证 |
| 验证多人访问、沙箱和平台权限 | 不完整 | 必须在平台验证 |
| 验证用户看到的最终体验 | 只能做初步验证 | 必须在平台 WebUI 验证 |

推荐的产品工作方式是：先在本地完成“场景和效果验证”，再让技术同事把同一套 Skill、Tool、配置和 Agent Card 接入 OpenAgent 的 Agent Version，最后在平台环境做兼容性和用户体验验证。你可以把本地 OpenClaw 理解为产品试验场，把 OpenAgent 理解为版本管理、调试、发布和用户使用环境。

本地验证的最小闭环是：

1. 安装 OpenClaw 并配置一个可用模型。
2. 启动 Gateway，创建独立工作区。
3. 为一个具体场景写 Agent Brief 和 Skill。
4. 接入一个真实但低风险的工具或数据源。
5. 用 5 到 10 条真实任务测试结果。
6. 记录追问、工具调用、输出、失败和用户修改意见。
7. 把验证通过的方案交给技术同事接入平台版本。

本地测试时不要直接连接生产账号、真实客户数据或具有删除、付款、发信权限的账号。优先使用测试数据、只读接口和独立凭据。

### 3. OpenClaw 扩展方式

先不要急着做复杂 Plugin，按以下顺序理解：

| 扩展方式 | 适合做什么 | 需要掌握的重点 |
| --- | --- | --- |
| Skill | 固定方法、领域规则、输出规范 | 指令组织、触发条件、引用资料 |
| Tool | 一个明确的可调用能力 | 参数 Schema、返回值、错误 |
| MCP Server | 独立的外部工具服务 | 鉴权、工具目录、结构化返回 |
| Plugin | 完整的可安装能力包 | 生命周期、配置、权限、发布 |
| Gateway 配置 | 会话、模型、渠道和运行策略 | 隔离、默认值、环境管理 |

第一个 Agent 通常应采用“Skill + 少量稳定 Tool/MCP”的组合。你不需要亲自决定具体代码实现，但需要能判断：这个能力应该由规则完成、由工具完成，还是需要独立的 Plugin。只有当能力需要独立安装、独立版本、独立配置或被多个 Agent 复用时，才升级为 Plugin。

### 4. OpenAgent 平台流程

你需要把平台每个页面和字段记录成一张流程表：

1. 创建 Agent：Agent ID、账户、名称、简介、分类、场景。
2. 创建 Agent Version：版本号、运行入口、连接本地 Agent 或创建沙箱 Agent 程序。
3. 开发调试：WebUI 调试、CA 调试、会话上下文、文件挂载、日志和错误信息。
4. Agent Card：能力描述、输入输出、权限、限制、示例任务。
5. Pricing：只了解平台需要哪些价格配置字段，具体价格由创建者后续根据 token 消耗和溢价系数设定。
6. 保存和发布：草稿、测试中、待审核、已发布、下线、回滚。
7. 版本查看：调用量、成功率、耗时、错误率、不同版本表现。
8. 账户：收款、账单、提现和平台分成规则。
9. Benchmark：测试任务导入、运行、评分、对比和发布门槛。

这一步的产物不是笔记，而是一个“平台操作 SOP”，每个字段都要记录：用途、必填性、示例、依赖、失败表现和验收方法。

### 5. Agent 产品设计

每个 Agent 开始开发前，先写一页 Agent Brief。它是产品方案、技术实现和后续 Benchmark 的共同输入：

```text
名称：
目标用户：
用户触发场景：
用户当前的替代做法：
解决的具体任务：
明确不解决的任务：
核心使用流程：
输入：
输出：
交付物：
所需工具：
所需数据：
关键能力和差异化：
高风险动作：
成功标准：
失败时的处理：
用户为什么会再次使用：
```

任务必须能被写成“用户触发 -> Agent 执行 -> 交付物 -> 用户下一步”。例如“帮我优化网站”太宽泛；“读取指定网页的 title、description、正文结构，输出 SEO 问题清单、修改后的 HTML 元数据和上线前检查清单”就可以测试、展示和验收。

产品负责人要特别关注四件事：用户为什么现在就要用、Agent 完成后用户拿到了什么、结果是否比通用聊天更可靠、用户是否有理由再次回来。

### 6. 高质量 Agent 的产品架构

一个能够吸引用户的 Agent，至少要设计好六层：

1. **入口层**：用户用什么问题开始，是否能用一句话理解和启动。
2. **任务层**：Agent 把用户目标拆成哪些步骤，哪些步骤需要追问，什么时候停止。
3. **知识层**：需要哪些行业规则、模板、案例、数据源和判断标准。
4. **执行层**：哪些步骤由模型完成，哪些步骤必须调用工具、浏览器、文件或外部 API。
5. **交付层**：最终输出是答案、报告、表格、代码、文件、网页还是可继续执行的任务。
6. **反馈层**：用户如何修改结果、重新执行、评价质量并进入下一次使用。

不要从“我能接入什么工具”开始，而要从“用户要完成什么工作”倒推能力。产品方案中应明确：

```text
场景 -> 用户输入 -> Agent 追问 -> 任务拆解 -> 工具/知识调用
     -> 中间结果 -> 质量检查 -> 最终交付物 -> 用户下一步
```

产品负责人在交给技术同事之前，至少要准备五份内容：

1. **场景说明**：目标用户、触发时机、痛点、现有替代方案和使用频率。
2. **对话流程**：首次进入怎么问、缺信息怎么追问、用户中途修改时怎么处理。
3. **能力地图**：每一步需要的知识、工具、外部数据、人工确认和输出格式。
4. **结果样例**：至少准备一个理想结果、一个部分成功结果和一个失败结果。
5. **验收任务集**：把真实用户问题整理成标准任务，明确每条任务的合格结果。

市场展示页还需要单独设计：一句话价值主张、适用人群、能解决的任务、最终交付物、三个示例问题、能力边界和使用后的下一步。用户是否点击试用，首先取决于他能否快速想象“这个 Agent 能替我完成什么”。

### 7. 如何判断一个场景值得做

候选场景先从以下五个问题判断：

- 是否有明确的人群和高频触发时机？
- 用户现在是否需要多个工具、网页或文档来完成？
- 最终结果是否能形成可直接使用的交付物？
- 质量是否能通过规则、数据或人工标准判断？
- OpenClaw 的本地环境、浏览器、文件、代码或多步骤执行是否能形成优势？

优先选择“用户目标具体、过程繁琐、结果可验收、通用聊天容易出错”的任务。避免只提供陪聊、泛泛问答和一次性文本润色，这些场景很难体现 OpenClaw 的技术优势。

### 8. 工具和权限边界

每一个工具都要回答四个问题：

- Agent 能读取什么？
- Agent 能修改什么？
- Agent 能向哪些外部系统发送数据？
- 哪些操作必须让用户确认？

默认按照最低权限设计：只读优先，写入按目录和资源限制，网络按域名或服务限制，凭据通过平台代理注入而不是直接放进 Prompt。外部消息、网页内容和文件内容都视为不可信输入，不能让它们改变 Agent 的权限或系统规则。

## 三、从 0 到 1 的标准推进流程

### 阶段 0：建立实验环境

交付物：

- OpenClaw 本地开发环境
- 一个可访问的模型 provider
- 一个独立的 Agent 工作区
- 一份平台字段和操作 SOP
- 一份运行日志和问题记录模板

验收：能启动 Gateway，创建会话，完成一次简单任务，并能找到本次运行的输入、工具调用、输出和错误。

### 阶段 1：选择可验证的任务

选题用五个维度评分，每项 1 到 5 分：

| 维度 | 判断问题 |
| --- | --- |
| 场景强度 | 是否有明确人群、触发时机和真实痛点？ |
| 可验证性 | 是否能明确判断成功或失败？ |
| 交付价值 | 是否能产出用户可直接使用的结果或文件？ |
| 技术优势 | OpenClaw 的工具、浏览器、文件或本地环境是否能形成优势？ |
| 复用潜力 | 是否能沉淀成模板、Skill、Tool 和 Benchmark？ |

第一项实验不追求复杂，优先选择输入和输出清楚、没有高风险外部操作、能够在 5 到 15 分钟内完成的任务。

### 阶段 2：先做最小可运行 Agent

实现顺序：

1. 先固定输入格式和最终输出格式。
2. 写出最短的任务流程和停止条件。
3. 接入一个最必要的工具。
4. 加入工具参数校验和错误信息。
5. 让 Agent 完成一条完整路径。
6. 再补充重试、缓存、并行和复杂编排。

最小 Agent 至少要有：

- 清楚的触发条件
- 结构化输入
- 明确的工具选择规则
- 中间状态或进度信息
- 结构化输出
- 失败原因和下一步建议
- 不会无限循环的最大步骤数或最大时长

### 阶段 3：在 OpenAgent 创建 Agent 和 Version

建议按这个顺序操作：

1. 创建 Agent 主对象，让平台自动生成 Agent ID 和账户。
2. 创建第一个开发版本，例如 `0.1.0-dev`。
3. 选择连接本地 Agent，先验证本地 OpenClaw 能否被平台调用。
4. 如果本地连接不适合发布，再创建沙箱 Agent 程序。
5. 配置工作区、模型、工具、环境变量和权限。
6. 在 WebUI 调试一次完整任务。
7. 记录真实输入、工具调用、输出、耗时和错误。
8. 编写 Agent Card，明确能力、限制、输入输出和示例。
9. 暂不把价格作为产品验证的阻塞项，只确认平台后续需要哪些 Pricing 字段。
10. 保存为草稿，完成测试后再创建可发布版本。

不要一开始直接覆盖已发布版本。每个有行为变化的修改都创建新版本，并保留旧版本的 Benchmark 结果。

### 阶段 4：调试和工程化

每次调试都按四层排查：

1. 平台层：版本是否生效、请求是否到达、账户和权限是否正确。
2. Gateway 层：会话是否创建、配置是否加载、运行是否超时。
3. Agent 层：是否选对工具、参数是否正确、是否遵守停止条件。
4. 外部系统层：API、网页、文件、模型是否返回异常。

每个工具都设计明确的错误返回，例如：

```json
{
  "ok": false,
  "error_code": "SOURCE_UNAVAILABLE",
  "message": "目标数据源暂时不可用",
  "retryable": true,
  "next_action": "等待 30 秒后重试，或让用户提供备用数据源"
}
```

重点记录：首次响应时间、总耗时、工具调用次数、重试次数、上下文长度、失败节点、结果完整度和人工介入次数。

### 阶段 5：建立 Benchmark

Benchmark 要覆盖真实使用，而不是只测试正常示例。至少准备：

- 5 个标准成功任务
- 3 个缺少信息的任务
- 3 个边界任务
- 3 个错误或不可用数据源任务
- 2 个恶意或越权诱导任务
- 2 个需要用户确认的任务

评分建议分成五项：

| 指标 | 权重建议 | 判断方式 |
| --- | ---: | --- |
| 任务完成度 | 30% | 是否完成用户真正要求 |
| 结果正确性 | 25% | 事实、计算、引用是否正确 |
| 交付格式 | 15% | 是否符合约定的结构和文件格式 |
| 工具效率 | 10% | 是否存在多余调用、循环和无效重试 |
| 安全与边界 | 20% | 是否拒绝越权、泄露和危险动作 |

发布前至少达到：标准任务成功率 80% 以上，越权任务拒绝率 100%，不可用数据源能给出明确错误，输出格式合规率 95% 以上。具体门槛可根据任务风险调整。

### 阶段 6：发布和用户验证

发布前检查：

- Agent Card 是否能让用户在 30 秒内理解用途
- 示例输入是否真实且能复现
- 输入输出是否明确
- Pricing 字段是否完整，具体价格由创建者后续配置
- 是否说明需要的权限和数据
- 是否存在高风险动作
- Benchmark 是否绑定当前版本
- 是否能回滚到上一版本
- 是否有运行日志、错误统计和用户反馈入口

发布后每周看四类数据：曝光到试用转化率、首次任务成功率、重复使用率、用户主动修改率和失败率。优先解决高频失败任务，再新增能力。

## 四、推荐的项目目录和 Agent 包结构

建议每个 Agent 单独一个仓库或目录，避免多个 Agent 共用不可追踪的 Prompt 和配置：

```text
agent-name/
├── README.md
├── agent-card.yaml
├── pricing.yaml
├── openclaw/
│   ├── skills/
│   │   └── main-skill/
│   │       └── SKILL.md
│   ├── plugins/
│   └── config/
├── tools/
│   ├── tool-name.ts
│   └── schemas.ts
├── evals/
│   ├── cases.jsonl
│   ├── rubric.md
│   └── expected/
├── fixtures/
├── docs/
│   ├── permissions.md
│   └── troubleshooting.md
└── CHANGELOG.md
```

`agent-card.yaml` 描述用户可见能力，`SKILL.md` 描述任务方法，`tools/` 实现可验证能力，`evals/` 保护质量，`permissions.md` 约束安全边界。平台发布版本时应把这些内容与版本号绑定。

## 五、建议的 4 周工作节奏

### 第 1 周：熟悉平台、OpenClaw 和产品方法

- 跑通 OpenClaw 最小示例
- 阅读并记录 Gateway、Skill、Tool、Plugin、MCP、安全和沙箱
- 在 OpenAgent 中走通创建 Agent、创建 Version、调试和保存草稿
- 完成平台字段 SOP
- 对现有 Agent 市场做场景、交付物和用户吸引力拆解

### 第 2 周：做一个最小 Agent

- 写 Agent Brief
- 明确输入、输出、权限和停止条件
- 实现一个 Skill 和一个必要工具
- 接入本地 OpenClaw
- 完成 10 个手工任务

### 第 3 周：工程化和评估

- 补充错误处理、超时、重试和日志
- 创建 18 到 20 个 Benchmark 任务
- 对比至少两个版本
- 修复最高频的失败类型

### 第 4 周：发布内测版本

- 完成 Agent Card、Pricing 字段、权限说明和示例
- 创建 `0.1.0-beta` 版本
- 在小范围用户中试用
- 每天记录成功率、耗时、失败和反馈
- 根据数据决定继续优化、换题目或下线

## 六、团队分工建议

一个 Agent 不应只由一个人凭感觉调 Prompt。最小团队可以这样分工：

- 产品：定义目标用户、使用场景、任务流程、交付物、成功标准和发布信息；价格只负责确认字段，不负责当前阶段的定价策略
- Agent 工程：OpenClaw、Skill、Tool、MCP 和运行逻辑
- 数据/评估：Benchmark、评分、回归和质量报告
- 体验/运营：示例任务、市场文案、试用反馈和数据分析
- 平台/安全：沙箱、权限、凭据、审计、版本和结算

早期一人多岗可以，但角色职责不能消失。

## 七、最容易犯的错误

1. 先做一堆通用 Agent，再寻找用户需求。
2. 把 OpenClaw 当作一段 Prompt，而没有工作流和工具。
3. 只测成功案例，不测缺少信息、工具失败和越权请求。
4. 让 Agent 直接持有长期凭据或宿主机高权限。
5. 只看模型回答好不好，不记录任务完成度、交付物质量和用户是否愿意继续使用。
6. 修改线上版本而不保留版本和 Benchmark 对比。
7. 用复杂多 Agent 编排掩盖单 Agent 任务边界不清的问题。
8. 只写平台展示文案，没有可验证的最终交付物。

## 八、从今天开始的具体动作

今天先完成三件事：

1. 把 OpenAgent 平台的创建、版本、调试、Agent Card、Pricing、保存、发布和 Benchmark 页面走一遍，形成字段 SOP；Pricing 先只记录字段和配置位置。
2. 在本地跑通 OpenClaw Gateway、会话、一个 Skill、一个 Tool 和一次日志查看。
3. 选一个低风险、输入输出清楚的任务，写出 Agent Brief 和 18 个 Benchmark 用例。

做到这三件事后，再决定第一个 Agent 的具体题目。这样后续制作的每个 Agent 都可以复用同一套目录、权限、测试、版本和发布方法。

## 完成定义

一个 Agent 达到 0 到 1 完成状态，需要同时满足：

- 能在 OpenAgent 平台创建并识别一个独立 Agent ID
- 至少有一个可运行的 Agent Version
- 基于 OpenClaw 完成真实任务，而不是只返回模板文本
- 至少调用一个真实工具或数据源
- 有明确的 Agent Card、输入输出和权限说明
- 有结构化错误、超时和失败处理
- 有 Benchmark 和可复现测试结果
- 有版本号、运行日志和回滚路径
- 有明确的目标用户、应用场景和用户使用理由
- 有可直接使用的交付物和示例任务
- 经过小范围用户内测并完成一次迭代

## 九、其他平台如何把 Agent 做成可用的应用

### 9.1 先区分“市场展示”和“实际能力”

扣子、智谱、阿里百炼等平台的作品市场，用户首先看到的不是 Prompt、插件或工作流节点，而是一个可以立即试用的应用入口。截图中 OpenAgent 作品社区也采用了类似结构：Agent 名称、开发者、短简介、标签、用户或收藏数据，以及“立即试用”入口。这说明市场竞争的第一层是“用户能否在几秒钟内理解它能帮我完成什么”，第二层才是实现方式。

可以把其他平台的做法归纳为四层：

| 层次 | 用户看到的内容 | 产品真正要解决的问题 |
| --- | --- | --- |
| 发现 | 名称、封面、简介、标签、热度 | 用户是否愿意点击 |
| 启动 | 示例问题、表单、文件上传、选项 | 用户是否知道如何开始 |
| 执行 | 多轮对话、步骤进度、工具调用、预览 | Agent 是否真的完成任务 |
| 交付 | 文本、图片、视频、表格、网页或文件 | 结果是否能直接使用 |

### 9.2 通用 Agent 为什么容易被使用

市场上表现较好的 Agent，通常把一个宽泛目标包装成一个具体任务。例如“内容创作助手”会进一步变成“输入一个主题，生成小红书图文”“上传长视频，找出 5 个适合发布的片段”“输入文章，输出 6 格漫画长图”。这种包装有三个特点：

1. 用户目标单一，第一次使用不需要学习产品结构。
2. 输入和输出可预期，用户能判断是否值得试用。
3. 中间过程被隐藏或简化，用户感受到的是完成工作，而不是配置 Agent。

高质量 OpenClaw Agent 要在这个基础上增加“真实执行能力”：处理文件和项目目录，调用专用工具，保存中间产物，允许局部重做，并对最终结果做检查。这样用户仍然可以简单启动，但 Agent 的内部完成质量比一次性问答更稳定。

### 9.3 其他平台常见的产品机制

| 机制 | 常见做法 | 对 OpenAgent 的启发 |
| --- | --- | --- |
| 场景命名 | 用“结果 + 场景”命名，如视频剪辑、漫画创作、运营助手 | 名称应直接表达任务和交付物，少用技术名词 |
| 快速启动 | 预置示例问题、表单和文件上传 | Agent Card 中必须有可复制的示例输入 |
| 输入收敛 | 用下拉项、上传框、主题字段收集关键信息 | 先设计输入契约，再设计对话追问 |
| 能力组合 | 把分析、生成、改写、排版、导出串成一条流程 | OpenClaw Skill 负责流程，Tool 负责可验证动作 |
| 即时反馈 | 展示处理中、当前步骤或部分结果 | 长任务要有阶段状态和中间产物 |
| 多格式交付 | 同时输出文字、图片、视频、表格或文件 | 交付物要在 Agent Brief 中提前定义 |
| 可编辑结果 | 允许重写某段、重画某格、替换素材 | 设计局部重做入口，避免每次从头运行 |
| 示例驱动 | 市场页用真实案例展示前后效果 | 准备 3 个真实任务和理想输出样例 |
| 数据沉淀 | 收藏、历史记录、最近使用、版本或模板 | 让用户能复用项目和偏好，形成回访理由 |
| 运营分发 | 分类、搜索、榜单、热度、推荐 | 标签要按用户任务设计，不只按技术能力分类 |

### 9.4 普通 Prompt Agent 与高质量 OpenClaw Agent

| 维度 | 普通 Prompt Agent | 高质量 OpenClaw Agent |
| --- | --- | --- |
| 输入 | 一段自然语言 | 文件、素材、结构化参数和上下文 |
| 工作方式 | 一次回答 | 多阶段、可恢复的任务流程 |
| 工具 | 泛化插件或搜索 | 针对任务设计的工具链和数据处理 |
| 输出 | 文本或简单图片 | 视频、时间轴、分镜、项目文件、报告等交付物 |
| 状态 | 无状态或只保留聊天 | 保留项目、角色、素材、中间结果和版本 |
| 质量 | 主要靠用户主观判断 | 规则检查、格式检查和 Benchmark |
| 交互 | 问一句答一句 | 引导式输入、进度、预览和局部修改 |
| 失败处理 | 重新提问 | 说明失败节点，支持重试、替换输入或人工接管 |
| 技术差异 | Prompt 配置 | OpenClaw runtime、Skill、Tool、Plugin 和工作区 |

## 十、剪辑视频 Agent 产品蓝图

### 10.1 这类 Agent 应该卖什么价值

“剪辑视频”不是一个足够具体的产品定位。用户真正要完成的通常是以下任务之一：

- 从一小时直播或课程中找出可发布的短视频片段。
- 把口播原片整理成节奏更好、字幕更清楚的成片。
- 根据商品卖点生成适合短视频平台的商品宣传视频。
- 把一个视频改编成多个平台、多个时长和多个比例的版本。
- 从视频内容中提取标题、封面文案、发布文案和标签。

因此首个视频 Agent 不应宣称“什么视频都能剪”，而应选择一个明确的主任务。例如“长视频切片助手：上传访谈或直播，输出 3 个可发布短视频方案和对应剪辑素材”。这个定位比“AI 视频剪辑专家”更容易理解、测试和传播。

### 10.2 目标用户和触发场景

| 用户 | 高频触发 | 当前替代做法 | Agent 的核心价值 |
| --- | --- | --- | --- |
| 自媒体运营 | 每周处理多条长视频 | 人工观看、标记、剪辑、写文案 | 缩短找素材和包装时间 |
| 课程/知识创作者 | 一次课程拆多条内容 | 手工找知识点和补字幕 | 按知识点批量切片 |
| 直播运营 | 直播结束后复用内容 | 回看录像、凭经验找高光 | 识别高价值片段和完整语义 |
| 品牌营销 | 为一个商品做多版本视频 | 多人协作改稿和剪辑 | 根据卖点生成平台适配版本 |
| 普通用户 | 家庭或旅行视频整理 | 手动筛选和拼接 | 快速得到主题化成片 |

### 10.3 用户输入契约

最少需要收集：

```text
视频或音频素材：必填
目标用途：短视频发布 / 课程切片 / 商品宣传 / 个人记录
目标平台：抖音 / 小红书 / 视频号 / B站 / 不确定
目标时长：15秒 / 30秒 / 60秒 / 自定义
风格：信息密度高 / 情绪感染 / 专业讲解 / 商品转化
是否保留原声：是 / 否
品牌或账号要求：可选
```

Agent 不应一开始让用户填写过多字段。缺少关键字段时，用一到两个问题追问；有合理默认值时直接执行，并把默认假设展示给用户。

### 10.4 推荐工作流

```text
上传素材
  -> 读取时长、音轨、画面和字幕信息
  -> 转写并按语义切分
  -> 识别高价值片段和完整表达
  -> 根据平台、时长和风格生成剪辑策略
  -> 生成候选时间轴、标题、字幕和封面文案
  -> 检查片段完整性、字幕错误和时长限制
  -> 用户预览并选择保留/修改/重做
  -> 导出视频、剪辑指令或工程文件
  -> 输出发布文案和后续复用建议
```

这里的关键不是让模型“想象一个视频”，而是让它围绕真实素材做可追溯的选择。每个候选片段都应能回到原视频的开始时间、结束时间、主题、核心观点和选择理由。

### 10.5 中间产物和最终交付物

中间产物决定用户是否能修改和信任结果：

- 素材解析报告：文件、时长、音轨、画面比例、语音质量。
- 分段转写：带时间戳的文本和主题标签。
- 候选片段表：开始时间、结束时间、核心内容、适合平台、推荐理由。
- 剪辑方案：开头钩子、正文顺序、结尾动作、字幕节奏和 B-roll 建议。
- 字幕稿：原始字幕、修订字幕、重点词强调方式。

最终交付物可分为三档：

1. 基础交付：候选片段清单、剪辑时间轴、标题、封面文案和发布文案。
2. 标准交付：带字幕、画幅和音频处理的可预览视频。
3. 完整交付：多个平台版本、素材清单、剪辑工程或可继续编辑的项目文件。

产品上应明确当前版本承诺哪一档，避免市场卡片写“自动生成视频”但实际只输出脚本。

### 10.6 质量标准

视频 Agent 的验收不能只看“看起来像不像成片”，至少要检查：

| 检查项 | 合格标准 |
| --- | --- |
| 内容完整 | 片段开头和结尾不截断关键语义 |
| 时间准确 | 时间轴与原视频内容对应 |
| 字幕质量 | 人名、数字、专有名词和断句可读 |
| 平台适配 | 时长、比例、标题长度符合目标平台要求 |
| 节奏 | 开头迅速进入主题，冗余停顿得到处理 |
| 交付可用 | 文件可播放，下载或继续编辑路径明确 |
| 可修改 | 用户能针对片段、字幕或标题局部重做 |

### 10.7 OpenClaw 的产品级差异化

技术同事负责实现，但产品需要明确这些能力的业务价值：

- 通过工作区保存原视频、转写、候选片段和导出文件，用户可以继续上次项目。
- 通过 Skill 固化“找高光”的判定规则、平台规范和品牌语气。
- 通过 Tool 处理转写、时间戳、剪辑指令、字幕和文件导出。
- 对多个候选片段并行分析，再按统一标准排序。
- 只重新处理用户改动的片段、标题或字幕，不必整条流程重跑。
- 失败时指出是文件、转写、素材不足还是导出环节失败，并给出下一步。

### 10.8 可拆分的产品线

不要把所有能力堆进一个入口，可以按明确任务拆成：

- 长视频高光切片 Agent
- 口播视频字幕和节奏优化 Agent
- 直播回放内容复用 Agent
- 电商商品视频包装 Agent
- 视频多平台改编 Agent
- 视频内容复盘与选题 Agent

其中“长视频高光切片”最适合作为首个验证题目，因为输入、过程和输出都比较清楚，用户也容易提供真实素材。

## 十一、图文漫画创作 Agent 产品蓝图

### 11.1 这类 Agent 应该卖什么价值

“生成漫画”也不是完整的产品定义。用户可能要的是把一篇文章变成可发布长图，把一段故事变成分镜，把知识内容变成易传播的科普漫画，或持续维护一套角色和世界观。真正的产品价值不是单张图片生成，而是让多页内容在人物、画风、叙事、文字和版式上保持一致。

建议把第一个产品定位为“文章转图文漫画：输入文章或主题，输出可发布的分镜、漫画长图和配套文案”。它具有清楚的内容入口，也能充分体现 OpenClaw 对长流程、文件和中间状态的支持。

### 11.2 目标用户和触发场景

| 用户 | 高频触发 | 当前替代做法 | Agent 的核心价值 |
| --- | --- | --- | --- |
| 自媒体作者 | 把文章改成更易传播的内容 | 写脚本、找画师、排版 | 降低图文内容制作成本 |
| 品牌营销 | 制作品牌故事或活动漫画 | 多轮策划和设计协作 | 快速得到可评审初稿 |
| 教育创作者 | 把知识点变成漫画 | 手工拆知识点和画面 | 保证知识结构清楚 |
| 小说作者 | 把章节转成分镜或宣传图 | 人工提取角色和场景 | 快速生成视觉化内容 |
| 连载团队 | 维护固定角色和画风 | 反复提供参考图 | 保留角色资产并支持局部更新 |

### 11.3 用户输入契约

```text
主题、文章或故事原文：必填其一
内容用途：科普 / 品牌故事 / 小说片段 / 日常连载
画面形式：单页 / 多页长图 / 九宫格 / 漫画视频脚本
期望页数或格数：可选
角色信息：可选，支持已有角色卡
视觉风格：写实 / 扁平 / 国风 / 轻松手绘 / 自定义参考
画幅和发布平台：可选
品牌词、禁用元素和版权要求：可选
```

如果用户只有一个主题，Agent 应先给出故事方向和页数建议，再进入生成；如果用户已经提供完整脚本，Agent 应减少重复创作，优先做结构提取、分镜和视觉规划。

### 11.4 推荐工作流

```text
输入文章、主题或脚本
  -> 提取人物、事件、冲突、知识点和结论
  -> 生成故事大纲与页数建议
  -> 创建角色卡、场景卡和视觉风格规范
  -> 生成逐页/逐格分镜脚本
  -> 用户确认叙事和角色设定
  -> 按统一设定生成画面
  -> 检查角色、画风、动作、文字和页面顺序
  -> 生成气泡、旁白和版式
  -> 用户局部重画、改文案或调整顺序
  -> 导出单页图、漫画长图、图片包和发布文案
```

角色设定和分镜确认是重要的产品节点。若直接从文章一步生成整套漫画，错误通常要到最后才暴露，用户也无法知道应该修改故事、人物还是某一个画面。

### 11.5 中间产物和最终交付物

建议把每次创作保存成一个项目，至少包含：

- 故事大纲：主题、受众、情绪、冲突和结论。
- 角色卡：姓名、身份、外观特征、服装、性格、禁用变化。
- 场景卡：地点、时间、光线、关键道具和背景规则。
- 视觉规范：画风、色彩、线条、镜头和文字规范。
- 分镜表：页码、格号、画面描述、人物动作、对白、旁白和构图。
- 生成记录：使用的参考图、版本、失败原因和用户修改。

最终交付物包括：

1. 可审阅交付：故事大纲、角色卡和分镜表。
2. 可发布交付：逐页图片、漫画长图、对白排版和封面。
3. 可复用交付：角色资产、场景资产、无字图、文字层和下一集创作入口。

### 11.6 质量标准

| 检查项 | 合格标准 |
| --- | --- |
| 叙事连贯 | 页面顺序清楚，读者能理解起因、过程和结果 |
| 角色一致 | 发型、服装、年龄、主要特征跨页稳定 |
| 画风一致 | 线条、色彩、视角和整体风格不突然变化 |
| 信息准确 | 科普、品牌和原文中的关键事实没有被改错 |
| 文字可读 | 气泡和旁白没有错字、遮挡和超出边界 |
| 版式可用 | 手机阅读时字号、画幅和阅读顺序清楚 |
| 局部可改 | 可单独重画某格、修改某句对白或调整某页 |

### 11.7 OpenClaw 的产品级差异化

- 用项目工作区保存文章、角色卡、场景卡、参考图、分镜和导出版本。
- 用 Skill 固化叙事拆解、分镜规则、角色描述格式和平台排版规范。
- 用 Tool 管理素材、生成图像、合成文字层、拼接长图和导出文件。
- 用结构化状态记录当前处于“大纲、分镜、生成、检查还是导出”阶段。
- 允许对单页或单格执行局部重做，保留其他页面结果。
- 对角色和画风执行检查，发现不一致时标出具体页码和原因。
- 对连续创作保留上一集的角色和视觉资产，形成复用和回访理由。

### 11.8 可拆分的产品线

- 文章转漫画 Agent
- 知识科普漫画 Agent
- 小说章节转分镜 Agent
- 品牌故事漫画 Agent
- 漫画转视频脚本 Agent
- 连载漫画创作工作台 Agent

其中“文章转漫画”适合验证首次使用，“连载漫画工作台”适合验证项目状态、资产复用和用户留存。两者不应在第一个版本中混成一个复杂入口。

## 十二、面向 OpenAgent 的通用 Agent 设计模板

每个候选 Agent 在进入技术开发前，产品至少提交下面这张设计卡：

```text
Agent 名称：
一句话价值主张：用户提供什么，Agent 交付什么
目标用户：具体人群，不写“所有人”
触发场景：什么时间、因为什么任务而使用
用户当前做法：使用哪些工具、花多长时间、哪里最痛
核心任务：只写本版本最重要的一件事
明确不做：列出相邻但不承诺的能力
用户输入：文件、文本、结构化字段和可选信息
追问规则：什么信息缺失时必须询问，什么信息使用默认值
任务阶段：按顺序列出分析、执行、检查和交付
知识/规则：行业规范、模板、案例、禁用项和判断标准
工具能力：每个工具的用途、输入、输出和失败处理
中间产物：用户可查看、修改或确认的阶段结果
最终交付物：文件、文本、图片、视频或可继续执行的项目
用户可修改节点：允许局部重做的对象和入口
权限边界：读取、写入、外部发送和必须确认的动作
质量标准：可观察、可评分、可复现
能力边界：输入限制、时长限制、格式限制和不适用场景
示例任务：至少 3 个真实可复制的输入
市场卡片文案：名称、简介、标签、适用人群、示例和交付物
Benchmark 任务：成功、缺失信息、边界、失败和越权样例
版本策略：本版本新增能力、已知问题和回滚版本
```

### 12.1 设计评审时要问的十个问题

1. 用户能否在 30 秒内说清楚这个 Agent 做什么？
2. 用户第一次使用是否能在一个入口内开始？
3. 输入是否真实存在，用户是否拿得出来？
4. 最终结果是否能直接使用或继续编辑？
5. 哪一步是通用聊天很难稳定完成的？
6. 哪些步骤必须调用工具，而不是继续生成文字？
7. 用户在哪些节点需要确认，哪些节点可以自动执行？
8. 结果出现问题时，用户能否定位并局部修改？
9. 是否有足够真实的 Benchmark 判断它比通用 Agent 更好？
10. 用户完成第一次任务后，为什么还会再次打开它？

### 12.2 从竞品借鉴什么，不直接照搬什么

可以借鉴其他平台的“低门槛入口”：清楚的名称、真实示例、预置输入、即时试用和结果导向的市场卡片。也可以借鉴它们按场景分类和用热度数据降低选择成本的方式。

OpenAgent 应进一步强化项目化能力：保留素材和中间产物，支持多步骤运行，展示进度，允许局部重做，提供可下载或可继续编辑的交付物，并用 Benchmark 说明质量。这样对用户仍然简单，对开发者则有更完整的能力承载。

### 12.3 产品负责人实际推进顺序

对剪辑视频或图文漫画这类 Agent，建议按以下顺序推进：

1. 先选一个单一主任务，写出目标用户、触发时机和最终交付物。
2. 收集 10 个真实输入样本，不用先收集大量泛化需求。
3. 把任务拆成阶段，明确哪些阶段用户需要确认。
4. 先画出中间产物，不先讨论技术组件名称。
5. 为每个阶段定义工具、知识、权限和失败处理要求。
6. 写 3 个理想结果和 3 个失败结果，作为技术联调依据。
7. 让技术同事用本地 OpenClaw 跑通一条完整路径。
8. 在 OpenAgent 建立 Agent Version，检查平台调试、文件、状态和交付体验。
9. 用 Benchmark 和 5 到 10 位目标用户试用，优先修复最高频失败点。
10. 再扩展相邻能力，形成同一场景下的 Agent 产品线。

这套顺序的核心是先证明“用户任务和交付物成立”，再扩大工具和能力范围。一个能稳定完成单一任务的 Agent，通常比一个能力列表很长但结果不可控的 Agent 更适合上架和获得首批用户。

## 十三、冷启动阶段的工作职责和总体方法

你的职责可以定义为：**发现值得做的用户任务，定义 Agent 产品方案，推动 Agent 可运行交付，建立可复现的效果评测，并根据数据决定上架、迭代或淘汰。**

这不是单纯的选题工作，也不是单纯的 Prompt 调试。每个 Agent 都要完成下面这条链路：

```text
市场机会
  -> 选题立项
  -> Agent Brief
  -> Vibe Coding 开发
  -> 可运行交付
  -> 对话式 Benchmark
  -> 竞品对测
  -> 小范围试用
  -> 发布/迭代/淘汰
```

冷启动阶段建议把“上架数量”改成三个组合目标：

1. **覆盖面**：覆盖办公、创作、开发、营销、生活等不同入口，帮助平台形成市场丰富度。
2. **示范性**：选择能体现文件处理、多步骤执行、工具调用和可交付文件的 Agent，证明平台不是普通聊天机器人。
3. **复用性**：优先做能沉淀通用 Skill、Tool、模板和评测集的 Agent，后续开发速度会越来越快。

你每天需要维护的不是一张“Agent 名单”，而是四张表：

| 表 | 记录什么 | 用途 |
| --- | --- | --- |
| 选题池 | 用户、任务、竞品、需求证据、优先级 | 决定做什么 |
| 开发看板 | Brief、开发状态、阻塞项、交付物 | 推动做出来 |
| 评测库 | 测试对话、输入文件、评分和版本 | 判断效果 |
| 发布观察表 | 试用、成功、失败、反馈和复用 | 决定下一步 |

## 十四、先盘点已有 Agent，再寻找新增选题

### 14.1 存量清单需要先去重和归并

按你本次逐项提供的名称统计，清单实际约为 63 项，和“已有 60 个 Agent”的口径不一致，需要先和后台 Agent 列表核对。产品上还存在以下潜在重复或强关联：

| 现有组合 | 问题 | 建议处理 |
| --- | --- | --- |
| 视频剪辑、素材初剪 | 用户难以区分“选素材”和“完成剪辑” | 定义为素材筛选版与成片版两个明确层级 |
| SEO 内容生产、SEO 优化、SEO 优化执行 | 名称和用户目标高度接近 | 统一为 SEO 内容策略、内容生成、执行监测三个阶段，必要时合并市场入口 |
| 建站大师、企业官网、电商站 | 都可能被理解为建网站 | 分别限定为个人站、企业官网、电商转化站 |
| 商品数据、商品图标准化 | 都服务电商商品资料 | 可组成“商品资料标准化”产品线，区分数据和图片能力 |
| 视频生成、数字人口播、脚本大师、音频生成、播客生成 | 都属于内容生产链 | 按脚本、声音、数字人、剪辑、播客成片拆成输入输出明确的 Agent |
| 竞品分析、深度调研、消费者调研、新闻雷达、趋势雷达、账号雷达 | 都有信息搜集和分析能力 | 统一研究方法和引用规范，按研究对象区分产品定位 |
| 投行简报、深度研报、财报解读、个股分析、金融可视化 | 都属于金融研究内容 | 分别定义输入材料、研究深度和交付格式，避免重复覆盖同一任务 |

这一步的产物应是“能力地图”，不是把重复的 Agent 简单删掉。能力地图需要标出每个 Agent 的：目标用户、主任务、输入、交付物、核心工具、复用 Skill、相邻 Agent 和真实使用频率。后续新选题不能只判断“市场上有没有类似产品”，还要判断“平台现有产品是否已经覆盖了这个任务”。

### 14.2 用五项评分筛选新增选题

候选题目先按 1 到 5 分评分，再决定是否进入开发：

| 维度 | 5 分标准 | 冷启动解释 |
| --- | --- | --- |
| 需求强度 | 高频、明确、用户已有替代工具 | 用户有马上使用的理由 |
| 交付价值 | 直接产出文件、结论或下一步动作 | 用户能判断结果有没有用 |
| 差异化 | 通用聊天或现有 Agent 难以稳定完成 | 能体现 OpenClaw 的执行优势 |
| 传播性 | 一句话可理解，前后效果易展示 | 容易做市场卡片和案例 |
| 可开发性 | 输入可获得、工具可接入、风险可控 | 能在一到两周做出可测版本 |
| 复用性 | 能沉淀模板、工具和测试数据 | 后续 Agent 可以复用 |

总分建议达到 22 分以上才进入第一批开发。需求强度或交付价值低于 3 分，即使技术上有趣，也先放入观察池。评分必须附证据：竞品上架数量、用户评论或收藏、搜索/咨询记录、内部用户访谈、真实文件样本，不能只写“感觉有市场”。

### 14.3 建议优先研究的新增方向

以下方向与已有 60 多个选题有明显区隔，也适合做成可交付的 OpenClaw Agent。这里是候选池，正式立项仍要经过评分和样本验证：

| 优先级 | Agent 方向 | 核心交付物 | 为什么适合冷启动 |
| --- | --- | --- | --- |
| A | PPT/汇报材料生成 Agent | PPTX、页面大纲、演讲稿、素材清单 | 办公需求明确，结果容易展示和对测 |
| A | 会议资料到行动项 Agent | 会议纪要、决策、负责人、截止时间、跟进表 | 高频、复用强，适合文件和多轮修改 |
| A | 合同/制度/招投标文件审阅 Agent | 风险清单、条款对照、修改建议、审阅报告 | 专业价值明确，通用聊天容易漏项 |
| A | 数据清洗与分析 Agent | 清洗后表格、字段说明、异常报告、分析结论 | 与 Excel/可视化区分，聚焦脏数据处理 |
| A | GitHub 项目问题处理 Agent | Issue 分析、修改方案、代码变更、测试报告 | 能体现代码执行和交付代码能力 |
| A | 客服知识库与工单处理 Agent | 问答、工单分类、回复草稿、升级判断 | 真实企业场景，具备高频重复使用价值 |
| B | 会议录音到周报 Agent | 会议转写、事项汇总、周报和下周计划 | 入口简单，适合快速做出可试用版本 |
| B | 旅行研究与行程工作台 Agent | 多日行程、预算表、预订清单、备选方案 | 消费者容易理解，展示形式丰富 |
| B | 求职投递工作台 Agent | 岗位解析、定制简历、求职信、面试准备包 | 与已有面试/简历设计形成完整链路 |
| B | 招投标响应材料 Agent | 需求应答矩阵、技术方案、偏离表、材料清单 | 文件密集、规则明确、交付价值高 |
| B | 论文资料整理与引用 Agent | 文献矩阵、研究问题、引用清单、综述提纲 | 与学术写作和论文图表形成前置能力 |
| B | 软件测试用例与验收 Agent | 测试场景、用例、验收清单、缺陷报告 | 研发团队高频使用，适合结构化交付 |
| C | 家庭/个人文件整理 Agent | 文件分类、摘要、待办和检索索引 | 场景直观，但需谨慎设计隐私边界 |
| C | 菜谱与采购规划 Agent | 周菜单、采购清单、营养约束和替换方案 | 消费者入口轻，适合作为生活类补充 |

建议第一批先从 A 类中选 4 至 6 个，再从 B 类中选 2 至 3 个，形成“办公生产力 + 专业文件 + 开发工具 + 消费者场景”的组合。不要一次开发所有候选题目。冷启动的目标是找出有效的开发和评测方法，并验证哪些类型能带来真实试用。

## 十五、用 Vibe Coding 开发时，产品必须定义“可交付”

### 15.1 产品不需要写代码，但不能只给一句需求

使用 Claude Code 等工具时，产品的输入应是一份“开发任务包”。至少包含：

```text
1. Agent Brief：给谁用、解决什么任务、明确不做什么
2. 用户流程：首次输入、追问、执行、确认、修改、交付
3. 输入契约：支持哪些文件/字段，缺少什么必须追问
4. 输出契约：必须生成哪些文件、字段、格式和命名
5. 能力分解：每一步由模型、Skill、Tool 或外部服务完成
6. 质量标准：什么结果算合格，哪些错误不能出现
7. 测试样本：真实输入、期望结果、边界和失败样本
8. 权限边界：读什么、写什么、哪些动作需要确认
9. 市场信息：名称、简介、标签、示例任务和封面展示重点
10. 运行约束：本地如何启动，OpenAgent 如何连接，依赖和环境变量
```

产品方案中不要写“做一个专业的 Agent”“效果要好”“自动完成全部流程”这类不可验收的句子。应改成“上传一份 60 分钟访谈，输出 5 个候选片段，每个片段包含时间戳、核心观点、标题和推荐理由，用户可选择其中一个生成字幕版视频”。

### 15.2 Vibe Coding 的分阶段交付

让 Claude Code 一次性生成完整产品，容易得到看似完整但无法稳定运行的代码。建议按四次交付推进，每次都要运行验证：

| 阶段 | 交付目标 | 产品验收 |
| --- | --- | --- |
| 0. 骨架 | 能启动、能接收输入、能返回固定结构 | 本地命令可运行，入口和依赖说明完整 |
| 1. 主流程 | 一条真实任务从输入到最终交付跑通 | 使用真实样本得到完整结果 |
| 2. 可靠性 | 补齐追问、错误、超时、重试和文件处理 | 异常样本不会卡死或静默失败 |
| 3. 交付 | 形成可接入 OpenAgent 的版本包 | 技术同事可复现部署，用户可下载/查看结果 |

每次让工具完成一小段后，要求它先运行命令并报告结果，再继续下一步。你不需要审查每一行代码，但必须看运行结果、产物和日志。产品验收的对象是用户能否完成任务，不是代码看起来是否复杂。

### 15.3 每个 Agent 的最低交付包

一个“已开发”不能只代表仓库里有代码。最低交付包应包括：

```text
agent-card.yaml 或等价信息
README.md：用途、启动、输入、输出和限制
运行配置：依赖、环境变量示例和模型要求
OpenClaw Skill：任务流程、追问、停止和输出规则
Tools/MCP：工具清单、参数、返回值和错误码
fixtures/：脱敏的真实输入文件
evals/：对话测试用例和评分标准
outputs/：至少一个可展示的完整样例
permissions.md：文件、网络、账号和写入权限
CHANGELOG.md：版本变化和已知问题
```

### 15.4 “能使用交付代码”的验收标准

交付代码必须同时满足：

- 新环境按照 README 可以启动，不依赖开发者个人电脑上的隐含配置。
- 产品提供的真实样本可以跑通完整流程，而不是只通过模拟数据。
- 输入错误、文件缺失、外部服务失败时会返回明确的下一步。
- 产物能在约定位置找到，并且文件可以打开、播放或继续编辑。
- 重新运行同一个任务不会污染上一次项目或产生无法区分的文件。
- 技术同事可以将同一版本接入 OpenAgent 的 Agent Version。
- 版本号、配置、评测结果和已知限制可以对应起来。

可以把交付分成三个状态：`可运行`、`可测试`、`可上架`。只有本地真实样本跑通，不能直接标记为可上架。

## 十六、Agent 的对话式 Benchmark 怎么做

### 16.1 测试对象不是一句回答

对话式 Agent 的测试单位应是一条完整任务会话：用户输入、Agent 追问、用户补充、工具调用、中间产物、用户修改和最终交付。测试结果要回答三个问题：

1. Agent 是否理解了真实目标？
2. Agent 是否按正确步骤完成了工作？
3. 用户是否拿到了可以使用的交付物？

### 16.2 每个 Agent 的最小测试集

第一版建议每个 Agent 准备 20 条测试会话：

| 类型 | 数量 | 测试重点 |
| --- | ---: | --- |
| 标准成功 | 5 | 正常输入能否完成主流程 |
| 缺信息追问 | 3 | 是否只追问必要信息，默认值是否合理 |
| 脏数据/复杂输入 | 3 | 长文、格式混乱、重复内容、大小写或语言混杂 |
| 多轮修改 | 3 | 用户要求局部修改时是否保留其他结果 |
| 边界任务 | 2 | 超出能力范围时是否清楚说明 |
| 工具或服务失败 | 2 | 失败是否可解释、可重试、可替换 |
| 越权/提示注入 | 2 | 是否拒绝泄露、越权写入或执行危险动作 |
 
对于文件型 Agent，每条测试都要绑定具体文件；对于生成型 Agent，要绑定期望的结构、关键字段和最低质量要求。测试输入必须脱敏，且保留来源和版本。

### 16.3 测试用例格式

可以让 Claude Code 按下面结构生成和运行测试：

```json
{
  "case_id": "ppt-001",
  "type": "standard_success",
  "user_goal": "把季度经营数据整理成管理层汇报材料",
  "turns": [
    {"role": "user", "content": "请根据 fixtures/q2.xlsx 做一份季度经营汇报"},
    {"role": "user", "content": "面向总经理，控制在 10 页以内，突出问题和下季度动作"}
  ],
  "required_outputs": ["pptx", "page_outline", "speaker_notes"],
  "must_include": ["收入变化", "问题分析", "下季度动作"],
  "must_not": ["捏造不存在的数据", "遗漏数据来源"],
  "rubric": {"completion": 30, "accuracy": 30, "format": 20, "usability": 20}
}
```

### 16.4 评分维度和发布门槛

建议每条会话按 100 分评分：

| 指标 | 分值 | 评分问题 |
| --- | ---: | --- |
| 任务完成度 | 30 | 用户真正要求的任务是否完成 |
| 内容正确性 | 25 | 事实、数字、引用和逻辑是否正确 |
| 交付物完整性 | 20 | 文件、字段、格式和命名是否齐全 |
| 可用性 | 15 | 用户是否可以直接使用或少量修改 |
| 对话与效率 | 5 | 是否少做无效追问和重复工作 |
| 边界与安全 | 5 | 是否遵守权限、隐私和能力边界 |

冷启动发布前可采用以下基础门槛：标准成功任务平均 80 分以上；所有高风险或越权用例不得通过；交付文件可打开率 100%；关键事实错误为 0；至少 80% 的任务不需要技术人员介入。对于法律、金融、医疗等高风险方向，应提高人工复核要求，不能只依靠模型评分。

### 16.5 测试执行流程

每个版本按同一顺序执行：

1. 固定模型、系统规则、工具版本和输入文件。
2. 先跑标准成功和失败样本，确认运行环境正常。
3. 跑完全部 20 条会话，保存完整对话和中间产物。
4. 用规则检查硬性要求，例如文件存在、字段齐全、数字一致、时间轴合法。
5. 由产品或领域人员进行主观评分，记录扣分理由。
6. 汇总失败类型，而不是只看总分。
7. 修复最高频问题后重新跑全量回归。
8. 把当前版本号、模型、配置和评分结果绑定保存。

每次评测至少保留：输入、输出、工具调用记录、耗时、错误、评分、评测人和版本号。没有原始记录的“测试通过”不可复查。

## 十七、如何做竞品对测

### 17.1 竞品测试的正确目标

竞品对测不是为了证明自己的 Agent 一定更好，而是找出：

- 竞品已经解决得很好的任务。
- 竞品没有解决、但用户确实需要的环节。
- 自己的 Agent 在交付物、稳定性或修改体验上的优势。
- 自己只是换了一个名称，实际没有产品差异的部分。

### 17.2 建立同题测试表

对每个候选方向，选择 3 个代表性竞品或平台 Agent，准备 5 条完全相同的任务。每次记录：

| 记录项 | 内容 |
| --- | --- |
| 测试任务 | 同一份文本、文件、图片或视频和同一目标 |
| 入口方式 | 对话、表单、上传、模板或工作流 |
| 首次响应 | 是否理解任务，是否会追问关键条件 |
| 执行过程 | 是否展示步骤、进度和中间结果 |
| 最终交付 | 具体文件、内容、格式和可编辑程度 |
| 修改成本 | 用户需要几轮修改，能否局部重做 |
| 错误表现 | 漏项、事实错误、格式错误、无法运行 |
| 时间和操作 | 完成耗时、用户点击和手工工作量 |
| 复用能力 | 是否能保存项目、模板、偏好和历史 |
| 证据 | 截图、导出文件、对话记录和测试日期 |

比较时要控制变量：相同输入、相同目标、相近模型能力、同样的工具权限，并注明竞品有而自己没有的外部服务。不要用竞品的最好案例和自己的第一次运行直接比较。

### 17.3 竞品对测的输出

每个方向形成一页结论：

```text
用户为什么使用这类 Agent：
竞品最强的三个环节：
竞品最常见的三个问题：
我们可以提供的明确差异：
必须达到的最低效果：
第一版本不做的能力：
需要接入的工具或数据：
建议是否立项：立项 / 观察 / 淘汰
```

“比竞品更好”必须具体化为可测指标，例如：同样一份长文，漏掉的关键条款更少；同一组视频，候选片段更完整；同一份数据，生成的 PPT 数字一致且能打开；用户完成局部修改少用两轮对话。不能只写“更智能”“更专业”。

## 十八、冷启动的周工作节奏

### 周一：选题和证据

- 查看上一周试用、失败、搜索和反馈数据。
- 从竞品市场补充候选题目。
- 更新现有 Agent 能力地图和重复项。
- 为候选题目补需求证据和五项评分。

### 周二：产品定义

- 选定本周开发的 1 到 2 个 Agent。
- 完成 Agent Brief、用户流程、输入输出契约和市场卡片。
- 准备至少 5 个真实输入样本和预期交付物。
- 和技术同事确认工具、环境、权限及不可做事项。

### 周三到周四：Vibe Coding 和联调

- 用开发任务包驱动 Claude Code 分阶段实现。
- 每个阶段都要求实际运行，保存日志和产物。
- 产品重点体验首次输入、追问、中间结果、修改和最终交付。
- 发现问题按“任务失败、结果错误、交付不可用、体验问题、技术阻塞”分类。

### 周五：评测和决策

- 跑完 20 条对话式 Benchmark。
- 对 3 个竞品做同题测试。
- 汇总分数、失败类型、修改成本和交付质量。
- 做出上架、继续修复、缩小范围或淘汰决定。

每周只要求 1 到 2 个 Agent 进入可测试状态，避免同时铺开过多题目导致每个 Agent 都没有真实验证。

## 十九、产品负责人的工作台和状态定义

建议用以下状态管理每个 Agent：

```text
候选
  -> 已验证需求
  -> 已完成 Brief
  -> 开发中
  -> 本地可运行
  -> OpenAgent 可调试
  -> Benchmark 通过
  -> 竞品对测完成
  -> 内测中
  -> 已发布 / 继续迭代 / 淘汰
```

每个状态必须有进入条件和退出证据：

| 状态 | 进入条件 | 退出证据 |
| --- | --- | --- |
| 已验证需求 | 有目标用户和至少 10 个任务样本 | Brief 和需求证据 |
| 本地可运行 | 主流程能用真实输入跑通 | 日志和完整交付物 |
| OpenAgent 可调试 | 平台版本能正常调用 | WebUI 调试记录 |
| Benchmark 通过 | 达到发布门槛 | 评测报告和原始记录 |
| 竞品对测完成 | 同题测试已完成 | 对测表和差异结论 |
| 内测中 | 目标用户开始试用 | 用户反馈和行为数据 |
| 已发布 | 市场信息、权限和版本完整 | 发布版本和回滚信息 |

你最重要的日常动作是推动状态向前移动，并及时把没有证据的“已完成”退回到上一状态。

## 二十、建议你现在立即执行的动作

1. 从 OpenAgent 后台导出或手工整理现有 Agent 清单，核对 60 个的准确口径，并完成去重、归并和能力地图。
2. 给每个存量 Agent 补上目标用户、主任务、输入、最终交付物和当前质量状态。
3. 从新增候选中优先挑选 PPT、会议行动项、专业文件审阅、数据清洗、GitHub 项目处理和客服工单六个方向做竞品验证。
4. 每个方向只找 3 个竞品、准备 5 条同题任务，先比较交付物和用户修改成本。
5. 选出 1 个办公类和 1 个技术类 Agent，分别写开发任务包和 20 条对话式 Benchmark。
6. 让 Claude Code 先交付“本地可运行”版本，再逐步推进到“可测试”和“可上架”。
7. 用一周完成首轮真实测试，依据失败类型决定是缩小场景、补工具、改流程还是淘汰题目。

冷启动阶段不应以“我已经让 Claude Code 写出了多少个 Agent”作为进度，而应以“有多少个 Agent 具备真实输入、可用交付物、可复现评测和明确的用户复用理由”作为进度。

## 二十一、标准化 Agent 工厂 SOP

为了让后续新增 Agent 只需要重新确定选题和测试效果，已将固定模块、选题变量、开发交付、对话式 Benchmark、竞品对测、平台调试、发布门槛和标准目录单独整理为：

[openagent-agent-standard-sop.md](C:\Users\admin\Documents\Codex\2026-09-07\ban\outputs\openagent-agent-standard-sop.md)

后续每个 Agent 只需复制 Topic Pack，填入用户、任务、输入、交付物、工具、样本、竞品和发布标准；其他基础模块按照该 SOP 直接复用。建议先用一个办公类 Agent 和一个技术类 Agent 验证 SOP，再批量推进。
