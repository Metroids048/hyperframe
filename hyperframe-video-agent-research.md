# Hyperframe 视频 Agent 首版研究与实现方案

## 1. 结论

首版建议做“商品/品牌短视频广告制作 Agent”，但采用**真实素材优先、生成素材补充**的路线：用户提供商品信息、图片或原视频，Agent 产出 15/30 秒广告的脚本、分镜、素材清单、配音/字幕方案和可预览视频。首版不承诺任意风格、任意时长和完全自动成片。

这个方向适合 Hyperframe 的原因是：公司已有生图、生视频 API，Agent 的核心价值可以放在需求澄清、广告结构、素材规划、模型路由、质量检查和可修改工作流，而不是重复建设底层模型。

## 1.1 为什么先选这个题目

这不是因为“广告视频”天然比动画或其他视频更高级，而是它在冷启动阶段同时满足五个可验证条件：

| 条件 | 该题目的表现 | 对 Hyperframe 的意义 |
|---|---|---|
| 输入边界清楚 | 商品信息、图片/原视频、平台、时长、风格 | 容易做 Brief 和结构化参数 |
| 输出可验收 | 脚本、分镜、15/30 秒视频、字幕、CTA | 可以定义明确的 Benchmark |
| 模型能力匹配 | 生图用于场景/B-roll，生视频用于镜头，合成服务完成成片 | 能直接验证公司模型 API 的业务价值 |
| 用户修改频繁 | 改卖点、改镜头、改字幕、改 CTA | 能体现 Hyperframe 的状态、版本和局部重做能力 |
| 商业价值可解释 | 一条素材能快速生成多个投放版本 | 用户容易理解“为什么要重复使用” |

相较于“动画创作 Agent”，广告题目的优势是交付标准和用户目标更具体；相较于“通用视频生成 Agent”，它减少了开放式创作空间；相较于已有的“视频剪辑/素材初剪/视频生成/数字人口播”选题，它的差异在于把商品 Brief、广告策略、生成素材和版本化交付串成一个完整任务。

因此选题是一个**用于验证 Hyperframe 编排能力的产品切片**，不是对最终视频产品线的永久限定。若 API 对参考图一致性或视频时长支持不足，应先退化为“广告脚本+分镜+时间轴+素材清单”，仍可验证 Agent 主流程。

## 1.2 Chatcut、ViduClaw 在 Hyperframe 中应处于什么位置

它们应被视为 Codex 可调用的外部插件/工具入口，而不是整个 Agent 的产品定义。Hyperframe Agent 负责判断“什么时候调用、传什么参数、如何检查结果、失败如何恢复”；Chatcut/ViduClaw 或公司模型 API 负责具体的视频处理动作。

推荐的适配层如下：

```text
Hyperframe Video Agent
  -> Video Tool Interface（统一参数和错误码）
      -> Chatcut connector（剪辑/时间轴/字幕能力）
      -> ViduClaw connector（视频生成/镜头生成能力）
      -> Company Image API adapter
      -> Company Video API adapter
```

这样即使某个插件不可用，Agent 仍可以切换到公司 API，或降级交付脚本和时间轴。插件的具体能力、鉴权和返回字段必须以其官方文档或已安装连接器的 schema 为准；当前环境未能访问外部搜索页面，不能把未经核实的插件细节写成确定事实。

## 2. 从 Chatcut、ViduClaw 类产品应借鉴的模式

当前工作区没有发现 `@chatcut` 或 `@viduclaw` 的源码/接口文档，因此这里按同类视频 Agent 的产品模式抽象，接入前需要技术同事用真实 API 再核对一次。

可复用的模式有四个：

1. **项目化而不是一次问答**：每个任务有素材、脚本、分镜、生成记录、版本和导出物。
2. **先规划再生成**：先让用户确认广告目标、受众、卖点和风格，再调用昂贵的视频模型。
3. **中间结果可审阅**：脚本、分镜、素材引用和时间轴都可单独修改；局部修改不重跑整个项目。
4. **异步任务与预览**：视频生成通常耗时，Agent 应返回任务状态、进度和可重试错误，而不是阻塞一次对话。

## 3. MVP 产品定义

### 用户

电商运营、品牌市场人员、短视频代运营和小型商家。

### 用户输入

- 商品名称与一句话卖点（必填）
- 商品图片或原视频（至少一种）
- 目标平台：抖音/视频号/小红书/通用
- 时长：15 秒或 30 秒
- 风格：产品展示、促销转化、生活方式
- 品牌色、禁用词、CTA（可选）

缺少素材时 Agent 只追问影响结果的条件；未指定平台时默认 9:16、30 秒，并明确告知用户。

### 输出

首版必须交付：

- 广告脚本（Hook、卖点、证明、CTA）
- 6~8 镜头分镜，包含画面描述、时长、旁白、字幕和素材来源
- 生成任务清单及每个任务的模型参数
- 字幕稿、标题、发布文案
- 一个可播放的预览视频，或在生成失败时提供可执行的时间轴 JSON

## 4. Agent 工作流与状态机

```text
INIT
 -> INTAKE（收集商品、素材、平台、时长）
 -> BRIEF（提炼受众、卖点、禁用项）
 -> SCRIPT（生成并检查脚本）
 -> STORYBOARD（生成分镜和素材计划）
 -> CONFIRM（用户确认脚本/分镜）
 -> GENERATE（并行调用生图、生视频、配音接口）
 -> ASSEMBLE（拼接、字幕、音乐、画幅）
 -> REVIEW（时长、字幕、素材一致性、合规检查）
 -> PREVIEW
 -> REVISE（局部重做）
 -> EXPORT
 -> COMPLETE
```

所有长耗时节点都应支持 `PENDING/RUNNING/SUCCEEDED/FAILED/CANCELED`。用户修改镜头 3 时，只重做镜头 3 及其依赖，不重新生成整条广告。

## 5. Hyperframe 中的组件边界

| 组件 | 职责 |
|---|---|
| 主 Agent/Skill | 识别任务、追问、状态路由、用户确认、错误解释 |
| 广告策划 Skill | 受众、卖点排序、脚本结构、CTA 和平台规则 |
| 分镜 Skill | 把脚本拆成镜头，定义画面、时长和素材依赖 |
| 模型路由 Tool | 根据镜头类型选择公司生图/生视频 API |
| 素材 Tool | 上传、校验、缩略图、元数据和安全扫描 |
| 生成 Tool | 创建异步任务、查询状态、下载结果、重试 |
| 合成 Tool | 按时间轴拼接视频、字幕、音频和转场 |
| 评测 Tool | 检查时长、比例、字幕、缺镜头、文件可播放性 |
| Workspace | 保存项目、版本、素材、任务和导出物 |

## 6. 建议的统一 API 契约

```json
POST /v1/video-projects
POST /v1/video-projects/{id}/plan
POST /v1/video-projects/{id}/generations
GET  /v1/video-tasks/{task_id}
POST /v1/video-projects/{id}/render
POST /v1/video-projects/{id}/revisions
GET  /v1/video-projects/{id}/artifacts
```

生成接口统一返回：

```json
{
  "task_id": "task_123",
  "status": "PENDING",
  "stage": "GENERATE",
  "provider_task_id": "company_456",
  "retryable": true,
  "estimated_seconds": 90
}
```

模型供应商应被适配器隐藏。Agent 只依赖统一字段：`prompt`、`input_assets`、`duration`、`aspect_ratio`、`seed`、`result_assets`、`error_code`。这样后续更换公司模型不会改变产品流程。

## 7. 产品必须提前确定的内容

产品负责人需要提供：定位、目标用户、输入最小集、两种默认风格、脚本模板、分镜字段、平台限制、禁用词规则、可接受的失败结果、三个成功样例和至少 18 条验收任务。技术同事负责 API、队列、存储、渲染、权限和日志。

## 8. 评测与发布门槛

首版用 20 个真实商品 Brief：10 个正常任务、3 个缺素材、3 个长文本/脏素材、2 个模型失败、2 个越权或违规请求。评分包括：卖点覆盖 25%、脚本可执行 20%、镜头与素材一致 20%、视频技术合规 20%、局部修改成功 15%。建议标准任务成功率 ≥80%，技术合规率 ≥95%，模型失败必须可解释且可重试，违规请求拒绝率 100%。

## 9. 分阶段实现

### 阶段 A：不接视频生成模型

用固定图片和短视频样本跑通 Brief→脚本→分镜→时间轴 JSON→评测。目标是先验证用户是否认可结果结构。

### 阶段 B：接入公司 API

实现模型适配器、异步任务、状态查询、失败重试和素材落盘；先接一种生图模型和一种生视频模型。

### 阶段 C：完成预览与局部重做

实现字幕、配音、合成、预览、镜头级重做和版本对比。

### 阶段 D：平台化

补齐 Agent Card、权限、审计、Benchmark 回归、版本发布和回滚。

## 10. 当前不做

不做通用电影级生成、多 Agent 编排、复杂时间线编辑器、自动发布到平台、计费策略和多供应商智能竞价。这些应在首版有真实使用数据后再决定。

## 11. 下一步需要技术同事确认的 8 个问题

1. 生图/生视频 API 的鉴权方式和限流。
2. 是否支持异步任务、回调或仅支持轮询。
3. 输入素材上限、支持格式和保存期限。
4. 输出是否包含可下载 URL、缩略图和元数据。
5. 是否支持固定 seed、参考图、角色/商品一致性。
6. 视频生成的最小/最大时长和画幅。
7. 是否有内容安全审核接口。
8. Hyperframe 当前可用的工作区、队列和渲染能力。
