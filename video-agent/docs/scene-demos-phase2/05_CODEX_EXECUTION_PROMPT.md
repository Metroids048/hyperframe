# 发给本地Codex的执行Prompt

你是 Metroids048/hyperframe / video-agent 的本轮实施负责人。读取当前工作区真实AGENTS.md、必要的agent.md/codex.md、现有执行状态，以及 video-agent/docs/scene-demos-phase2/00_START_HERE.md、02_PRODUCTION_PLAN.md 和当前场景卡。当前用户最新授权是开始八场景Demo制作：一个场景一个场景完成，并支持自然语言多轮修改。不要再以全局层未重构完为由停止制作，也不要把以前估算的百分比当成状态。

一、保留边界
保留现有未提交改动、素材、所有历史revision、已成功配音和缓存。不强制同步、reset/clean、删原件或强推。沿现有WebUI与Agent生产/编辑链；不另建UI、Agent服务、监督平台。继续暂停新商品图和原始视频镜头生成；HyperFrames文字图形、布局、动效、剪辑和已授权音频正常继续。已有米家V2只作参考，耳机不作新Demo。

二、先做最小真实预检
确认实际工作树/当前构建/服务端口/可用运行时及是否已有运行中的项目；不要重启他人任务。复用 config/commerce.json 的 materialRoots、lib/creative/material-roots.mjs 和现有素材入口，确认仓库根目录“素材”（相对video-agent为../素材）在已授权范围；不要仅把文件列表写出来。主目录十条文件先实测解码并观察内容、原声、字幕烧录、商品身份、镜头选择空间和可读性。生成真实source-range证据；这份制作包没有提供已观察时码，禁止根据来源页时长或名字补造。

三、素材对应和补充
按照01_MATERIAL_SCENE_MAP.json进行初选而不是强行匹配。S02显卡，S03即食饭，S01/S06掌机，S07握力球为候选；S08从通过检查的S02母版派生。S04首饰/S05香水按04_SUPPLEMENTAL_SOURCES.json的具体页面，在来源允许的下载方式下补入建议目录；不要绕过访问限制。下载完成必须记录实际路径、字节、SHA256、完整解码、同款身份及授权状态。短补镜头不伪造成长原素材；内容或数量不足继续补同商品真实视频，不循环凑时长。原广告、通用科普、不明文件不得硬套成全新商品场景。

四、执行顺序和前置
推荐S02→S03→S01→S04→S05→S06→S07→S08。一个主任务、一个生产写入者。S08真正依赖S02有效母版；其他场景按素材就绪推进。只修当前Demo实际阻塞的共同能力，其他既有义务保留在原台账，不把整套116卡全完成设成第一条片的前置。外部来源、账号/额度问题只阻塞相应项，继续可执行的其他场景，绝不伪装全部完成。

五、首片从S02接着做
优先读取 deliverables/minimax-audio-integrated-20260916/verification.json，定位真实原生包、当前项目/revision和Agent来源。远端该目录没有直接列出历史ZIP，不能假设文件在场；查找本地已保留原生项目及已有workspace恢复机制。找到记录中的本地包再校验，不将MP4重新导入当原分层恢复。恢复已有S02已验证音色/字幕/音乐，复现当前版本是否仍有19.2秒暗虚细节、33.4秒整体关系和底板先出问题；只修仍存在的缺陷。不能直接把旧MP4算本轮新成果。若旧来源无法证明普通需求经过Agent，则复用真实原素材和获准音频，由当前产品Agent重新做对应候选，明确与旧片区别。

六、每个场景必须使用不同业务链
逐个读scenes/Sxx.md及实际commerce/scenes/<scene>中的人格/输入/素材/故事/资源/音频/质量/编辑规则，补缺口后通过scene-package加载进入实际阶段。S01亮相记忆、S02整体局部解释、S03动作步骤、S04组合身份、S05活动字段、S06原问题与证据、S07保义删留、S08母版继承与分支。S07/08是recut/variant模式，不能丢失原业务目的。

七、必须真实调用而不是造片
首轮用卡里的普通中文需求通过现有WebUI，真实路由与场景选择必须经产品发生。不得为了某条测试而先破坏正确作品；遇到本来就满足的请求，应如实返回无改动，再补一个不同的有效局部编辑测试，不把no-op冒充成功修改。测试期待的route/Skill只用来断言，不直接注入标准答案绕过理解。Codex可修程序和配置，但不能手写一条终片、直传固定结构化操作、或复制现有预设代替Agent制作。NativeDocument/HyperFrames工程保留分层；不要导出MP4再导回通用timeline导致文字、字幕、音轨、特效失去语义。

资源按当前catalog精准匹配：实际resource ID/来源/版本/本地路径/执行器/节点范围/依赖必须有回执。候选名称和已调用资源分开。不能为炫技每个切点都用色散，也不能指定色散失败后悄悄换淡化。先定各场最可见静态布局，再加有限、可seek、预览和渲染一致的动效。

八、声音与事实
复用有效TTS及已确认音色；只有用户要求的对应段才重配，先测时长再入轨，不截尾、不擅自加速关键动作。字幕样式不改旁白，音乐和原声独立；若原片是混合音轨，明确限制，不假装无损分离。MiniMax音乐状态只按现场真实查询，失败不拖停已有本地音乐和字幕。不得更换计费账户、提高预算或重复提交未知状态的付费任务。

S05明确使用卡内虚构活动事实包，全片标记“虚构活动／非官方演示”，不得误做真实品牌促销；真实投放需经确认的事实和用途授权。所有商品性能、价格、容量、材质、步骤参数都必须有可靠证据，未拍动作不虚构。

九、每场景成片后当场多轮编辑
逐轮通过真实自然语言执行该卡8轮会话，不能每轮创建新项目（明确variant分支除外），不能人工把模糊指代换成对象ID。核对current/base/result revision、焦点、target/preserve和实际diff。必须包含连续相对修改、选择性恢复、undo、redo和导出；前5轮有真实编辑差异。目标歧义时先读工程与执行记录，再问最小问题。S08母版/A/B分别检查，不把回到旧版当作选择性恢复。

十、验收与持续推进
实际运行修改相关测试与现有核心回归，并用现有UI连续播放到结尾、暂停/拖动、刷新重开、真实下载；看每幕最可见帧、转场中间、密集文字和片尾，听感无法观察则准确标待审。不能只判断文件存在、exit code或模型自评分。当前错误定位→最小修复→回归→实际新片证据；同一根因两次无变化就换诊断，不能无限重试。每轮修改原子发布，失败保留旧有效版本。

场景有效通过后冻结证据，继续下一个就绪场景，不等用户重复说继续。人工审美未审单列，不冒签。平台中断保存真实状态、当前任务和下一动作；没有运行器时不承诺离线自动继续。恢复读取现有台账，不覆盖已验收项。

十一、最终交付
每场景在现有作品入口可打开，提供准确MP4、从实际成片截取封面、原生历史包及复开后继续修改的证据。建议输出目录deliverables/scene-demos-phase2/Sxx/，但以实际生成路径登记；S08要A/B两条。九条是最低结果，不能用方向预览或复制文件充数。保存原始输入、源窗、路由/Skill/资源、版本链、八轮修改、视觉/声音覆盖、授权状态。新增/改动文件在现有状态中用[NEW]/[MODIFIED]/[REUSED]列出，区分开发指令与运行时规则。

最终报告直接说明八场景分别用了什么素材、为什么这样制作、实际成片/原生入口、八轮修改结果、仍未通过项以及是否可公开商用；不要只报单测数量。现在从最小预检与S02真实工程开始执行工具操作。
