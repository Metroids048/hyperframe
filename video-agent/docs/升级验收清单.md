# 通用对话式视频剪辑升级验收

更新时间：2026-09-10。工程验收记录与人工质量评测分开保存；“达到相对 85 分”必须由专家参考片和两位独立真人评分确认。

| 计划项 | 当前结果 | 证据与边界 |
|---|---|---|
| 1. 核心流程与速度 | 已完成工程实现；精确字幕路径通过 | 真实 11 步连续流程、严格导出：`outputs/upgrade/live/run.json`；字幕 p50 1.15 秒、缓存撤销约 0.1 秒。复杂模型任务仍可能 20–80 秒。 |
| 2. ChatCut 形态 | 已完成 | `/`、`/edit` 为聊天+预览；版本、引用、撤销、原片对比、导出均可用。 |
| 3. 对话交互 | 已完成 | 一个输入框覆盖导入、连续修改和导出；桌面/手机真实浏览器验收通过。 |
| 4. skills | 已完成接入注册表 | `config/skills/registry.json` 固定来源、commit、许可证和能力；执行器只暴露受控子集。 |
| 5. 真人素材 | 已完成 | Tears of Steel、Blender 实录、普通话实录及来源哈希见 `assets/edit-samples/source-manifest.json`。 |
| 6. 通用场景 | 已完成主要动作 | 时间、重排、语义候选、字幕/翻译、配音、音乐、画幅、裁切、画中画、转场、多素材均有 schema、校验或真实/工程测试。外部生成服务需配置。 |
| 7. 原理说明 | 已完成 | [中文原理与技能说明](剪辑原理与技能说明.md)包含 HyperFrames、ChatCut、Agent 架构、执行过程和能力边界。 |
| 8. 相对 85 分 | **待人工评测** | 24 项基准已按发布副本重新冻结，12 校准+12 保留；当前没有专家片和双人评分，因此不能宣称达到 85。 |

## 已运行检查

`npm run check`、`npm run test:upgrade`、`npm run test:upgrade:ui`、`node scripts/test-upgrade-preview.mjs`、`node scripts/test-upgrade-vad.mjs` 均有通过记录。预览检查按整数帧核对媒体和字幕；未标色彩管理的旧测试源存在浏览器与 FFmpeg 色彩数值差异，报告将几何一致性与颜色差异分开记录。

真实界面截图和报告在 `outputs/upgrade/real-ui/`；模型执行曾遇到满载，Codex provider 会按配置切换备用模型并在任务中记录实际模型。语义 C04 已完成真实分析、剪辑和内容复核，但 HyperFrames 长帧导出在本机超时，保留失败证据，未将它伪报为通过；本轮已隔离复导成功（37.533 秒），完整记录见 [最终审查](最终审查-2026-09-10.md)，原超时记录保留。

评测流程见 [evaluation/README.md](evaluation/README.md)。填入专家和 Agent 的 24 个真实成片后运行 `scripts/evaluate-edit-quality.mjs prepare`，两位评审完成匿名评分后再运行 `score`。
