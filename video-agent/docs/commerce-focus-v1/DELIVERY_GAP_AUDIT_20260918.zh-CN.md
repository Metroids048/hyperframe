# 电商视频 Agent 当前交付差距审计

时间：2026-09-18  
结论：用户当前唯一优先目标——**在自有 WebUI 上传图片/视频，通过对话生成视频，并在同一工程继续多轮对话编辑、撤销/重做、导出与重开续编**——已有真实通过样例。八场景 Demo 覆盖不作为该主链路是否跑通的替代证据，也不再作为当前执行重点。

## 已经由当前工作区重新验证的能力

- 当前 3024 工作台健康，使用 `data/result-completion-projects`，检查时无其他活动制作任务。
- 素材库真实 WebUI 可读取 38 个索引条目，并对 108 个原始文件完成只读哈希复核。
- 六媒体冷启动寻帧回归通过；此前 `ISOLATION_MEDIA` 假失败已通过有界重试与诊断信息修复。
- 视频上传链路：咖啡工程 `b2a3a09a-a6fa-4743-b281-f61e3a972cb7` 已有 WebUI 自主首轮、自然语言分支修改、导出、撤销、重做、历史包重开及再次对话编辑证据。
- 图片+视频混合上传链路：工程 `9ee2c915-4f0a-4c57-a63c-447480bac18e` 含 2 张图片和 3 段视频，已生成 3 个可导出版本；当前真实 WebUI 再次核对 MP4 与历史 ZIP 的本地/HTTP 哈希一致，浏览操作无写请求。
- 当前源码 `verify-editor core` 全部通过；这证明受控编辑、字幕/声音分离、路由、恢复、资源约束等代码合同，没有替代实际八场景成片验收。

## 八场景实际状态

| 场景 | 原始素材 | 自有 Agent 候选 | 参考片 | 结论 |
| --- | ---: | ---: | ---: | --- |
| S01 新品首发 | 1 | 0 | 0 | 未补做；按用户最新优先级停止 Demo 制作 |
| S02 商品详情 | 2 | 2 | 1 | 有待审候选，未完成人工验收与完整续改集 |
| S03 使用教程 | 1 | 0 | 1 | 只有参考片，自有 Agent 正例未完成 |
| S04 搭配系列 | 3 | 0 | 1 | 只有参考片，自有 Agent 正例未完成 |
| S05 促销预告 | 3 | 0 | 0 | 自有 Agent 正例未完成 |
| S06 选购问答 | 1 | 0 | 0 | 自有 Agent 正例未完成 |
| S07 已有视频精剪 | 1 | 0 | 1 | 只有参考片，自有 Agent 正例未完成 |
| S08 一稿多版 | 7 | 2 | 0 | 母版+A 版存在；竖版等派生和人工验收未完成 |

素材目录总表见项目根目录 `素材/00_请先看这里.md`。所有场景原片已落盘，当前缺失的是自有 Agent 成片，不是原片下载失败。

## 与完整八场景交付要求的差距（非当前主链路阻塞项）

1. 八场景没有全部形成“普通需求 → 自有 Agent 首轮 → 实际 MP4 → 同工程续改 → 导出/重开”的完整闭环；目前只有 S02/S08 有归档 Agent 候选。
2. 合同要求的同工程八轮自然语言编辑、保留集、输入变化和 holdout 素材尚未完成。
3. 字幕、配音、音乐、转场、动效在代码和局部工程中可用，但尚未在八场景逐条完成最终视频级证明。
4. 现有候选均仍是 `awaiting_review`；连续观片、真实听音、动作完整性和用户人工认可不能由测试代签。
5. 116 项完整交付卡最近有效快照仅 11 项 verified，剩余绝大多数仍 pending；后续链路修复尚未回填为“全项目完成”，也不能把它解释成接近 100%。

## 当前执行重点

- S01 工程 `6d3c7cdc-19e3-47b4-86a4-7a0f98d74cf2` 的生产任务 `job-370b3460-5ad0-4698-9ba8-4da4cc1d8b23` 已取消，保留上一有效版本，不再继续场景补片。
- 当前只围绕自有 WebUI 的用户主路径工作：上传真实素材 → 对话生成 → 同工程多轮对话编辑 → 版本控制 → MP4/原生工程导出 → 重开后继续编辑。
- 已通过的视频素材链路证据：`outputs/product-remediation-20260918/webui-chain-20260918.json`。
- 已通过的图片+视频混合工程：`9ee2c915-4f0a-4c57-a63c-447480bac18e`，当前第 3 版为 `rev-39db3c6fdadec695`。
- 当前剩余工作应聚焦任意用户素材下的成片质量、失败提示和可恢复性，不再用 Demo 数量冒充产品主链路完成度。
