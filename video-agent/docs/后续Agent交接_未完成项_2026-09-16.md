# HyperFrame 后续 Agent 交接：未完成项与恢复入口

更新日期：2026-09-16。此文档记录真实未完成范围，不代表原全量合同已通过。

## 本轮已冻结的交付入口

用户最终明确“尽快收尾，细节自己改，全部项目推送 GitHub”。因此停止进一步视觉修改，以下为可播放、可继续修改的当前版本，保留质量报告中的问题，未宣称视觉验收通过。

- 工作台入口：<http://127.0.0.1:3020/?work=s02-gpu>。
- MP4：`deliverables/s02-gpu-closeout-20260916/versions/final/commerce-final.mp4`，35 秒，1920×1080，30fps，H264/AAC，完整解码通过。
- SHA256：`9cf4fa46dfe476bb422afc916e5509194bcacc27305686e14dbfb4f0edeae3f7`。
- 原生工程：`deliverables/s02-gpu-closeout-20260916/S02-native-history-final.zip`，84,270,673 字节；SHA256 `cc04e1f306ba4a73618a0d9d1deaff1fcd80115e2704006a9b8add48863de61d`。实际解包校验通过，保留三个版本，当前 `rev-a5b13075fbc28aac`。
- 待用户自行修改的细节：19.2 秒侧边连接座偏暗失焦；33.4 秒结尾三个视角尚缺准确的整体位置联动；多处切点说明卡先于文字出现。原始 R8 报告保留在该版本 `final-quality-report.json`，两项 major、一项 minor，未清零或改为通过。
- 最新实际媒体验证：`outputs/eight-scenarios-20260916/S02-closeout-rev-a5b13075fbc28aac/evidence.json`。结尾和切点前/中/后实际抽帧已看过，旧镜头残留已消除。两次导出解码音轨 SHA 相同；人工听感/质量超过米家未确认。
- 列表登记使用显式 `--defer-known-details`，依据用户停止细节修改的授权，仅提供成品入口，不等于质量批准。保留原来的所有作品条目。

## 1. 用户最新决定与本轮边界

用户最新指令：“把第二个视频显卡的这个做完就算完事，然后剩余未完成项内容单独梳理出一个文档便于之后 agent 继续任务”。因此本轮只完成 S02 显卡视频及交接；其他场景与平台欠项移交后续任务。MiniMax 暂停，用户以后提供 API Key。不要继续擅自启动七组新制作，也不要把范围收缩解释为旧欠项通过。

持续约束：唯一仓库 Metroids048/hyperframe；主工作区 video-agent；保留未提交代码、素材、缓存、工程、配置和历史。新制作不使用耳机素材，使用根目录“素材”中的现有真实视频；暂停新的 AI 商品图片/镜头。米家 V2 只作视觉参考，不能计作自有 Agent 生产，也不能替代完整教程。质量超过米家的目标尚没有真人确认。

## 2. 先读与恢复步骤

工作区：`C:\Users\admin\Desktop\hyperframe-main\video-agent`。

1. 在该目录执行 `node scripts/workspace-context.mjs --fetch-soft`。
2. 读取 `outputs/workspace-context.json`、`prompts/workbench-agent.md`、`AGENTS.md`、`codex.md`、`agent.md`、`docs/commerce-focus-v1/03-IMPLEMENTATION_SPEC.zh-CN.md`，以及下列原合同。不要只读本交接就删减验收。
3. 原始完整要求：`docs/HyperFrame_八场景与音频全量交付合同.md`，尤其末尾继承 M01—M12/F1—F3 的逐条输入与验收。业务总览当前文件：`docs/八场景业务链路、意图识别、Agent 调度与 HyperFrames 融合设计.md`。旧记录曾使用另外两个文件名，查真实文件再提供链接。
4. 查 `data/commerce-runs/*/native-project.json` 的 jobs、当前端口进程和命令行，再操作服务；不要中断活动渲染。工作台 `http://127.0.0.1:3020/`；前端是 `web-dist`，由同一 `server.mjs` 提供。
5. 本机 `python` 是不可用的 WindowsApps 别名。启动使用 `& 'C:\Users\admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' start.py all`。语音环境 `.venv-speech`。HyperFrames 固定 0.8.33。

初始目录是 ZIP 解压工作区；推送阶段已建立 `.git`，bootstrap 的 dirty:false 不证明内容与 GitHub 完全相同。上游镜像位于 `C:\Users\admin\Desktop\hyperframe-upstream-20260916`，核对过 main `4cd6045881b71c17af48de900240286bce23e376`，此前 `704e4acf…`。安全三方合并保留旧文件后复制 111 个可合并文件；证据见 `outputs/eight-scenarios-20260916/sync-baseline.json`、`three-way-check.json`、`sync-applied.json` 和 `preserved-before-sync/`。镜像 checkout 有 git-lfs 缺失，不应谎称完整 LFS 同步。后续本地修复尚不等于已推送仓库。

## 3. S02 显卡工程与本轮收口记录

- WebUI：<http://127.0.0.1:3020/?project=2c34075d-b02d-4c13-8ac9-f2da2df7ea8f>
- projectId：`2c34075d-b02d-4c13-8ac9-f2da2df7ea8f`
- jobId：`job-a8dfd435-347c-420e-91bb-7cb710f314e8`
- runId：`9181cc44-094d-4991-9549-ae62c900fe8f`
- 母工程目录：`data/commerce-runs/2c34075d-b02d-4c13-8ac9-f2da2df7ea8f/versions/job-a8dfd435-347c-420e-91bb-7cb710f314e8/`
- 真实原片：根目录 `素材/ASUS_PROART_RTX_4070_Ti_Unboxing_-_By_INVADERPC.webm`；SHA256 `44d52066b5d267c868a47a6b79f52afbfae42663764eb3a6ef9ad7bb981bc3b9`。原片 105.52 秒，1920×1080，无音轨；目标 35 秒横屏，简短中文本地旁白，无背景音乐。
- 原始普通需求与执行来源：`outputs/eight-scenarios-20260916/S02-execution.json`。通过真实 WebUI 上传并进入自有 Agent；不是预设视频复制。
- 旧审阅快照 `outputs/eight-scenarios-20260916/S02-review-candidate-1789538234161/S02-显卡详情-审阅版-未验收.mp4` 为 35 秒实际 MP4，但含旧声音错位/字幕错字等问题，永久保留且不能冒充最终版本。
- 最终文件、原生包、修订号、媒体检查与当前状态在本文件末尾“本轮最终记录”补入；没有该记录时只能按真实工程检查点继续，不能假定已完成。

已实际发现并修复过的缺口：旁白 startSeconds/durationSeconds 没有进入音频图；字幕问题被错交视觉作者；中文窄栏孤字行；R5 Prompt 更新导致打包无法保留旧指导内容；恢复只作废最近检查点，旧旁白成功调用被重新命中。所有历史保留。新增内容寻址指导存档 `lib/creative/guidance-history.mjs`；真实资源锁测试曾成功锁定 38 条。最新恢复修复在 `lib/creative/recovery.mjs` / `production.mjs`；测试 `scripts/test-stale-stage-recovery.mjs` 用真实 AgentKernel 验证旧结果不能复活、无关证据/旁白历史/累计 98 次调用保持。

## 4. 八场景剩余作品

| 场景 | 当前已有 | 仍须后续完成 |
|---|---|---|
| S01 新品首发／品牌亮相 | 米家 V2 原件、来源、原生包、完整浏览器播放证据 | 非耳机素材的自有 Agent 正例；亮相/使用/细节/收尾链；反例、输入变化、同工程改片尾、冻结。旧相机任务有缺口，不是成功片 |
| S02 商品详情／卖点图解 | 本轮仅此片收口，见最终记录 | 原合同若尚欠同工程卖点次序/标注保持测试、反例、输入变化或真人质量认可，逐项保留；单片完成不证明所有 M03-C 项通过 |
| S03 开箱／安装／使用教程 | 业务文档、共用入口、素材候选 | 全部正例生产链。建议先看 NissinCupNoodleGohan 的加水/微波/搅拌/结果连续性；也有陀螺腕力球操作视频。不得编瓦数和时长；缺一步只能记反例，不能替代完整教程 |
| S04 穿搭／组合／系列 | 本地 ElsaLeeParis 珠宝穿搭候选 | 全部制作与验收；区分单品身份、搭配关系；交换展示顺序后名称/画面一致 |
| S05 活动促销／直播预告 | 入口和场景包 | 全部制作与验收；活动时间/价格/条件来自真实输入；缺优惠可以无价格预告，不能补造；改日期须同步已读旁白或提示冲突 |
| S06 选购说明／场景问答 | 入口和场景包 | 全部制作与验收；具体问题→条件→证据→限制→回答；不得用品牌口号替代回答或编性能 |
| S07 已有视频精剪与包装 | 共用 recut 模式、输入语义恢复修复 | 新场景作品、前后对比、关键内容/原声保持、反例与续改；不是全片重写 |
| S08 一稿多版 | 共用 variant 模式及母版关联规则 | 至少两个真实派生版本：改开头及改画幅可分别验证，稳定母版/分支链和保持对象；不是复制 MP4 两份 |

每组均需：人读业务链→运行时合同/实际 Prompt→真实选材→普通 WebUI 输入→自有 Agent→真实资源绑定→整片审查修复→同工程续改→反例/输入变化→冻结。S03—S08 本轮没有启动新制作。不得用单一标题模板换六组标题。

素材初筛证据：`outputs/eight-scenarios-20260916/visual-source-review/`（sheet-0 米家参考；sheet-1 耳机排除；sheet-2 显卡；sheet-3 手机预制图形；sheet-4 金属 3D 打印；sheet-5 珠宝；sheet-6 杯面；sheet-7 Steam Deck；sheet-8 腕力球；sheet-9 小米原片）。具体源区间必须再次看原片和动作，不把缩略图当全片已看。

## 5. M01—M12 未完成验收清单

以下均非模块全通过；已有证据可复用，只有受改动影响的部分才复验。

| 模块 | 已有实现/证据 | 尚欠的实做与验收 |
|---|---|---|
| M01 通用输入/Prompt | 制作/修改/精剪/变体统一字段；明确/推导模式迁移；普通中文资源意图修复；真实 Prompt 收据 | M01-1—5 完整 WebUI 输入组、同义改写与未知价格正例；字段级目标/保持对象不变量；禁止项在最终片落地。不能以 parser 单测替代完整生产 |
| M02 素材事实/动作 | 本地素材发现、probe、密集观察、源区间/动作证据能力 | M02-1—5 真反例：缺步骤、相似异款、无依据性能、源片尾及变速保护；全范围素材充分性与动作完整性 |
| M03 八场景链路 | 八入口共用主流程、场景包和业务文档；S02 实际生产 | 上表所有未完成场景与每场景专属反例/输入变化/续改；同素材不同目的；未参与调优素材 |
| M04 资源发现匹配 | 本地目录发现与资源哈希、功能匹配/否定范围修复 | M04-1—6 同义输入、动作遮挡、第二处/其余范围、缺依赖/版本变化、超长文本/第二路输入缺失、新资源刷新；必须真实候选过滤与不静默换效果 |
| M05 十类执行 | 资源收据、参数化原生配方、部分信息条/排版/技能应用；S02 源绑定 | template/composition/component/effect/transition/skill/caption/typography/lower-third/shader 各自当前素材上的适用证据；另一组素材复用；shader 实际纹理/预览与导出一致。扫描到或测试夹具通过不算十类交付 |
| M06 本地音频/混音 | 声音落地、独立音轨、字幕、完整旁白源覆盖保护；过长替换拒绝截断 | M06-1—5 真实连续工程换音乐/降音量/淡出/原声变速映射；无声/单声道/过短/坏文件；混合音轨分离边界；实际 MP4 声音变化与真人听感单列 |
| M07 字幕/文字 | 本地 ASR/TTS 时间、内容与声音职责分离、孤字行修复、音频修复保留词时间 | M07-1—5 只改样式零 TTS、长中文多尺寸、步骤避让手、转写纠错/断句/改写原话区别、烧录字幕限制；全部真实导出检查 |
| M08 MiniMax | 客户端及协议替身链、本地语音/字幕/出片；见下节 | 当前暂停。local 尚不可整体签收；live 全部待 Key/权限。不能报告全量 Key-ready 已验收 |
| M09 连续修改 | 受控编辑/版本/撤销底座及若干测试 | 同一实际工程：片尾文字→换音乐→移动说明→换指定现有镜头→撤销→重开→导出；逐步保持差异。另测不存在对象、过期 revision、重复字幕、已读事实变更 |
| M10 恢复 | S02 多次 WebUI 恢复保留同 run/累计；依赖失效、指导存档、旧阶段缓存修复 | M10-1—5 完整证据链，尤其恢复后局部编辑/新导出；重复提交/队列/短断网；隔离损坏检查点、素材和 Prompt 变化；避免恢复反复重策划造成成本膨胀 |
| M11 原生包 | 本地音频工程独立目录修改/再导出脚本证据，路径/哈希校验 | 正式作品实际 WebUI 下载→干净目录导入→改文字和声音→重导出；损坏/缺资源/重复导入/中文空格路径；证明不读旧绝对目录；未运行平台待验 |
| M12 UI/质量/回归 | 米家参考可播、未导出工程独立列表、20 次只读预设浏览；核心回归曾通过 | 八场景实际作品可达、完整连续编辑链、F1/F3余项、实际整片视听与米家对照；所有未运行浏览器/平台回归明确待验 |

## 6. F1—F3 特定欠项（不能被上表吞掉）

- F1-01：参考米家、历史/候选区分已有部分证据；旧条目完整性还须清点。F1-02：20 次只读预设浏览已实际测过，项目/版本不增长，活动 S02 文件变化另计，见 readonly-browse 证据。F1-03：显式编辑副本双击仅创建一次仍须真实验。F1-04：同源未改副本归组、同名不同作品及历史保留仍待全量验。F1-05：缺 ZIP 可播/缺视频可见不可用/单条错误隔离有测试，真实边界继续核对。F1-06：复制/下载的实际路径、revision、hash，刷新与精选不被替换仍需最终工程证据。F1-07：迁移前后全部工程/媒体/历史 ID 和哈希保持清单待闭环。
- F2-01/02：第二处/第一三处/否定“其余”的目标集合已有目标测试，仍需实际视频位置与幂等复验。F2-03：不存在边界、动作保护、第二输入/依赖不兼容必须诚实失败。F2-04：普通中文功能要求到最终片实际生效，目标外不变。F2-05：另一批素材小范围复用。F2-06：转场前/中/后连续帧与离线输出一致；无黑场/占位纹理。F2-07：保留阶段调用与缓存统计，不报无依据提速比例。
- F3-01：同工程版本链/保持对象/零新商品生成的实证仍需完整。F3-02—04：换音乐、文字样式零 TTS、只换指定现有镜头及撤销的连续链。F3-05：刷新重启后真实新输出。F3-06：干净目录改字改音再出片及坏包边界。F3-07：八场景下拉/快捷/URL 同一语义实际验。F3-08：Windows 临时路径安全回归不得跳过。F3-09：最终整片解码/全部段落转场结尾与声音检查，真人听感不伪造。

## 7. MiniMax 暂停点（待用户 Key 后继续）

已有：真实音色查询、TTS/音乐协议、hex/URL 响应解析、WAV 落地、供应商字幕时间、音频应用、缓存与单飞、持久提交记录、超时结果未知保护、下载重试、安全下载、不外传鉴权、前端试听/应用与未配置提示。本地 Kokoro 1.1 中文/ASR 能出片；旧模型保留。协议替身只替换传输边界，明确不是实际供应商生成。

明确未完成：未知提交后用户授权“新操作”的完整恢复路径；当前相同 payload 会继续被结果未知保护挡住，不能靠盲重发解决。真实账户音色、真实 TTS/音乐权限、质量/时长变化、供应商费用/结果回查全部待验。live 缺 Key 是外部阻塞，但 local 全量仍需逐项补证据。不得把 Key 写入文档/日志/工程包。

脚本证据：`scripts/verify-audio-portable.mjs` 和 `outputs/eight-scenarios-20260916/audio-portable-*/evidence.json`；`audio-portable-with-timing.log`；`minimax-http-errors-test.log` / `minimax-ui-missing-key.json`。HTTP 缺 Key 提示修复已实际进入 WebUI。这些不证明真实 MiniMax 验收。

## 8. 验证与历史限制

最后一轮完整非浏览器核心检查曾通过：`outputs/upgrade/verification/core-2026-09-16T06-33-46.480Z/report.json`；早于最终阶段缓存修复，不能作为最新全覆盖结论。指导历史修复专项 `guidance-history-regression-fixed.log` 通过；首次失败日志保留。最新缓存专项 `scripts/test-stale-stage-recovery.mjs` 通过。最终新增检查在末尾记录。

现有 npm test/browser harness 包含独立 Puppeteer 自动化；本会话浏览器交互使用 CUA，未运行那些浏览器套件。不要写 npm test 全通过。已有 Windows 核心证据不代表 macOS/Linux 实际回归。十个历史 S/N+moka 条目原外部 Mac 文件未在本机找到，标记外部媒体缺失，保留列表与记录，不删除来缩小分母。

下一位 Agent 应先选当前授权范围中一个具体可执行缺口，沿现有服务完成；不另建八套服务、不先重写一轮审查计划、不要求用户提供组件名/逐秒分镜。

## 9. 本轮最终记录

本轮按用户“尽快收尾”的新范围，停止自动恢复再次重做十幕的过程，保留原 job 为 cancelled，累计 103 次调用、新恢复已完成 1 幕；没有清零、删除或冒充自动生产全链通过。旧旁白缓存修复已实际验证：同一 run 恢复到了 voice-7a6509e33c8e1395 的短稿，历史保留。

收口改用该自有 Agent 已保留的十幕完整 document/sourceBundles 做受控 patch，独立目录 `deliverables/s02-gpu-closeout-20260916/`。保留原视频区间、十幕源码、对象、资源绑定、35 秒和五段音频图，字幕依据已合成的中文稿纠错并重新按实际短句边界分组。自动恢复后新故事仍保留在原目录，不能误当收口片分镜；收口片以自己的 document.json 为准。

脚本 `scripts/closeout-s02-retained-project.mjs`，修复前/后两个原生版本，父子关系与精确 patch 写进包。初次模块导入错误、第二次因中间母工程缺最终 scenePackage/新增旁白的候选准入元数据而失败，日志均保留。随后携带实际场景包、原候选审核记录及核验 WAV 哈希的本地旁白来源；仍为 candidate_only，未伪造商业权利或人类批准。此工程收口方式不是原合同“全部八场景自动闭环”验收。

最新非浏览器核心回归通过：`outputs/upgrade/verification/core-2026-09-16T06-57-20.999Z/report.json`（包含本轮旧阶段缓存修复）。正式文件和实际媒体核验待下方追加。

首个收口导出 rev-9438c3466bbf9ae3 已完成 35 秒/1050 帧/H264+AAC/完整解码，没有检出黑场或重复源区间；实际导出 ASR 中接口说明为 22.02—23.88 秒，位于 21.6—24 秒接口镜头内。它的 R8 实际抽帧发现片尾只列部位名（major）及切点残影（minor），所以没有登记为最终成品。

随后 `scripts/repair-s02-final-review.mjs` 只修片尾对照与切点边界：从同一视频实际提取 62.5 秒侧边和 73.3 秒端部静帧并保留来源/哈希；静帧只用于片尾对照，不是 AI 生成或新动作。原视频节点、源区间、五段音频和六条字幕保持。compiler.mjs 用 clipPath 关闭结束的场景/媒体容器，避免后代显式可见造成残留；不是改依赖版本或跳过检查。第一次 visibility 写入 clip 被 HyperFrames 检查拒绝，日志保留；已改为符合框架规则的遮罩边界。最新非浏览器核心报告 `outputs/upgrade/verification/core-2026-09-16T07-20-09.546Z/report.json` 通过。第三版 rev-a5b13075fbc28aac 已导出并冻结；实际残余问题及用户停止细节修改的决定见本文开头。
