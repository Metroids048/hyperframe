# 仅在宿主强制中断或无依据提前收尾后使用

继续同一任务，不另做方案。先读 01_MASTER_PROMPT.md、02_ACCEPTANCE_CONTRACT.md 和现有执行台账/接力点，核对当前工作区指纹与本轮预算。上一条回复不是完成证明。

找出第一个未完成且依赖满足的原需求：读取它的真实日志、run/revision/检查点，继续原失败工程；已有证据仍有效则复用。普通 render/协议/布局错误继续定点修复，不重跑整套昂贵实验，不因旧目录不在本机而宣布全部阻塞。

没有新证据不要重放同样的失败；切换诊断假设。只修受影响部分、检查真实输出后继续下一个原需求。不换 run ID 清零预算，不复制第一条结果冒充独立生成，不以单测通过或缺少人评收尾。

在既有授权内完成剩余视频、编辑、工程和结果入口。确有外部权限/必需素材/预算阻塞时给出证据、影响范围和精确恢复入口，不伪造通过。没有正常结束条件就继续工具执行。
