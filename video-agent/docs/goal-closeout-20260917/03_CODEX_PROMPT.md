# 给 Codex 的全目标收尾主 Prompt

你负责本地 Metroids048/hyperframe / video-agent 的实际实施。用户只验收业务交付，工程诊断和实现由你处理。先阅读本目录00_README、01_AUDIT、02_CLOSEOUT_PLAN与项目当前生效AGENTS规则，再开始工具操作；不要再次提交一份大范围规划。

审查基线10fa622107087499f06258e682a45d5ebe3f7c07；工作以当前本地最新代码/未提交修改/实际服务为准，不回滚以迎合旧SHA。保留素材、原生工程、成功声音、缓存、历史、未提交改动和用户权限边界。

## A. 这轮交付什么

四项原始目标全部保留：八场景高质量可交付视频；清楚的路由/Skill/兜底与实际资源应用；字幕/旁白/音乐/动效；自然语言连续再剪辑。至少八个场景、九条完整视频，S08两派生，每个场景原生可编辑。之前“全局后三项完成”只是有限隔离环境验收，必须迁到日常主入口验证。不要把同一S02的多个revision算多个场景；米家reference-author-v2不计自有Agent场景通过。

本轮不是重新开发Agent，已有global-router、skill-resolver、conversation-edit、transition-catalog继续复用。不要按旧Prompt再重复创建第三套。用户说2/8可能在本地恢复数据中，必须先找真实sceneId与成果证据，不能凭首页二个卡片承认或凭远端没展开否认。

## B. 先解决三个真实断点

1. 运行版本断点：当前start.py::ready只看workspace/engine，不看已加载后端源码和前端构建。status曾记录3041通过而3020未重启。核对PID/cwd/dataRoot/前后端指纹和活跃任务；在不损害运行中任务情况下完成安全交付。health必须报告启动时加载指纹，不能用磁盘新hash冒充旧进程已更新。最终复验主入口，不只隔离服务。
2. 成品编辑断点：web/commerce.js::showFinishedWork把project清空，case-edit只对preset显示；finished-works公共数据不绑定原生project/revision。修成“自己的成品→打开真实原生工程→同框自然语言续改”，存在包就幂等导入一次，不要求用户找ZIP/猜端口；参考作品明确只读，不能假恢复MP4烧录层。
3. Windows发布阻断：HEAD的Actions35189572547、job105098942901在test-creative-custom.mjs:61报ISOLATION_PROTOCOL/Missing or duplicate supervisor receipt。先复现正常/超时/内存限制/取消各路径，修真正协议与子进程回收，不删测、不伪造receipt、不绕过隔离。

只根据现场复现修复；有新修复已解决则保留并给证据，不为凑改动制造缺陷。

## C. 在真实入口测出慢在哪里再改

为同一请求记录：ack、queue、route、各model调用、素材观察/ASR/TTS、compile、isolation、snapshot、review、render、package，以及cache命中和最终project/revision。已有日志扩展即可，不新建监控系统。

当前可定位代码：
- production.mjs有多阶段与逐镜头静态作者→静态评审→动画作者。
- runner.mjs::writeCompiledProject每次调用verifyCustomProject；isolation缓存键含输出目录及完整document，新revision常导致全部custom场景再次串行检查。
- service.mjs::publish持有preview槽位直到模型评审完成；render-queue已有不同池，不重新造队列，缩小临界区。
- CodexProvider.structured每次spawn并写入大上下文；不要盲目提高限额或重试。
- runner导出硬编码workers1，发布/导出有重复check；修改前先收集数据。

目标方案：低风险对象patch直接更新可用原生预览；精确字幕/音量新增语义模型=0、ASR/TTS=0；模糊请求真正走模型且最多问最小歧义；按场景与依赖哈希复用未变安全证据；素材与声音缓存按真实源hash保持；不在模型网络等待期间占用preview池；批处理可合并的规划；优先复用合适原生组件，保留各场景不同结构；新增或改变不可信源码、执行依赖或安全边界时触发必要隔离；只有输入与全部安全依赖未变、证据仍有效时才复用，布局和时序变化仍做对应画面检查；完整导出仍严查真实媒体。

前端先保存并回执消息，让用户能看阶段和取消。忙碌时状态/取消控制仍可进入路由；普通编辑有序排队，绑定执行时正确base，不允许并发覆盖。导出必须固定revision，导出完成不能把更晚的当前编辑预览倒退。

性能验收按02中的建议目标执行，先冻结机器、版本、尺寸、素材、质量、缓存及真实provider，再采样。精确编辑至少30次，生成/导出至少3次；保留冷/热与失败样本。不要用CI科幻片3.5秒冒充本机Commerce速度，不用“请求已接收”代替“预览可用”。若未达标，继续剖析，不擅自降低标准；确有外部阻塞列出原始证据。

## D. 后三目标必须这样证明

从主首页点击自己的真实S02进入，而非测试脚本直接给project URL。真实自然语言不少于10轮，覆盖字幕字号、再往上、只改一个标题、指定某个色散、音乐轻一点且旁白不动、真实已选供应商单段换声、选择性恢复转场但保持字幕、撤销重做、导出。可额外多轮，不得删义务。混合同义表达、否定、复合指令、同名对象歧义和一个故障恢复；不得全硬编码成快捷正则或预传operations。

请求目标/保持项必须在规划前绑定到当前对象与已接受历史。规划失败不能把“模型计划想改的全部对象”自动视作用户授权范围。结构修改允许必要时间映射，但源内容/事实/未指明音色/其他分支应保持。

字幕真实来源分清ASR、人工/业务文案、TTS、烧录层。样式不生成声音；换声仅目标段且真实音频时长/字幕对齐，保留其他声音与样式。真实MiniMax已配置且有授权就复用验一次；没有配置的供应商写未覆盖，不为广覆盖新开户/收费。音乐API410独立记录，已有合法音乐可继续工作，不偷换指定能力。所选效果应真正进入预览与导出，失败保留上一有效版本，不用淡化/静态装作指定效果成功。

每轮保存userRequest/baseRevision/requestedTarget/preserve/actualChange/resultRevision、必要截图与实际媒体。与持久化状态、真实播放一致；success自报不是证据。

## E. 八场景一条条做透

先恢复用户已有场景状态与素材，用本项目已授权目录，不扫描其他私人文件。不要重新发明场景标签，参考既有commerce/scenes及八场景业务正文：
S01亮相；S02详情；S03开箱/安装/使用；S04多款组合；S05活动；S06问答；S07精剪；S08母版派生两条。S07/S08仍继承原业务目的。

已有通过的成片封存，缺哪项补哪项。默认先补S02到主入口的质量和续改，再推进就绪场景。一次只保留一个主要生产任务；某项真实外部阻塞仅阻塞依赖它的项，其余继续。

每场景按：输入/观察→业务取舍→真实路由Skill资源→完整MP4→视觉与声音检查→定点修复→不少于8轮对应该场景的自然语言修改→原生包换目录重开再改再导出→挂到主作品入口。S08还验证母版/兄弟版不变、只改开头、竖屏重布局，不仅改宽高。模型接口调用和实际工程必须一一可追溯，不能Codex手写最终片冒充产品Agent结果。

素材不适配就先找现有合法替代或联网补具体许可素材；记录来源/授权/同款身份、源时间与真实可见动作。未下载不算已补，网页可看不算可商用。暂停新商品图/视频生成继续有效，TTS/图形包装/原生编辑不受此暂停影响。活动资料不足可做明确标识的内部演示，但不得计作真实活动商用全部签收；只向用户要工具无法解决的事实，其他场景继续。

## F. 必须可见的最终产品状态

主入口显示同一构建、正确数据根、当前项目/版本。所有自己的成品有“继续编辑”，参考与未验收候选单独标识；场景计数只来自真实scene验收。每片同时列出：已产出、业务自动验收、视觉/听感人审、商用条件、可编辑状态。不要混成一个completed。

正式交付前，在最终提交/构建上重跑受影响回归与完整主链：干净启动、首页打开、普通自然语言、取消/重试、刷新、撤销/重做、分支、下载、工程重开再改。Windows与Linux CI不留已知失败；用户实际平台本地复验。不能用mock测试替代真实语义/ASR/TTS/渲染，不用签名文本或SHA单独证明语义质量。

## G. 产物路径与进度

所有新增/修改核心路径写入本目录04_FILE_MAP的实际执行回执：标NEW/MODIFIED/REUSED以及路由/Skill/兜底/字幕/声音/动效/编辑所属目标。别为了表格新造对应模块。

扩展现有EXECUTION_STATUS，另把必要本轮报告存 outputs/goal-closeout-20260917/：runtime-identity、entry-roundtrip、perf-before/after、goal-acceptance、scenario-acceptance。视频仍在真实工程目录，登记准确project/run/revision/sha及用户可点入口。既有日志直接引用，不反复复制巨量项目。

每次只读当前未通过项与必要依赖。中间完成一个模块或场景后继续下一个就绪项，不能写总结束。同一错误两次没有新证据时换诊断，不无意义重试全片。平台中断前保存真实下一动作，恢复后从它继续；不要声称没有执行器时能永续后台运行。

## H. 最终只有这一种收尾

最终报告：四目标逐项证据、八场景和S08两条准确视频/原生包/续改入口、主服务构建、真实性能前后、CI、未覆盖及阻断。全部门槛实际通过才能说全部目标完成；否则明确哪些未完成并继续可执行部分。真实账户/素材权利/活动事实/真人质量签收缺失不能造假，也不能偷删验收项。

现在开始：先核对主服务真实构建与成品入口，复现S02从首页自然语言续改的断点，同时保留当前任务与作品；不要以完成文档或创建任务清单结束。
