# R1 再测试、对抗性审查与工作台收口

日期：2026-09-15。基线：46c6ff6066fba063f18b0fd057180eece65994fc。

## 本轮改动

- 移除 WebUI 素材目录选择、历史样例卡片与示例详情区域。磁盘素材、已有工程和版本保留，上传入口、生成目标、业务场景、画幅选择继续可用。
- 顶部成品下拉框统一列出有实际导出的工程及登记的参考成品。当前工程有未导出的新编辑时，仍能打开最近导出版；测试工程与重复来源不重复上架。
- 接入已有米家相机 V2：72秒，SHA256 `5d181af4b27709a3e728a38c99bf5166e8843d242b2e7ceb1d05ff1f560c9ade`。沿用现有媒体响应处理 Range、播放和下载。登记为 `reference-author-v2`，不是 Agent 自动生成成功证据。
- 参考成品只读取明确登记的本地文件；哈希改变、路径越界、重复ID及未知ID不假成功。缺少本地成片不会显示为可播放；参考目录故障不使正常工程下拉框消失。
- 切换作品清理旧审阅／下载链接；渲染完成后更新成品列表。前端入口使用 esbuild 打包，避免新增模块未发布导致整页脚本加载失败。
- Windows 素材标签使用统一的 `/` 表示，保留实际文件路径与访问限制。

## 先失败后修复的证据

1. 成品列表模块初次构建未打包，浏览器等待下拉项超时；修复构建入口后界面回归通过。
2. 并发缓存测试原先假设第一个调用必然抢到锁。在Windows运行出现 `[true,false]`，实际仍只构建一次；改为验证一个构建、一个命中，不依赖非确定的抢锁顺序。未减少并发次数或放松只构建一次的要求。
3. 跨平台素材发现测试因 `..\素材` 与 `../素材` 标签差异失败；统一展示标签后相关8项回归通过。
4. 独立发布副本在Windows检出时将规则文件转换成CRLF，触发真实POLICY_HASH失败。增加仓库LF规则，并给两条CI的Git检出及子模块设置core.autocrlf=false；保留哈希门禁，未更新哈希来容忍错误内容。修复后发布副本完整core通过。
5. 首次远端运行已通过检出、依赖和原生单测，但旧素材夹具把合成图片送入真实生产准入，因没有权利/身份审核而被正确拒绝。本地复现后保留生产拒绝断言，将纯模板编译与渲染移到只允许指定NOVA合成图片和固定测试目录的脚本。该脚本不接入服务路由，不新增生产跳过标志，不写人评批准；原生局部修改仍通过真实工具执行并保留历史。
6. 远端Windows浏览器阶段失败后，在独立发布副本复现了264字符暂存路径被FFmpeg误报不存在。Node能读取同一文件，普通路径解码失败、Windows长路径格式解码成功。媒体调用现对长绝对路径使用Win32长路径格式；HyperFrames内部抽帧通过临时短路径入口访问同一工程，结束后只移除入口，保留文件身份和原工程位置。新增超过280字符路径的真实生成、转封装、探测和完整解码回归。独立副本再次完成真实浏览器十轮修改、严格MP4与ZIP导出、下载和移动端检查（outputs/r1-review-long-path-conversation-retest-2.log）。CI同时直接报告失败套件，避免只有退出码而无法定位。

`npm run commerce:build` 与 `npm run commerce:render` 是上述NOVA合成样例的本地复现命令，同步使用夹具编译脚本，输出携带fixture-provenance.json。它们不声称完成真实商业制作；真实需求仍走WebUI或受准入约束的commerce-agent入口。

## 测试结果

|检查|结果与范围|
|---|---|
|`node scripts/verify-editor.mjs core`|最终通过，包含R1恢复/去重/精确路由、原生包、成品清单新增回归。报告：outputs/upgrade/verification/core-2026-09-15T11-17-38.530Z/report.json。|
|`node scripts/verify-editor.mjs browser`|通过；真实浏览器、FFmpeg、HyperFrames、十轮对话与导出验证。一项平台专属隔离测试跳过。报告：outputs/upgrade/verification/browser-2026-09-15T11-12-13.152Z/report.json。该次运行列表尚未包含新成品UI脚本，后者单独执行通过并已加入后续CI。|
|`npm test`|20通过、0失败；包含浏览器流程、移动端及网络异常检查。报告：outputs/acceptance/2026-09-15T11-17-32-146Z。|
|新增成品UI测试|通过：删除区域、保留下拉、最近导出选择、米家参考选择、刷新恢复、旧审阅链接清理、目录错误隔离。使用隔离API和播放器，不冒充真实模型生产。|
|额外对抗性组合|初次78项中77通过，1项Windows标签失败；修复后该组8项单独通过。保留原失败日志，不改写。|
|实际服务|3020工作台已通过start.py重新启动。真实米家MP4 Range请求206，返回1024字节；未知成品ID被拒绝；当前HTML不存在被删除区域。|
|独立发布副本|两个固定子模块从GitHub恢复成功，npm ci全新安装成功；完整core再次通过。报告：独立副本outputs/upgrade/verification/core-2026-09-15T11-23-03.162Z/report.json。|

本轮日志：outputs/r1-review-*.log。旧故障probe不计正确行为通过。CI已纳入新增回归，远端结果以实际推送后的GitHub Actions为准。

## 对项目预期的审查结论

已对照当前仓库的 workbench-agent、codex、agent、commerce-focus-v1规范，以及R1台账与米家案例复盘。持续保留单一工程、稳定素材ID、精确范围编辑、检查点恢复、候选导出和正式人评分离；未增加供应商或监督平台。

指定的“研究方案可行性”私有对话尚未成功读取：浏览器连接重试与重置均失败，任务读取无匹配，公开URL只返回登录页。已请求用户粘贴原始目标，**本报告不声称完成了与该私有对话的逐条核对**。

能力边界仍成立：RunningHub真实生成分支缺本地Key／授权，不能因隔离测试通过就宣布供应商生产验收。R1耳机工程实际完成初始候选和两次指定修改，第三次缺确切文案。米家V2是参考作者片，不能拿它填补Agent自动生产闭环。此前既有成片的真人完整观片、听感与商业权利确认没有被自动升级为通过。

## GitHub交付方式

在独立发布副本中基于远端main整合当前本地源码；不覆盖原工作目录。保留原仓库媒体/LFS记录，只提交应用源码、测试、配置定义与文档；用户原始素材、生成MP4、运行配置、Key和字体不作为本轮新增提交。

米家成品文件仍在本地 outputs/mijia-brand-test；新检出机器没有该本地文件时不会伪造显示或自动下载。此处提交的是播放入口与显式登记，非公开发布用户视频。
