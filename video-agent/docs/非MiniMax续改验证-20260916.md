# 非 MiniMax 续改验证（2026-09-16）

本轮按用户最新要求暂停 MiniMax 工作；本轮验证没有请求语音或音乐供应商。保留所有已有本地改动和 S02 冻结交付物。

## 已落实

- 复用已有统一消息入口，并补齐确定性路由：独立字幕移动、独立音乐降低并淡出，不先调用模型。复杂或含糊请求继续走语义路由。
- 增加 `update_caption_style`：可按字幕 ID 修改，或一次修改全部独立字幕；连续上移从当前值累计。字幕文字、源时间、音轨和商品说明保持。
- 不再将 `feature/body/description` 文字当作字幕修改；缺独立字幕时说明缺项，不能声称已移动烧录字幕。
- 字幕改字和样式变化使实际覆盖的镜头进入重新检查；字幕样式遵守布局锁、范围和参数校验，失败事务不改原工程。
- “音乐再轻一点，片尾自然淡出”只降低 `music` 轨并设置淡出；无独立音乐轨时拒绝误改旁白或混合原声。
- 新增服务历史验证：两次上移后撤销保留第一次调整，服务重开后重做恢复第二次调整，音轨保持。

## 实片证据

母版：`deliverables/s02-gpu-closeout-20260916/versions/final`，版本 `rev-a5b13075fbc28aac`。

验证副本：`outputs/s02-caption-edit-HAIAVF`，版本 `rev-15d4c4099e9895e5`，不是替换母版的新正式交付。

- `verification.json`：全部检查通过。
- `commerce-final.mp4`：35 秒、1920×1080、30 fps、1050 帧、有声 H.264/AAC，完整解码成功。
- 浏览器实测字幕 Y 从 916.015625 到 836.015625，连续两次各上移 40 像素，文字和高度一致。
- 新旧 MP4 解码为相同规格 PCM 后哈希一致：`5f13924af9c3ad5d8ba71fd9bff8a7ffef5c7873936c1b1d9157c8219b0ecbd5`。
- 母版 MP4 哈希保持 `9cf4fa46dfe476bb422afc916e5509194bcacc27305686e14dbfb4f0edeae3f7`。
- 新 MP4 哈希 `d8dce2db3ba3aded3e3a60dfaed02c3244ced2c64fdd24d87aef39b494771de1`。
- 副本已从另一目录重新读取，字幕样式完整保留。`edited-frame.png` 是新 MP4 第 1 秒实帧，已查看。

复现：`node scripts/verify-s02-caption-edit.mjs`。该脚本使用已有音频，不生成新旁白。每次生成独立输出目录。

## 回归

- 核心回归通过：`outputs/upgrade/verification/core-2026-09-16T10-23-47.311Z/report.json`。
- 常规验收 20/20：`outputs/acceptance/2026-09-16T10-25-49-439Z`。
- 浏览器与真实媒体回归通过：`outputs/upgrade/verification/browser-2026-09-16T10-25-34.044Z/report.json`。自定义场景测试 18 通过、1 个非 Windows 用例按平台跳过。
- 独立字幕与音乐范围测试 6/6；消息路由与历史测试 4/4。
- 字幕位置浏览器与 R3 合同测试合计 9/9。
- 新增的消息路由和字幕测试已纳入后续 `verify-editor.mjs core`。

## 未销账的范围

本轮未证明同一 S02 工程全部八轮工作台交互，也未完成换镜头后撤销、竖屏分支、完整跨目录打包重开链。已有 MP4 缺独立字幕/音乐时只做明确失败提示，不冒充恢复分层。

S02 原有质量问题、其余七个场景正例成片、场景外通用制作合同和全部资源执行器实片证据仍待完成。定向字幕测试通过不消除这些欠项；第一秒截图也不是全片人工质量认可。

本轮未重启主工作台，新增后端路由和字幕功能只在独立测试进程验证；不得将已有服务在线等同于已加载最终磁盘代码。
