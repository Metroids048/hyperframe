# Codex 下一轮执行 Prompt

用途：在用户本地现有项目执行差量修复。与 [审查及下一步方案](REVIEW_AND_NEXT_PLAN.md) 一起放入 `video-agent/docs/commerce-quality-next/`。本文件是指令，不是已完成记录。

## 一、启动指令（可直接复制到 Codex）

你要在现有 Metroids048/hyperframe 本地工作区继续实施，不另建演示项目，不重复上一轮已完成的功能。

先读取真正适用的 AGENTS.md、嵌套规则，以及：

- video-agent/docs/commerce-agent-next/01_SOLUTION.md
- video-agent/docs/commerce-agent-next/02_CODEX_EXECUTION.md
- video-agent/docs/commerce-agent-next/03_RUNTIME_PROMPTS.md
- video-agent/docs/commerce-agent-next/04_BENCHMARK_AND_ACCEPTANCE.md
- video-agent/docs/commerce-agent-next/IMPLEMENTATION_REPORT.md
- video-agent/docs/commerce-agent-next/EXECUTION_STATE.json
- video-agent/docs/commerce-quality-next/REVIEW_AND_NEXT_PLAN.md
- video-agent/docs/commerce-quality-next/CODEX_NEXT_PROMPT.md

本次远端审查快照为 6d85be33480bb4b07c354d66082e43db14c32d37，基线 4848b72 不是当前版本。仍以实际本地 HEAD、未提交代码、依赖与大媒体为准。记录差异，不重置、清理、自动 stash、无差别覆盖、强推或自动提交/推送；不要把旧报告“尚未推送”当作当前事实。

本轮目标不变：普通电商用户通过已有浏览器 WebUI，以自然语言和现有素材完成高质量视频，并在同一原生工程中精确续改。需求和输入决定制作步骤，不强制选择场景；HyperFrames 深度参与资源、制作和检查，不只充当最后渲染器。

保持当前登录/模型提供方案、HyperFrames 0.8.33 和已有原生对象/版本。禁止以重写 UI、复杂多 Agent 平台、接入生图/生视频/数字人、新收费 API 或广告投放扩散范围。任何依赖升级先给出必要性及兼容回归，不默默升级。

请按审查方案 N0—N5 的依赖推进。一个主要创作验证任务在运行即可，不要同时跑所有长片和 holdout；阶段通过后继续下一步，不用每步问我是否继续。缺外部素材/授权/模型可用性/真人评分时只阻塞相关工作，记录实情，继续不依赖该项的代码或离线验证。达到已约定调用/费用/资源上限则保存并停止相关执行，不无限消耗。

### N0 先解决基线和 CI

核对最新实际 CI。审查时电商 run 34675877194 在 test-commerce-fixtures.mjs:47 失败：期待 image-pan-zoom、实际 split-detail；对话剪辑 run 34675877198 通过。先复现并确认应有行为，再修正不一致。不得通过删断言、跳过渲染或伪造 fixture 让 CI 变绿。

盘点新增生产链的测试是否真被现有 CI 调用。离线、真实媒体、鉴权模型、人工质量分别报告。注册或单测通过不等于真实应用完成。

登记已有 C3/A2/B/C1 的实际文件、哈希与版本，保留全部失败记录。H03 若母工程有效且只是导出被取消，可恢复原导出，明确不是新生成。

### N1 优先修复真实阻塞

重点检查 production.mjs 的 storyImages、replanSourceShot、assets.inspect_ranges、assets.inspect_actions、scene.author、preview.review。

把规划和修复所用素材证据统一为可检索的源区间索引。相关旧观察和动作证据不能因为最新抽帧而丢失。未知、观察不足、确认不存在、预算耗尽、媒体失败是不同状态。

不要仅增加观察次数或超时时间。用现有 L90/L120、C4、H02 的真实失败输入定位。固定总预算，按信息缺口分配，重用已解码的证据。

修复错误路由：环境/解码失败不能让模型重设计，纯排版不能误触发换素材，文字按要求退出不应判为缺失。minor 不自动无限重做。每个修复都保留范围、依据、前后版本和无关对象锁定。

### N2 将资源变成真正可执行复用，并提前看方向

当前 7 个 recipe、关键词排序截前三、模型从头适配 source 只是现有起点，不是最终资源系统。按需求和镜头角色检索，必要时扩候选；不是扩大一次性上下文或盲装全库。

为本轮确实需要的成熟场景/组件实现受审、可参数化的原生适配器。内部区分保持实拍、参数化复用、组合适配和受控原创；不要每个简单镜头都必经三次模型重新写源码。遇到原创需求也不能降级成普通图文卡。

保留 AST/CSS、资源路径、浏览器网络/桥接隔离和运行预算。新表达能力有正反测试；稳定对象 ID、品牌字体、中文排版、视频源入点和声音必须保留。

在同一个母工程中先完成代表性关键镜头，产出 10—15 秒范围预览，绑定真实工程版本、时间范围、设计与资源依赖。它必须先于整条长片结束出现，不能单独手写一条漂亮短片冒充母工程产物。

### N3 先完成高质量 60 秒，再推进更长视频

当前 C3 是功能基线，不是已认可的质量终点。将 Q02 摩卡壶作为动作/原声回归；旗舰商品片应使用现有、权利适用的真实商品资料，缺品牌、卖点、价格就不能编造。根据资料实际范围做商品展示或营销表达，不能承诺销量/转化。

用户只需简短请求，应用自行完成设计与资源调用。至少两次独立真实应用生成（预算允许时），保留首稿、失败、修复和工作量。直接 Codex 参考与应用使用同输入和尽量相同条件；参考整片源码不得直接塞进产品结果。

完整有声检查，验证商品主次、真实细节、信息推进、中文层级、动效目的和节奏。不得循环、慢放、冻结或延长空白凑满一分钟。完成局部修改、撤销、并发导出与工程重开。

原定 85/100 与相对合格参考差距不超过 5 分仍是待人评目标，不自行签收。没有两位独立评审就保留质量 pending；模型复核可以辅助定位但不冒充人审。

### N4/N5 之后才扩 90/120 秒与未见需求

保留 L120 已有匹配的 73.782 秒旁白与词时间；剩余画面时间须有真实内容，不需强行配满。段落计时用确定性整数帧分配，减少模型反复解小数求和。

L180 先重放具体失败 bundle，按风险判断修模型输出还是扩受控合同，禁止直接放开所有 HTML/CSS。H02 保留已有两个镜头，不从头反复重跑。

90/120 秒完整生成、试听、续改和恢复通过后，再按序执行冻结 holdout。600 秒合成压力测试不计入创作质量。未发布任务的恢复包不得假装最终成片包；补齐输入、检查点、依赖和状态。

每个阶段修改代码后先本地运行、检查再交付。完成报告必须提供真实命令、环境、结果、对应输入/run/revision、可访问成片和原生工程、实际执行的 HyperFrames 资源、剩余限制。不要仅汇总测试数量宣称所有任务完成。

## 二、执行结果模板

每个工作包写清：

- 当前本地 HEAD/工作区指纹、与远端快照关系。
- 目标用户问题与实际修改位置。
- 主链路是否调用；没有调用的能力不能计入完成。
- 离线测试、媒体/浏览器、真实模型、人工质量四类各自结果。
- 实际产物、run/revision/资源锁、保留的失败。
- 下一项依赖与阻塞；不得把未运行写为通过。

状态应是具体的未开始、进行中、工程已验证、实际产品已验证、待质量评审、阻塞，而不是所有 Loop 永远一起 in_progress。

## 三、中断后继续的指令

读取最新执行账本和对应 run/checkpoint/artifact。重新核对用户未提交工作与依赖，验证缓存和输入哈希。只恢复未完成或受本次变更影响的阶段，不重做已验证且仍有效的镜头/声音。若代码、技能或组件合同已改变，依据依赖使相关缓存失效并重测，不盲目复用或全部清空。将本次恢复和原失败记录分开保存，预算沿用既有计数，不通过换 run ID 清零重试上限。继续当前主验收，不擅自启动所有长任务队列。

