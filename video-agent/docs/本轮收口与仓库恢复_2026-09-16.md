# 本轮收口与仓库恢复

用户要求停止细节修整、冻结显卡视频、整理欠项并将整个项目推送到 Metroids048/hyperframe。此记录不宣称八场景合同通过。

## 先看交付

- 视频：`video-agent/deliverables/s02-gpu-closeout-20260916/versions/final/commerce-final.mp4`，35 秒。
- 原生三版工程：`video-agent/deliverables/s02-gpu-closeout-20260916/S02-native-history-final.zip`。
- 当前版本：`rev-a5b13075fbc28aac`。视频 SHA256 `9cf4fa46dfe476bb422afc916e5509194bcacc27305686e14dbfb4f0edeae3f7`。
- 工作台：http://127.0.0.1:3020/?work=s02-gpu 。视频和 ZIP 的真实 HTTP 下载均返回 200，下载 SHA 与磁盘完全一致；播放器入口及下载按钮已实际看见。随后内嵌浏览器页崩溃，未认证整片浏览器播放完成。MP4 独立完整解码已通过。
- 全部后续任务：[未完成项交接](后续Agent交接_未完成项_2026-09-16.md)。包括两项 major、一项 minor 的 S02 视觉细节，按用户决定留待修改。

## 从仓库恢复

在完整 clone 的根目录运行：

```powershell
git lfs install
git lfs pull
python video-agent/scripts/workspace-content.py --all
python start.py all
```

Python 应使用实际可执行的 Python 3，不使用 WindowsApps 空别名。本机可用路径和 bootstrap 指令见交接文档。HyperFrames 保持 0.8.33。

源码、文档、素材目录、成品和原生包按原目录提交。被日常 Git ignore 的运行工程、历史、输出证据、有效缓存与构建文件采用已有 `workspace-content` 内容寻址快照；32 MiB 分块、文件哈希和恢复路径在 manifest.json。此次快照包含 33,901 个文件，逻辑大小 23,163,788,053 字节，去重后 2,113,958,690 字节。旧 manifest 放在 history，旧对象不清理。

恢复脚本只补缺失文件，不覆盖已有本地文件；已有工程整组保持。因此继续本机工作不用重复恢复覆盖。素材缓存中临时符号链接不复制为第二份媒体；实际媒体及原工程在快照中。包是证据与数据，不是另一套执行源码。

不推送真实 API Key、.env 和 *.local.env/json；它们保留本机，由新环境自行配置。node_modules、Python 虚拟环境、Git 元数据和可重装依赖不作为业务成果提交；依赖由锁文件恢复。旧基线嵌套 Git 目录保留本机，仓库历史已有对应内容。本轮未删除工程、缓存、素材、配置或历史来缩小验收范围。

## 验证边界

最新非浏览器核心报告 `outputs/upgrade/verification/core-2026-09-16T07-20-09.546Z/report.json` 通过；成品完整解码与三个版本实际解包通过；冻结后的作品列表专项测试通过。未声称 npm test 浏览器套件、macOS/Linux、人工听感、商业权利、MiniMax 真实供应商或全量合同验收完成。
