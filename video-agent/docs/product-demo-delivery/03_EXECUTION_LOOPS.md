# P00—P14 可直接执行的任务 Prompts

每个阶段执行前先读 `01_MASTER_PROMPT.md`、当前运行状态与对应需求。已验证部分复用；已实现但未验证的先验后改；先本地运行再报告。

本轮编号是执行依赖，不表示重做旧 Loop。先将本地历史工作映射进来。各阶段应持续交付真实入口和作品证据，不能等 P10 才第一次跑产品。

共有停止规则：只有本阶段依赖缺失或真实外部阻塞才停止该分支；继续其他可执行任务。安全/授权不能绕过。开发测试输入允许提前使用，P12 才是最终展示包准备。


## P00 — 现场审计、需求继承与可验证跳过

依赖：无。

你现在执行 P00。读取全部现有要求和实现，定位当前 WebUI 启动器/真实入口/模型模式/数据目录。记录 Git 与运行基线，保护未提交工作。将 `10_REQUIREMENTS.json` 合并成运行矩阵，不把本包种子当事实；发现未列出的已约定需求新增 LEGACY-* 条目，不默认丢弃。

为每条需求填已有文件、handler、UI 路径、旧 Loop、当前证据、缺口。检查旧状态的源码/依赖/素材指纹，复跑相关最小验证；不能仅因文件存在就跳过。确定 REUSE_VERIFIED、VERIFY_EXISTING、COMPLETE_GAP、IMPLEMENT_OR_FIX、BLOCKED 行动。

先核对当前安装版能力，不强制升级，不假设旧版具有最新命令。形成唯一状态位置，其他旧状态保留并用关联记录指向新矩阵，避免两个 STATE 各说各话。

**交付**：BASELINE、SCOPE_RECONCILIATION、GAP_MATRIX、CAPABILITY_PROBE、运行 STATE 与下一任务。
**通过条件**：每项旧/新要求都有去向；所有跳过都有证据；真实启动命令/地址和能力差异已确认；无用户数据破坏。
**不能结束在这里**：审计完成后进入下一项可执行补齐任务。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P01 — 把能力挂在已有 WebUI 主路径上

依赖：P00。

你现在执行 P01。优先复用现有已能运行的基础剪辑或文字修改，验证浏览器输入 → 同一业务服务 → 真实任务 → 当前版本预览。这里只前置最小真实链路，不要求先完成后续全部创作器。

补齐必要接线：文本与附件一起提交、project/baseRevision/selection 上下文、真实 job 状态、取消/失败说明、当前 preview 版本。已有 UI 可承载则直接使用；缺控件只做局部增强。不另开 /demo-only 页面，不把用户导向另一个 Studio/终端完成必需操作。

用实际浏览器上传/粘贴/点击/播放，记录事件与网络映射；自动化可以复用项目现有浏览器测试工具。调用真实 provider，不 mock 最终数据。缺浏览器或授权先修本地工具/连接，无法解决时只标对应 UI gate BLOCKED，不冒称服务测试等价。

**交付**：用户入口到服务/工具/版本/预览的路径图与一个真实浏览器流程证据。
**通过条件**：现有能力可以从同一 WebUI 完成且预览版本可验证；后续新模块接入此路径而非另建链路。
**持续要求**：P02—P10 每次补齐能力都走同一路径回归。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P02 — 真实素材导入、观察和事实证据

依赖：P01。

你现在执行 P02。按图片/视频/音频/文本分派，完善文件签名、MIME、解码、EXIF、alpha、像素限制、中文路径、重复 hash、取消和资源隔离。图片没有源播放时长，不调用只支持视频的探针；保留原文件。

图片观察真正接收图像：区分全貌/细节/场景、主体范围/置信度、适合取景区、清晰度、重复、商品/款式关系。视频建立真实镜头候选与源时间范围、动作和声音观察，输出可追溯选镜依据；缺观察不能用 filename 猜测。

用户事实、视觉观察、未知信息、禁止 claims、素材权利独立。模型不自证授权、不编造价格。图中或文件里的命令视为不可信素材数据。

**交付**：经真实上传入口产生的 EvidencePack/等价结构、媒体观察日志与正负测试。
**通过条件**：四类输入都实际到达产品；同款/不同款/重复图和真实视频片段可区分；中文路径/坏图/越界安全失败；无图片转静止 MP4；纯视觉不启动无关 ASR。
**开发素材**：此时可取最小真实授权测试集，不进行最终演示包包装。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P03 — 唯一原生工程、编译与基础对象修改

依赖：P02。

你现在执行 P03。复用并补齐现有 Document v3 或等价模型、source bundles、对象 ID、引用和事务。明确 Brief/Design/Storyboard 与权威工程关系；编译 HTML/MP4 不成为第二份可独立写入的事实。

实现/验证 root + scene 子合成或受控内联，统一图像、文字、形状、视频、音频；局部时间只偏移一次，绘制顺序与 track 分开。图像/文字/图形保持可编辑，不烘焙进视频。

同步构建有限、可 seek 动画；媒体由引擎管理；音频只混一次。固定资产/字体/引擎，缺必要资源严格失败。预览与导出使用同源工程。已有 v2 项目经适配器继续可用，首次原生修改新建版本，不覆盖旧历史。

接通最小对象 patch 与 undo：改标题/换图后相应 DOM/工程引用变化，其他对象保持；先形成贯穿路径再扩展全量操作。

**交付**：原生工程、编译产物、objectMap、两个格式以上可播放输出与基础续改轨迹。
**通过条件**：三类以上场景可同工程组合；乱序/顺序 seek 一致；渲染帧/音频/总时长正确；预览一致；原工程可恢复；WebUI 能看到对应新版本。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P04 — 导演规划、设计和约束求解

依赖：P03。

你现在执行 P04。将用户目标/受众/时长/画幅/风格/声音/锁定与素材证据转换为可执行 Brief/Design/ScenePlan。场景数不按 Demo 固定；不要求用户逐幕输入。

先确定信息与真实视觉证据，再分配场景、选镜范围和 motion route。构图先于动画；用真实中文字体测量文本，处理主体避让和最短阅读时间，不删事实或缩成小字过关。

帧预算包含重叠转场；15/30/45 秒目标精确；30→15 秒重新编排而非简单倍速。锁定时长/内容与新目标冲突时解释具体冲突，非关键设计空白采用记录过的保守假设，不每步追问。

同一素材能给出质感、功能说明、促销强调等有实质构图/节奏区别的方案；不止换颜色。不强制混合素材视频主导。纯文本允许原生 motion/diagram，不虚构商品照片。

**通过条件**：四类输入从真实请求得到有信息意义的方案；已知商品事实一致，未给价格不出现；长中文可读；阅读/锁定/时间冲突有负例；规划真正进入原生工程。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P05 — 可执行动效与非模板场景能力

依赖：P03, P04。

你现在执行 P05。核对现有 motion/registry 的 handler 和真实执行，已有验证过的能力复用，guidance 与 executable 分开。覆盖媒体揭示/聚焦/细节窗、文字层级/强调、图文/分屏/卡片布局、标注、价格/片尾和适用的场景连接；组件数量不是质量目标。

为每个能力补参数 schema、素材前提、支持画幅/时长、可修改字段、运行时锁、关键过程帧与小样渲染。运动选择由 purpose/mood/evidence 决定，不随机。尊重当前生效的 HyperFrames 创作技能和运行合同，不能用全程 pan/zoom 顶替多层设计。

完成一个确实超出现有已验证组合的需求：模型仅提出当前场景源码/参数/对象映射/资源声明，在隔离进程运行。静态解析加运行时边界；限制网络、其他项目、凭据、CPU/内存/时间、worker/navigation；禁止服务 eval 和 --no-sandbox。失败保留旧版，自动修复最多两轮。

**通过条件**：目标对象在全过程真正按要求运动；静态主体也能被明确判断为失败；非模板场景可真实渲染并在下一轮改参数；恶意/无限执行安全失败；Windows 隔离有证据。只有组件路径过关不可签收非模板目标。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P06 — 四类输入的真实产品自动成片

依赖：P01, P02, P03, P04, P05。

你现在执行 P06。使用开发测试输入，从空项目经已有 WebUI 让产品 Agent 独立执行四条流程：文字→动效，纯图片→完整商品片，真实视频→剪辑包装，混合→完整成片。无人工修改最终 HTML/EDL。

保留短能力片，但额外覆盖完整 30 秒图片商品片与 15/45 秒时长；全貌、真实细节、卖点组织（有依据时）、场景/收尾都成立，不要求任意输入必须套固定四幕。

视频测试集准备 5—10 个真实 B-roll，至少 5 个真实不同有效镜头入片，明确选镜/舍弃、trim/reorder/时长变化以及 speed 或 reframe；镜头事实和 source ranges 可追溯。不能把同镜头重复切割或静态图变视频冒充。

混合素材保留三类场景与可编辑对象，不先分管线渲染两个 MP4 再合并。资料不适合某效果就选合适表达，不偷偷伪造。

**通过条件**：四类真实用户请求都产生完整可播放作品和原生工程；实际调用轨迹属于产品 Agent；同素材变化目标会改变设计而不改事实；各路线独立 gate；已有 UI 流程录屏/trace 与渲染相符。
**注意**：这是开发期能力验证，最终可演示素材包仍待 P11 后整理。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P07 — 自然语言精确续改、锁定与版本分支

依赖：P03, P05, P06。

你现在执行 P07。按 `04_WEBUI_ACCEPTANCE.md` 补齐寻址、typed patch、复合请求拆解、依赖失效、锁定和可逆变更。用当前 project/baseRevision/选中对象/对话指代，不让用户输入内部 ID。

覆盖改文案/价格/样式/图片、细节聚焦、trim/split/reorder/rate、场景时长、三画幅、转场、声音参数；“只改价格”“其他别动”必须有非目标语义/资源 hash 和受影响画面检查。

锁定分清内容/布局/段内时长/绝对位置，不笼统不可移动导致所有操作冲突；后续授权只解除明确指定的锁。30→15 秒应重规划保留信息；不能以 2× 播放冒充。选择性恢复只回退指定历史属性，保留后来价格；冲突有解释。

三个不同开头基于同一 baseline 建分支，共用后文逻辑，保留主版本；版本导出与后续编辑并行安全。若旧代码仅支持整版 undo，不得把它冒充选择性恢复。

**通过条件**：同一工程 ≥12 轮经 WebUI 连续修改、撤销恢复，含复合请求、歧义/冲突负例；锁定和非目标对象保持；无重新生成整片；普通视觉修改无无关 ASR/TTS。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P08 — 声音、字幕、音乐编排与按需配音

依赖：P03, P06。

你现在执行 P08。复用已有音频和字幕模块；明确 none/原声/用户 BGM/音效/明确配音。视频 muted + 独立音频或当前 pin 的等价受控方案，验证不重复混音。普通字幕、样式或价格改动不能改变声音。

测试原声保留、音量、ducks、分段静音、淡出、按需 SFX；两条独立任务区分“给既有画面加 BGM，保持 timing”与“按上传音乐重新卡点”。后者真实分析起音/乐句/能量/终止，不机械每拍切镜头；用户未要求解说就不加。

讲话/旁白字幕用真实时间源；纠错不改音、重排/变速后正确映射。原需求包含的文本/文章→稿件→试听→明确选声→最终声音→成片能力继续继承，若存在未完成项显式补齐，不以本轮主要电商而删掉。只要求试听时不提前制作全片。

使用已有合法本地或已配置提供方，不更换登录、不接新收费 API；缺中文能力、依赖或授权精确标 BLOCKED，不能用英文顶替或把人声当配乐。

**通过条件**：实际听音与媒体分析相符，峰值/响度/截断/同步有记录；两种音乐操作分别通过；无声项目没有意外音轨；按需配音和声音试听遵守用户请求。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P09 — 可靠性、导出重开、安全和旧功能回归

依赖：P06, P07, P08。

你现在执行 P09。验证同项目原子提交/幂等/过期 base 冲突；停止、取消、模型失败、浏览器失败、服务重启、Windows 文件占用、缓存损坏都保留上一有效工程。导出持有旧 revision，完成后不覆盖当前预览。

在新会话/新数据目录重开原生包，保留素材、字体依赖声明/哈希、源码、对象图与历史；不能偷用旧缓存/绝对路径/私有凭据。只有外部 MP4 只能承诺片段级编辑，不伪称恢复原生图层。

受控范围正负边界：30/31 素材、文件大小/像素、600 秒、竖横方输出、30fps、源时基/变帧率归一化、半开区间与转场重叠。大边界用程序构造或许可测试媒体可以做资源压力测试，但不可冒充真实广告创作证明。

回归既有剪辑、字幕、原声、精确帧范围和旧工程。安全测试涵盖上传不可信输入、资源越界/外网/localhost、代码无限执行与取消进程树；不杀用户全部浏览器。

**通过条件**：失败不损坏、可撤销/重开、导出版本正确、受控范围不缩水、所有旧失败/本次新增失败都有归属，没有清理用户数据。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P10 — 画面质量、完整播放与 Windows 性能

依赖：P09。

你现在执行 P10。质量从此前各阶段已持续检查，此轮整合回归。检查每幕入场/hero-frame/运动极值/尾帧、转场前中后、复现随机采样、乱序 seek 与导出对应帧；文字读得清，商品无遮挡或错误裁切，声画同步，目标对象真的动。

自动视觉判断与完整人工观片分开。冻结开发/保留集、质量标准和硬失败；继承 24 任务中 12 开发/12 保留，不把四条熟悉样片当泛化证明。基础 cut 只作下限，专家相对分与两位评审另有真证据，不让模型自签。

在用户实际 Windows 机器按既有目标测冷/暖路径，记录至少 30 暖/5 冷样本、p50/p95、内存、失败率、缓存与模型调用数。视觉/文字 patch 不做无关 ASR/TTS；同硬件同素材同分辨率比较。不用降低质量、静音、跳检查或预制结果“加速”。最终编码另报，不混入局部修改指标。

**通过条件**：全部已约定工程质量和性能门槛有实测结论；未达到则修复或如实保留 FAIL/BLOCKED；人审缺失标 pending，不假报。依赖变更只重跑影响范围，但最终整片回归不可跳。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P11 — 产品工程关门，不用 Demo 替代缺口

依赖：P01, P02, P03, P04, P05, P06, P07, P08, P09, P10。

你现在执行 P11。逐项核对需求矩阵和当前证据，不按“所有 Loop 文件写完”汇总。所有 P00—P10 的 engineering-stage 能力与现有 WebUI 路径均须通过，包含此前继承的 LEGACY-* 必需项。R58 是消费这些结果的治理项，不将关门结果作为自身前置，避免自引用。

核对输入→真实模型→执行→预览/导出→续改/重开链路，非模板与音频路线不能被 components-only/无声测试替代。核对跳过项证据仍适用于现在的依赖。不能把 API-only 标成 UI passed。

输出 `PRODUCT_CAPABILITY_GATE.json`，逐项列工程通过/缺失/失败/阻塞；任何核心工程项未过，继续修复，不开始最终 Demo 包包装，不声明产品能力全齐。

人工完整观片、独立专家分、素材商业发布审批保留在独立 signoff 中。真实工程功能通过后可以进行 P12，最终包标“待用户/专家签收”，但不得因此写“所有历史验收通过”。

**通过条件**：所有应在此阶段完成的功能与工程 gate VERIFIED；没有把待实现改成可选，没有把真实故障改名人审待定；对外部签收缺口透明。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P12 — 准备最终用户输入素材与演示文本

依赖：P11。

你现在执行 P12。现在才按 `05_DEMO_KIT_SPEC.md` 和 `06_DEMO_PROMPTS_DRAFT.md` 组装最终演示输入。优先复用已有合法高质量原素材；不足时自行查明可演示/可再分发来源并获取真实文件，不要求用户自己四处搜图。

至少三组互不相同商品/素材集，主商品图和视频对应同款；图片全貌/细节/场景、视频真实不同镜头、替换素材、音乐/口播按演示任务准备。没有确定许可就换可合法素材；仍无法取得则明确素材阻塞，不伪造。

每个素材写来源、许可依据、hash、原图/裁切/抽帧关系、产品对应与使用范围。演示需要的价格/标题作为显式模拟事实，不伪称市场报价。严禁生成图/生成视频填充或把最终成片拿来当输入。

把草案中的所有描述绑定到真实文件和可见内容，生成无占位符 `INPUTS.md`、`PASTE_FIRST.txt`、`FOLLOWUPS.txt`；用户只需上传与粘贴，不输入 SceneSpec/对象 ID/代码。渲染目标和期望检查只放运行人员 evidence，不进入产品答案。

**通过条件**：输入目录确实有可解码真实图片/视频/音频，文本可直接复制、商品一致、授权清楚、所有案例覆盖需求矩阵；不是空目录、素材链接列表或等待用户补全的文案。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P13 — WebUI 预演、复跑与防特例验证

依赖：P12。

你现在执行 P13。启动现有 WebUI，用 P12 的原始输入/用户文本从空项目真实生成。每个核心创建案例至少 3 次独立新项目运行，其中至少一次新会话/冷项目；缓存情况披露，不能每次加载旧工程。记录全部成功/失败/修复/人工介入，不择优隐藏。

演示用户将走什么步骤，浏览器就走什么步骤。测试工具不得预填分镜、写内部参数、手修 HTML 或从后台“救片”；失败只能修产品通用能力，再使受影响工程 gate 重新通过，随后从原输入复跑。

完成同工程至少 12 轮修改脚本，导出中继续改、关闭重开、选择性恢复、不同开头分支；非目标内容检查与视觉检查都要有证据。

另测自然语言改写、输入改名/顺序置换（同步修正显式文件引用）、新商品/保留输入；意图与事实不变但无需像素级一样。不能靠匹配字符串/文件名/hash/示例 ID 命中固定答案。改句子后乱编价格或丢约束视为失败。

**通过条件**：所有演示用例有真实用户可访问输出、可重开原生工程、浏览器轨迹；核心三次复跑均满足关键约束，过程差异和自动修复率公开；人工评审另记，不把自动播放当人工观看。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。


## P14 — 现有 WebUI 手动演示交付与最终说明

依赖：P13。

你现在执行 P14。交付真实运行的 `demos/webui-ready/`（或仓库等价受管位置），按目录约定含原始输入、每组可粘贴文本、续改步骤、Agent 成品、原生工程、可重开包和证据。创建 `START_DEMO.md` 与 `DEMO_REPORT.md`，记录实际本机启动器、实际 URL/端口、每组需要点哪里/上传哪些文件/粘贴哪段/应看到什么/如何继续修改。

优先使用现有启动器；缺少用户可用的启动方式时，只补一个经本机实际运行验证的便捷启动入口，不重造前端。地址必须来自实际启动输出，不能猜固定端口；后续端口变化的查找方式也写清。

成片的开发参考、产品自动运行、机器视觉评估、真实人工评价分别标记。所有成果带 run/project/revision/hash，能追溯到输入和请求。用户手动试跑只能依赖 input/ 与可见文案，不依赖开发缓存或秘密预置。

最终先报告用户入口和可见结果，再列 REUSED/IMPLEMENTED/VERIFIED/BLOCKED、相较基线功能变化、真实命令、性能和剩余人审。不要只列新增文件和测试数；不自动推送私有素材/凭据/大媒体或提交字体。

**通过条件**：本机按说明确实能打开已有 WebUI；用户可直接上传/粘贴/看成品/续改/导出；包中没有未填占位符、空假视频或未披露失败；完整目标签收只取真实状态。

**阶段结束动作**：将真实结果写入运行 STATE/EVIDENCE 和需求矩阵，给出当前仍有效的通过证据与缺口，然后进入下一项可执行任务。禁止把“实现了文件”直接写成 VERIFIED。
