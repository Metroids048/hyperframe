# WebUI 成品入口与主页示例修复 · 2026-09-14

本次范围只包含用户指定的已完成作品切换和主页六条示例；此前其他制作任务不在此处宣称完成。

- 顶部作品下拉框恢复摩卡壶、键盘上新版、键盘细节版、化妆品精华护理。已通过真实浏览器切换、原生对象加载和 MP4 HTTP 读取。
- 历史 15 秒 keyboard-promo 的原生目录不在本机，保留归档记录；本机完整存在的两份键盘成品均可用。
- 六条主页示例沿用原工程增加新版本：重排文字与实物图片、取消不准确标注、教程使用完整实拍画面；N2/N6 加入现有原创配乐，其他配乐提高至 0.8，教程原声提高至 2。
- 修复全长音乐错误改变场景转场计时的问题：音频编辑不再延长成片。保持 35/40/45/30/15/20 秒。
- 耳机正面照片增加有来源的下半部精确裁切；原图、原始工程和历史版本保留。

## 验证

实际修订、导出文件与媒体检查：`outputs/demo-repair-20260914/state.json`。
每条成片逐幕抽帧、原生播放器 Web Audio 信号、MP4 响度、1 倍速完整播放：`outputs/demo-repair-20260914/delivery.json`。
作品切换和主页链接：同目录 `picker.json` 与 `gallery.json`。

核心与浏览器验证通过；package.json 的 test 两个命令（build-web + acceptance）直接执行通过 20/20；此环境未提供 npm 可执行文件。新增/相关原生回归 19/19 通过。

检查证明实际信号、媒体完整性、页面入口与当前画面可读性，不替代用户审美签收或人工试听。保留旧版本，未提交或推送远端，HyperFrames 仍为 0.8.33。
