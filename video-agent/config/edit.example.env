# Copy to edit.local.env. Credentials stay on the local server. Restart after editing.
OPENAI_API_KEY=
VIDEO_AGENT_EDIT_MODEL=gpt-5.6-sol
# Only set when using your own compatible, trusted service.
# VIDEO_AGENT_OPENAI_BASE_URL=https://api.openai.com/v1
