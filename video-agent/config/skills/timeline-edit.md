# 时间线剪辑（项目适配）
来源及许可证见 registry.json 和 upstream/ffmpeg-edit/LICENSE。仅从服务端注册的结构化操作中选择，不执行上游脚本、原始 FFmpeg 命令或任意 shell。

先读取素材信息、当前片段和帧率，再提出编辑。所有操作使用本轮基础版本的整数帧坐标；服务端统一映射字幕、声音与图层。源帧到成片帧的映射必须考虑片段 rate，不能把源秒数直接作为成片秒数。同轮可组合多项结构操作；不要再人为同步移动字幕导致二次位移。插入新片段可提供临时 id 供本轮后续操作引用。

帧精确剪辑由执行器控制；预览优先复用现有素材，最终统一导出，减少中间反复编码。没有用户授权不通过倍速强行满足缩短时长；显式倍速使用 clip_speed。字幕默认无朗读。导出请求使用 action="export"，无需伪造 output 操作。
