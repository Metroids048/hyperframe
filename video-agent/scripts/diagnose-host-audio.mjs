import puppeteer from 'puppeteer-core';
const b=await puppeteer.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});try{const p=await b.newPage();await p.setContent('<button onclick="window.ac=new AudioContext();ac.resume();const o=ac.createOscillator();o.connect(ac.destination);o.start()">play</button>');await p.click('button');const start=await p.evaluate(()=>({state:ac.state,time:ac.currentTime}));await new Promise(r=>setTimeout(r,2000));console.log(JSON.stringify({kind:'standalone-host-audio-clock',start,end:await p.evaluate(()=>({state:ac.state,time:ac.currentTime}))}));}finally{await b.close()}
