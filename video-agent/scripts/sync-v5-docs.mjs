import fs from 'node:fs';
let s=fs.readFileSync('../video-agent-definition.md','utf8').replaceAll('0.4','0.5');
s=s.replace('需要掌控内容的用户可以走 **先编辑分镜 → 保存并生成视频**。','分镜操作只出现在结果区：**编辑分镜 → 修改文案 → 保存并生成新版**。首页示例或案例成片点击编辑时自动创建独立草稿，随后显示可编辑输入框。');
s=s.replace('一个大文本框承接商品、品牌、卖点、受众、语气和行动目标；上传区接收素材。','一个大文本框承接商品、品牌、卖点、受众、语气和行动目标；上传区紧邻文本框，两者组成同一个主要输入区。');
s=s.replace('整理结果、手动补充、文案覆盖均折叠显示，仅缺少必要信息时展开补充。','品牌与文案覆盖统一收进可选的「补充信息」，无必填星号；商品未识别时提示用户在原描述中补充，不强迫完成另一张表单。');
s=s.replace('不把模型路由、工程阶段代号或实现模式作为主要用户选择。','页面不提供高级选项或模型路由选择；当前演示方式由应用统一配置。');
s=s.replace('正式版应支持按实际卖点数量调整版式；本轮 Demo 的三卖点模板仍需三个有效短句，缺项会要求补充。这是模板限制，不应写进 Agent 的长期定义。','本轮 Demo 已支持 0–3 个卖点，仅展示提供的内容，不补造卖点；未提供品牌时使用中性的「商品展示」标识。商品本身需从描述或可选补充中明确。');
s=s.replace('选择未启用的实时模式立即返回提示','界面不再提供实时模式切换，未启用的实时接口立即返回提示');
s=s.replace('默认折叠；缺项时展开','默认折叠，无必填标记；商品未识别时在主输入旁提示');
s=s.replace('三幕标题/补充文案可修改并用于成片；保存为新版本','结果区编辑，案例自动创建副本；三幕标题/补充文案可修改并用于成片；保存为新版本');
fs.writeFileSync('../video-agent-definition.md',s);
s=fs.readFileSync('README.md','utf8').replace('当前 v0.4','当前 v0.5').replace('也可先编辑分镜','生成后在结果区编辑分镜');fs.writeFileSync('README.md',s);
