# HyperFrame 全量收口：唯一启动入口

本包包含 **116 张功能/场景门槛任务卡**，保留M01—M12，另有P00执行准备。四目标、八场景、十类资源、原M/F全部验收义务不变。不是116条需要用户分别发送的消息。

## 放置
将整个 `hyperframe_full_closeout` 文件夹放在项目根目录，形成 `hyperframe/hyperframe_full_closeout/`；不要覆盖现有生产目录、项目AGENTS.md或用户配置。

## 初次执行顺序
读取本页→01_EXECUTION_RULES.md→03_REQUIREMENTS_MAP.md→实际项目规则/现有状态→当前任务卡。02_TASKS.json是任务定义；06_TASK_STATE.seed.json只用于首次初始化，恢复不可覆盖现有状态。模块任务详见modules，八场景另在scenes。所有历史原文位于sources，旧停工/等待Key文字不作为本轮停止条件。

## 范围事实
基于上一轮已提供审查快照和交付材料拆解；本包没有再读取最新远端。实施时P00-01对当前本地/远端差异复核后继续，不以文档里的旧SHA覆盖新修复。MiniMax已经有真实局部换声成果，优先复用；视频生成仍暂停；米家是参考；S02已有候选不是八场景全部完成。

## 默认依赖顺序
P00当前基线/安全/回归→M12的作品浏览早修→M01输入→M02证据与八场景选材→M04精准资源→M05原生装配→M08/M06/M07声音字幕与M09编辑相互联调→M10/M11恢复交付→逐场景S02、S03、S01、S04、S05、S06、S07、S08→M12全回归和最终结果。

任务卡的depends_on优先；不要求模块号机械升序。场景业务稿与素材预检提前完成，真实制作按依赖就绪推进；所有共用模块需先在已有真实S02工程中接入验证，不能全部拖到最后才联调。

## 发给Codex的主Prompt

你是 Metroids048/hyperframe / video-agent 的本轮实施负责人。读取当前工作区实际生效规则和 hyperframe_full_closeout/00_START_HERE.md、01_EXECUTION_RULES.md、03_REQUIREMENTS_MAP.md。用02_TASKS.json与项目现有执行状态作为逐项推进依据，不重新大范围规划。

本轮范围是四目标全部实现，原M01—M12、F1—F3以及八场景/十类资源义务全部保留。按每张任务卡的依赖、实际代码位置、实现步骤、正例、反例、集成和产物检查逐个关闭；模块/功能通过后立即继续下一就绪项，不等待我说继续，不把一个视频、一个API或单测完成当整体完成。

先执行P00-01的真实本地核对，再处理依赖就绪的最早实际缺口。已有实现和有效证据复用，不为凑改动重写；仅改过或依赖失效部分复验。一个写入者、一次一个主任务。旧文件“只做S02/只规划/等待MiniMax”只作历史，实际只规划产品功能仍必须不生成。

保留所有未提交改动、素材、原生工程、缓存、已成功MiniMax音频及历史。暂停新AI商品图/视频镜头，不用耳机完成新场景，不用米家参考/旧预设/手工直连作品充当Agent成果。修安全配置跟踪但不输出密钥，不擅自强推历史或提高付费预算。

P00-04只扩现有状态/验收器，不新造监督平台。每卡要有当前源码与运行构建、真实命令/报告、正反例、project/run/revision、目标/保持diff及实际产物证据；文档或测试替身只能完成相应子项。内部问题定位修复后继续；同一错误两次无变化需换诊断方法，不随机重做全片。真实外部限制只阻塞对应任务，不伪装已完成。

每次只读取当前卡及必要源码/依赖证据，不把整包重复塞进模型。按05_RESUME.md保存可恢复状态；平台中断不等于完成，不承诺没有执行器时自动继续。

当前可执行必验项未全部完成，不主动写最终收尾。最后交付四目标、八场景、旧验收账逐项结果，准确视频/原生包/版本入口、资源/声音来源、全回归及未覆盖人评。现在直接执行工具操作。
