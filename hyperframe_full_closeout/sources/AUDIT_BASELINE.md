# HyperFrame 最新仓库全局审查与四目标收口任务书

日期：2026-09-16。唯一项目：`Metroids048/hyperframe`，主工作区 `video-agent`。

## 0. 版本、事实来源与本次边界

本次没有沿用 4cd6045881b71c17af48de900240286bce23e376 旧版作为审查基线。

- 开始读取时 main：`6ca4f51e2f14b8100a43c65842df3ffa4a46665f`，2026-09-16 12:27:06 UTC，媒体分块 batch 1。
- 本次代码读取固定于 `ad5815707ac3b849d3d97344ea7166b623a5b3f0`，随后用 `afad6e37f045b157f7b6117d7048b4b1d05d42fe` 复核重点路径。
- 已复核的后续 main：`afad6e37f045b157f7b6117d7048b4b1d05d42fe`，12:37:31 UTC（北京时间20:37:31），媒体分块 batch 6。发布前的最终复核见随包 `version-check.json`。
- 实质源码提交：`f73511238b3dd62cb40d46f7d8977964fe587f68`，12:21:00 UTC（北京时间20:21）。
- 上述源码提交和已核对的媒体提交，其 `video-agent` 子树均为 `7b76cef6d6415735582d6b946d2546c7200f6c77`。代码没有因为媒体分块继续上传而产生这些快照间的差异；媒体恢复完整性需另验。

证据分四层：
1. 本轮实际读取的仓库源码、Git树、配置元数据、CI执行状态及日志。
2. 仓库提交的实际生产报告；其本机渲染、音色查询、试听与下载结果不冒充本轮重跑。
3. 本轮在 Node v22.16.0 / Linux 运行的原函数隔离复现；使用合成工程记录，不是用户数据库或完整Agent。
4. 未完成：本轮未接用户 localhost，未运行其生产模型或付费MiniMax，未对完整成片连续观片/试听，未完成整仓冷启动测试，未修改或推送用户项目。

## 1. 总判断：四项目标尚未全部完成，必须保留已实现进展

| 用户目标 | 可以承认的进展 | 不能宣布完成的原因 |
|---|---|---|
| 7—8个高质量场景工作流 | 八入口、场景/操作合同、场景Skill、S02真实工程 | 最新交接仍将其他场景的完整制作、反例、输入变化、续改列为欠项；S02仍有明确画质/关系表达缺陷 |
| 路由、Skill和兜底 | 制作单、控制/制作/修改消息分流、通用规划、资源作用域改进 | 通用规划不是全链路通用制作；复杂范围解析仍有错误；各分支真实执行与兜底后续动作的证据不齐 |
| 字幕、配音、动效转场 | MiniMax真实TTS、字幕与本地音乐实际入片，资源绑定与部分修复 | 目前真调用主要证明S02片尾局部换声；整片/跨场景音频与十类资源未完整验收；音乐生成记录HTTP410 |
| 自然语言多轮再剪辑 | 消息分发、受控修改、换声、字幕样式保持、撤销重做、工程包有真实局部证据 | 完整连续操作链、S07精剪、S08两个真实变体、跨目录再次编辑音画导出及复杂冲突未完整证明 |

不要把代码文件存在、计划输出、HTTP接收成功、单元测试、可播放、视觉认可合并为同一个“通过”。也不要因为还未完成，就否定已经真正落地的MiniMax和原生编辑能力。

## 2. 最新证据和必须处理的问题

### 2.1 MiniMax 已不再是“只预留配置”

最新已提交的 `MiniMaxClient` 有请求执行、响应处理、媒体落地、字幕读取、缓存/锁、结果未知保护。工作台有音色查询、声音生成、入轨及替换入口；服务有 `audio-apply`、`audio-replace-speech` 等路由。

最新音频验证报告：
- `deliverables/minimax-audio-integrated-20260916/verification.json`
- 视频：`deliverables/minimax-audio-integrated-20260916/S02-MiniMax-voice-subtitles-music.mp4`
- 报告记载35秒、1920×1080、30fps、1050帧、H264/AAC；视频SHA256 `f025af4c199fb4618f826926d1340951f11342c5bf807a4b22fd1ed0d5029daf`。
- 报告记载8个版本、7个资产、原基准未变、撤销/重做保持声音与字幕。
- 最新执行状态记载303个真实音色、两次片尾换声、字幕上移后保持、新混音真实音轨差异。
- 限制：音乐API HTTP410，当前成片音乐来自已有本地原创资产；不是MiniMax音乐成功。完整创意与听感仍需真实审阅。

这份 `status:passed` 只覆盖它验证的音频/包行为，不覆盖八场景或S02全部质量。对“已有声音改成MiniMax”的历史记录保留原有旁白来源，不把只替换片尾描述为全片旁白均由MiniMax生成。

### 2.2 场景覆盖仍不足，S02质量问题不只是“等用户点同意”

`deliverables/s02-gpu-closeout-20260916/REVIEW.md` 明确记录：
- 19.2秒侧边连接座偏暗失焦，细节辨识不足，major。
- 33.4秒片尾三视角缺少准确整体位置联动，major。
- 多处说明卡底板早于文字出现，minor。

这是源报告对旧冻结修订的结论。音频新版本是否仍有对应问题，必须检查实际画面和变更范围；没有重新检查不能自动清零，也不能凭旧时间码给新版本随意打分。用户曾要求快速冻结的历史决定只解释当时状态，不替代本次完整交付目标。

场景账：
- S01：米家保留参考；需要符合当前非耳机约束的自有Agent完整正例、变化和续改。
- S02：已有真实生产与声音编辑，继续修画面/关系表达并补专有测试。
- S03：需完整开箱/安装/操作的真实正例，关键动作缺失反例不能代替正例。
- S04：需多款身份与组合关系的真实片，以及换顺序后不串事实。
- S05：需真实活动事实支撑的新片与改字段联动；不编日期价格。
- S06：需问题→答案→证据→限制的真实新片，不使用广告口号假回答。
- S07：需真实精剪版、删留对照、原意/原声保持和续改。
- S08：需同母工程两个真实派生版本，开头范围修改、画幅重布局分别验证，母版保持。

以上依据当前提交的交接和生产记录，未把历史预设、Mijia作者片或同一场景的多个修订算作新场景。

### 2.3 新旧阶段范围仍可能误导下一执行者

`docs/NON_GENERATION_WORKFLOWS.zh-CN.md` 仍将“暂停MiniMax/新成片制作，只完成规划”写成该阶段范围；旧交接又有“只做S02、其余移交”的决定。后续MiniMax已经有新成果，说明阶段文件不能直接当全局状态。

本次最高业务范围是用户明确的四项目标。只规划模式继续保留给用户明确要求的规划任务，不能自动启动付费或制作。正常制作请求则必须继续走观察、资源装配、渲染、审查和交付。不是删除规划功能，而是将阶段范围标为历史、让当前任务入口只指向一个有效收口状态。

### 2.4 通用规划已有实现，但不等于通用生产兜底完成

`service.mjs` 对 `job.kind==='plan'` 调用 `planWorkbenchWorkflow` 后保存制作单；这一分支明确不制作/修改视频，是合理的规划功能。`dispatchMessage` 则将消息按模式转到generate/patch等实际路径。

需要补验：正常制作/修改是否使用同一份有效、版本化的工作合同和资源约束；general/复合目的是否继续执行；错误回退是否保持原目的。不要把“规划入口不出片”本身误判成bug，也不要把规划结果算成真实生产成功。

### 2.5 原函数隔离复现：旧范围bug修好，仍有未覆盖表达

复现源：`lib/creative/resource-catalog.mjs`，Git blob `e71bfe470f1d7ce62897899a8e9e8b8b6bf030a0`。运行相关原函数摘录，4处转场。

| 输入 | 本轮实际解析 | 判断 |
|---|---|---|
| 只在第二处用色散，其余不用 | 仅第二处启用，其他排除 | 旧例已修复 |
| 只在第二处用色散，其余不要色散 | 同上 | 旧例已修复 |
| 第一处用色散，第三处用色散 | 第一、三处启用 | 旧例已修复 |
| 最后一处用色散 | 四处全部启用 | 仍扩大作用域 |
| 第二、第三处用色散 | 只启用第三处 | 仍遗漏并列省略位置 |
| 所有转场不要色散 | 四处全部排除 | 正确 |

结果在 `repro/isolated-results.json`。这是解析器级证据，未声称完整模型链路一定以完全同样方式失败；实施需从WebUI到规划、编译和导出继续验证。修复应采用稳定目标ID/包含排除集合；未能解析但明显带局部范围的表达必须澄清或返回未决，禁止静默扩大为全局。

### 2.6 作品入口：部分修复成立，精选/只读浏览仍未收完

新的 `finished-works.mjs` 已将ZIP与视频可用性分离，单项问题有unavailable原因。这项旧问题不应继续报“完全没修”。

但 `finished-work-options.mjs` 仍把所有非显式test且有导出版本的项目加入列表，按projectId而非来源谱系消重；前端选择preset仍调用loadPreset，后端loadPreset仍新建工程并复制已导出预设。

本轮用原完整列表函数（Git blob `71d1530630041de0bcae33c84a761b3c88c8f9ca`，本地逐字Git哈希校验一致）复现：同源3份工程副本＋原预设→4个同名选项。测试对象为人工夹具，不宣称用户本地数量。

修复方向：浏览只读；明确“创作/编辑副本”才创建；精选固定具体已选版本；历史与候选可访问；按谱系归组而非按标题删除。保留全部工程、媒体和合法不同作品。

### 2.7 当前源码提交CI失败，不能复用先前本机“全绿”结论

源码提交 `f73511238b3dd62cb40d46f7d8977964fe587f68`：
- Commerce native pipeline：run `35095743526`，failure。
- Conversational editing checks：run `35095743630`，Ubuntu和Windows的core均failure；后续浏览器/媒体等阶段被跳过。

已读取Commerce日志：`test-commerce-next.mjs` 旁白改写重测/恢复的两个测试没有到达预期中断点，实际分别出现“步骤预算已用完，检查点已保留”和“旁白音色不在当前账户或本地目录中”。本次组合43项中41通过、2失败。这是远端CI结果，不是本轮本机全仓测试。

根因尚不能仅凭这两条错误断定。应对照新增音色目录/供应商配置、注入provider契约、测试音色、旁白改写/重测和缓存失效，分别复现；测试环境不得读取生产密钥或默认触发付费调用。不能删除测试、吞异常、扩预算或关闭音色校验来过关。

### 2.8 本地凭据配置进入版本库：优先检查的安全风险

当前Git树跟踪 `video-agent/config/minimax.local.env`；`.gitignore` 中还有 `!config/*.local.env`、`!config/*.local.json`，会抵消前面的忽略规则。

本轮只读了路径元数据和ignore规则，**没有读取、回显、验证或复制其中密钥**，因此不声称已确定真实有效Key泄露。需要本地立即检查：若含真实凭据，先在供应商侧撤销/轮换，再停止跟踪并按授权处理历史和可能的媒体快照副本；仅补.gitignore不能取消既有跟踪。历史重写/强推必须另行确认，不破坏用户工作树。公开仓库和迁移包只保留无秘密的example配置，私密配置单独安全迁移。

## 3. 下一轮执行：保留总范围，按依赖把模块逐个做完

本文件是当前审查的实施增量，不重新发明八场景业务，不另建框架，不删除原M01—M12/F1—F3欠项。模块通过需要实现、真实入口、正反例、实际产物四层证据。阶段汇报后继续，不等用户再次说“继续”。

### W0 安全、当前范围和可重现基线（M01/M08/M10/M12）

- 在当前真实工作区核对origin、HEAD、已改文件、实际服务端口/构建/数据目录，保护活动任务。
- 收敛当前任务范围；保留只规划为显式功能，旧停止条件仅记历史。密钥存在状态改为实际核对，不再套旧“等待Key”。
- 执行2.8的无泄密检查，修忽略/同步/归档规则；不输出密钥，不擅自轮换其他服务或强推历史。
- 复现当前CI的两项旁白测试和其他失败套件；对齐provider能力、配置注入与voice校验。冷检出core/浏览器测试不使用生产私密配置。
- 验证媒体分块manifest→objects→完整恢复，逐文件哈希和缺块错误可见；不要因manifest已推送就宣布新设备可用，也不能因为分块尚在上传就断言永久缺文件。

**完成判据**：当前业务规则唯一、秘密不进入新提交/包、当前可运行基线和真实失败全部有归属；CI根因修复后实际重跑，未跑阶段仍待验。

### W1 制作单到实际执行与兜底（M01/M02/M03/M04）

- 复用message-routing/workflow-intent/workflow-design、场景包和production/edit工具。
- 一个工作合同贯穿请求、素材、资源、时间线、字幕音频及修订；不要规划完成后自由重解释并丢失条件。现有链已满足则补当前版本证据，不无谓重构。
- 用户显式只规划时仍不生成；正常新建/修改无需另贴开发Prompt或手写分镜。
- 八入口与general/复合请求使用同一套受控工具。S07/S08保留原业务目标，不能通过进入变体丢掉教程保护。
- 资源选择与兜底阶段均记录输入条件、失败原因、目标/保持对象、候选、实际动作、质量影响和下一步。
- 普通可逆风格默认继续；事实、必要动作和不可兼容要求才最小澄清。缺步骤不伪造，明确指定效果不静默替代。
- 模型不可用只使用已授权且能力匹配的备选；不能用读不了视频的模型替代动作审核再算通过。

**必验**：八个普通正例请求的合同与真正执行对应；同义表达/明确新建vs当前编辑/教程+竖屏/场景外明确操作/general能执行组合/未知关键事实/指定资源失败/仅规划不生成/容量失败保留现场。各兜底必须继续到可用候选或准确缺项，不只输出建议。

### W2 资源调用精度与十类实际装配（M04/M05）

- 修2.5新复现，保留已修旧例；再加倒数、并列省略、引用、否定、跨轮纠正、多资源、不存在目标、动作保护与锁定冲突。
- 名称/源码/runtime身份分开；中文意图不能被平台名误当资源名；所选资源必须在当前素材和版本下可执行。
- 对template、composition、component、effect、transition、skill、caption、typography、lower-third、shader分别记录适用产物和真实绑定。skill以规则消费与产物对应验证，不要求文本自己渲染。
- 官方资源直接绑定、受控适配、基于skill原创如实区分；不能把所有原创都标成官方组件直用。
- 每条正式片只用有业务价值的效果；定点shader可以用独立候选测试，不强迫教程炫技。
- 新资源先隔离适配、真实渲染、确认输入和字体/尺寸/时间，再接主工程；不能只靠目录数量完成。

**完成判据**：精确范围实际生效且保持对象未变；十类各有证据；换素材、改画幅后复验；没有SCENE A/B、黑场、示例纹理、遮挡关键动作或静默降级。

### W3 S02修质＋完整声音与多轮编辑（M05/M06/M07/M08/M09/M11）

- 保留S02旧冻结修订和MiniMax音频成果，选择正确新基准继续，不覆盖旧件。
- 核查并消除已记录的暗/虚细节、结尾整体—局部联动、底板与文字时序。合适的已有源段替换优先，不能用锐化/亮度掩盖无细节；不编造新商品画面。
- 旁白每个信息段绑定可见证据窗口。来源语音、生成旁白、音乐、音效、转写字幕和营销文字分别管理；保留实际供应商信息。
- 在真实WebUI用自然语言执行：改片尾文字→仅目标旁白换MiniMax音色→音乐变轻并淡出→字幕再上移→目标镜头换已有片段→撤销最后换镜头但保留声音和字幕→撤销声音再重做→恢复后导出。每一步复核对象、源区间、素材/音频哈希和实际MP4，不以容器字节相同作为唯一保持判据。
- 增补新建有声片：从脚本分段到真实TTS、字幕、混音、导出，而不仅片尾替换；不同文本长度/音色触发时间变化时保护必要动作。
- MiniMax音乐410：检查实际base URL/区域/账户权限/官方接口与响应，分清配置错误和供应商限制；不得仅凭410断言所有地区服务关闭。不可用时保留已有音乐或明确授权的本地方案，标清不是MiniMax音乐。无受影响的编辑不被阻塞。
- 源码语音能力和测试替身的协议同步；错误注入覆盖401/429/超时/未知提交/损坏媒体/字幕缺失/取消，结果未知不盲目重复收费。
- 工程包换目录真正导入、再改文字和音乐、再导出；只有校验ZIP和HyperFrames check不够。

**完成判据**：S02当前问题有真实修复证据；完整编辑链来自同一工程；已改对象不回退；MiniMax真调用、字幕时序、音乐和可移植再次导出分别成立，音频不可用与本地替代状态准确。

### W4 八场景逐一制作、修复和冻结（M02/M03，依赖W1—W3）

先检查素材池和已完成工程，不重复让用户选逐秒分镜。S02合格证据复用，其余七类分别完成。该阶段不是只增加七个示例名称。

| 场景 | 正例核心 | 专有反例/输入变化 | 真实续改 |
|---|---|---|---|
| S01 上新 | 非耳机商品，整体/可信重点/使用或真实展示/收尾 | 未给价格性能不补；换商品或同义需求 | 改结尾、声音保持 |
| S02 详情 | 整体—局部关系清楚，主张有证据 | 无规格不画假参数；换说明对象 | 改标注或卖点顺序 |
| S03 教程 | 必要动作完整、步骤先后正确、原声对应 | 关键动作缺失副本准确阻断；等待不误删 | 移步骤字、缩等待不删动作 |
| S04 系列 | 各款身份和组合关系明确 | 缺一款镜头不借另一款；换顺序 | 调换款式不串名称事实 |
| S05 活动 | 真实主题/条件/时间/行动可读 | 缺价格不造价、已读日期更改不能声画冲突 | 改活动字段及必要关联 |
| S06 问答 | 回答原问题，证据和限制可见 | 无依据不绝对承诺；换问题 | 精简答案仍保留边界 |
| S07 精剪 | 真删等待/重复，保留原意和动作 | 限定条件不能被删、原声不串 | 改一段节奏，其他内容保持 |
| S08 多版 | 同母工程≥2派生：改开头、改比例 | 母版/兄弟版保持，竖屏不机械裁主体 | 再改单一分支并重开导出 |

每场景：普通WebUI输入、素材哈希/真实源区间、Skill与资源版本、原生工程、MP4/实际封面、正例/反例/变化、一次续改、实际质量报告。模型调用、成本和耗时如实记录，不填虚构价格或速度提升。

没有所需素材时先查已授权素材范围和其他可用商品；确实缺失才精确列缺项。正确拒绝是反例，不是成片。文档/规则可完成，正例保留未完成，不把所有任务停掉，也不宣称八场景齐全。

### W5 产品展示、恢复、发布回归与最终收口（M10/M11/M12，F1—F3）

- 修作品浏览副作用、来源归组、精选/历史/候选和版本固定。连续只读浏览同预设20次不增加工程；明确创建副本有幂等，不删用户历史。
- 文件位置由当前服务数据目录与真实revision解析；用户能找到当前MP4和包。参考片不记Agent生产，失效条目有原因，ZIP缺失不再拖累视频。
- 真正执行制作中刷新、受控服务重启、重复提交、断网与过期revision冲突；恢复后还能修改并输出新产物，不只展示旧片。
- 冷目录恢复项目依赖与媒体；媒体分块重建/哈希验证和原生工程导入各验各的。未执行平台明确待验。
- 在最终源码commit上通过core、实际浏览器、FFmpeg、HyperFrames、现有平台CI；允许明确外部供应商测试单列，不能把skip当pass。
- 最终状态按四目标＋八场景＋原M01—M12生成，一个来源的证据可以复用，但不同能力不互相代替。

## 4. 最终结束条件与交付

工作顺序W0→W1/W2→W3→W4→W5；允许先修某场景阻断的底层依赖，然后回原场景。不是让用户分六次重发任务，不以阶段文档或某次渲染成功结束。

只可在全部当前可执行必验项完成后正常收尾。真实凭据/权限/素材/供应商限制或用户审阅单列，不绕过安全、不扩大授权、不无限重试；内部未实现和测试失败不能改叫外部限制。客户端被迫中断保存准确恢复点，不承诺无人执行时自动继续。

最终交付至少包括：
- 最终源码SHA、运行构建、数据目录、各平台与CI状态。
- 四目标结论，八场景实际状态，原M01—M12/F1—F3的逐项结论。
- 当前可播放作品、母工程与可编辑包、完整版本链和跨目录再导出证据。
- 路由/兜底/资源绑定、声音来源、调用与保持对象的记录。
- 具体画面/声音问题的修复位置；未覆盖审查、真实用户认可单列。
- 保留失败、未经批准的候选和未完成；不通过删除分母/伪造人评提高完成度。

## 5. 直接交给Codex的入口Prompt

你是当前 Metroids048/hyperframe / video-agent 的实施负责人。读取本文件，核对当前工作区与真实服务后立即执行W0—W5。沿用已有八场景业务设计、M01—M12/F1—F3，不另造产品或计划，不删除原欠项。

用户本次明确要四项目标全部实现。历史“只规划”“只做S02”“MiniMax暂停”的阶段范围不能代替当前范围；保留明确只规划的产品功能和全部安全约束。当前MiniMax已有真实TTS成果，复用它而非再写配置占位。

先安全核查被跟踪的本地配置及同步包，绝不输出密钥；真实秘密应轮换并安全迁移，历史强推另经授权。复现当前源码CI的旁白恢复/音色失败，修真实契约，不删测试、放宽音色校验或盲目增预算。

将通用意图、制作单、场景Skill、素材观察、资源执行、声音字幕、原生工程与再编辑接成同一版本化链路；正常制作请求不能停在计划，明确只规划的请求不能被擅自制作。场景外可实现请求和错误兜底必须有实际后续路径。

修“最后一处”被扩大到全部、“第二、第三处”丢第二处，并保持已修好的多位置与否定例。通过真实WebUI验证目标和保持对象，不只跑解析单测。十类资源分别在真实素材上验证，禁止官方示例纹理和静默换效果。

在现有S02/MiniMax工程上修当前major问题，做完声音、字幕、换已有镜头、撤销恢复、变体和跨目录再次编辑导出的完整链。MiniMax音乐410单独核对和如实处理，不能把本地音乐标成MiniMax，也不能因此停其他功能。

S02复用合格成果，其余七场景逐一从授权素材池选材、普通输入、自有Agent制作、实际成片检查、修复、反例、输入变化和续改。S08至少两条派生，参考米家不算Agent场景。不是一套模板换七个标题。

修作品只读浏览与精选/历史分离，不删用户任何工程素材。真实恢复与干净目录导入改字改音乐再导出，最终源码提交实际回归。所有代码本地运行检查后交付。

阶段汇报后继续下一可执行项，不等用户说继续。内部失败继续定位和复测；外部限制逐项证明并只暂停对应项。完成的是四目标产品，不是一份计划或一次出片。现在从W0的真实代码/配置/CI基线开始工具操作。

## 6. 来源导航（固定版本，可供本地逐项复核）

源码读取commit：ad5815707ac3b849d3d97344ea7166b623a5b3f0。后续同应用子树复核commit：afad6e37f045b157f7b6117d7048b4b1d05d42fe。

- S01 版本树与CI：https://api.github.com/repos/Metroids048/hyperframe/git/trees/dede56ab653e43071adb5fe6ec9c15dea93bec74
- S02 工作流边界：video-agent/docs/NON_GENERATION_WORKFLOWS.zh-CN.md
- S03 旧阶段交接：video-agent/docs/后续Agent交接_未完成项_2026-09-16.md
- S04 S02缺陷：video-agent/deliverables/s02-gpu-closeout-20260916/REVIEW.md
- S05 MiniMax实际证据：video-agent/deliverables/minimax-audio-integrated-20260916/verification.json
- S06 MiniMax实现：video-agent/lib/edit/adapters/minimax-client.mjs；video-agent/docs/MiniMax本地接入与本轮验证.md
- S07 消息/工作流：video-agent/lib/creative/message-routing.mjs、workflow-intent.mjs、workflow-design.mjs、service.mjs
- S08 资源：video-agent/lib/creative/resource-catalog.mjs、resource-discovery.mjs、capabilities.mjs
- S09 UI：video-agent/web/commerce.js、finished-work-options.mjs；video-agent/lib/creative/finished-works.mjs
- S10 配置安全：video-agent/.gitignore；config树元数据。未读取minimax.local.env的内容。
- CI1：https://github.com/Metroids048/hyperframe/actions/runs/35095743526
- CI2：https://github.com/Metroids048/hyperframe/actions/runs/35095743630
- H1：https://hyperframes.heygen.com/catalog（官方要求替换示例内容并在完整视频中检查，不能把Catalog示例视为已完成设计）
- M1：https://platform.minimaxi.com/docs/api-reference/music-generation（当前可访问官方接口文档；本轮未验证用户账户410原因）

官方网页只用于能力和接口参考，本地已锁定版本与实测优先。本文不是质量通过报告，也不是已提交的修复代码。
