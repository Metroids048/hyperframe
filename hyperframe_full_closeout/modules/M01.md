# M01｜输入、意图、运行时规则与兜底

本文件是总任务的一段，不是仅做本模块的范围变更。依赖以02_TASKS.json为准；当前模块没有就绪任务时查依赖卡，完成后返回。状态仅写项目绑定的唯一任务账。

## 本模块执行Prompt

读取01_EXECUTION_RULES.md和本模块当前未通过任务。沿真实入口定位具体功能，按“复现→根因→最小修改→正反例→真实集成→产物与保持检查”逐张执行。代码已具备就核实并复用，不只列清单；不要一次改本模块所有文件再统一找问题。
每项通过后接着下一项；遇依赖不足先修依赖，不把原项提前通过。模块卡全部完成也继续总队列。文档、替身、候选、真人认可分别记；日志和源配置不得泄露秘密。

## 任务索引

- M01-F01：统一新建、编辑、精剪、变体和控制意图
- M01-F02：制作单字段与跨轮约束继承
- M01-F03：同一有效合同贯穿规划与实际生产
- M01-F04：general和复合目的的实际执行链
- M01-F05：最小澄清、业务兜底与可恢复继续
- M01-F06：路由回执、异常归属和按能力恢复

### M01-F01｜统一新建、编辑、精剪、变体和控制意图

**归属**：M01 / G1、G2、G4；建议顺序 8。

**前置**：P00-04

**代码导航**：`video-agent/web/commerce.js`；`video-agent/lib/creative/message-routing.mjs`；`video-agent/lib/creative/workflow-intent.mjs`；`video-agent/lib/creative/workflow-design.mjs`；`video-agent/lib/creative/service.mjs`；`video-agent/prompts/commerce/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 复用message-routing与workflow-intent，先识别操作再识别业务目标；草稿也需正确处理状态/取消/只规划语义。
2. 明确另做一条不能因当前有工程变成edit；已有工程局部编辑不重新走完整导演；保存原话证据。
3. 上传、粘贴、场景下拉、快捷和URL入口汇入同一受控合同，显式UI选择与文字冲突最小澄清。

**正例与应观察结果**
- 另做一条/这一版改字/精剪/派生/撤销/status均进入正确路径；相同语义的入口一致。

**反例／禁止通过的情况**
- 引号里的“撤销”、否定的“不要取消”、明确只规划均不得触发错误动作或收费。

**最小证据**：真实WebUI路由回执；正反例message→mode/business表。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M01-1、M01-2、F3-07

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M01-F02｜制作单字段与跨轮约束继承

**归属**：M01 / G1、G2、G4；建议顺序 10。

**前置**：M01-F01

**代码导航**：`video-agent/web/commerce.js`；`video-agent/lib/creative/message-routing.mjs`；`video-agent/lib/creative/workflow-intent.mjs`；`video-agent/lib/creative/workflow-design.mjs`；`video-agent/lib/creative/service.mjs`；`video-agent/prompts/commerce/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 将当前原话、事实来源、声音要求、目标/排除/保持集、母revision及默认假设写入统一版本合同。
2. 实现字段级显式覆盖关系：新要求只覆盖对应旧要求；回滚与分支保留原合同版本。
3. 程序校验真实对象、时间范围、锁与事实；模型猜测不升级成用户确认事实。

**正例与应观察结果**
- 第一轮无音乐，第二轮改标题仍无音乐；明确后续授权加音乐只变声音字段。

**反例／禁止通过的情况**
- 不明确“第二个”不得猜另一对象；旧revision与包含排除冲突不进入patch。

**最小证据**：合同版本diff；目标/保持集合校验；过期/冲突反例。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M01-3、M01-4

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M01-F03｜同一有效合同贯穿规划与实际生产

**归属**：M01 / G1、G2；建议顺序 11。

**前置**：M01-F02

**代码导航**：`video-agent/web/commerce.js`；`video-agent/lib/creative/message-routing.mjs`；`video-agent/lib/creative/workflow-intent.mjs`；`video-agent/lib/creative/workflow-design.mjs`；`video-agent/lib/creative/service.mjs`；`video-agent/prompts/commerce/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 查plan-workflow、普通generate/message、production/edit的真实调用；复用一份工作合同或有明确来源的派生版本。
2. 挂接阶段Prompt和Skill哈希及消费回执；只规划保留独立行为，普通制作自动继续观察到导出。
3. 规则变动按依赖使未来阶段失效；冻结旧工程不自动迁移，新任务确实使用新规则。

**正例与应观察结果**
- 一次普通输入连续走到真实candidate；每阶段可追到同一需求/派生合同；用户不用贴技术Prompt。

**反例／禁止通过的情况**
- 只有plan JSON不能计视频；规则更新不能命中不兼容旧缓存；只规划不得暗中合成。

**最小证据**：端到端需求ID/阶段回执；规则变更与缓存试验；实际工程引用。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M01-1、M01-5

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M01-F04｜general和复合目的的实际执行链

**归属**：M01 / G1、G2、G4；建议顺序 12。

**前置**：M01-F03

**代码导航**：`video-agent/web/commerce.js`；`video-agent/lib/creative/message-routing.mjs`；`video-agent/lib/creative/workflow-intent.mjs`；`video-agent/lib/creative/workflow-design.mjs`；`video-agent/lib/creative/service.mjs`；`video-agent/prompts/commerce/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 将场景外但可实现的剪辑需求拆成现有受控能力；general不是放开任意shell/HTML权限的后门。
2. 教程+精剪+竖屏继承必要动作；多目的用主目标和辅助子流程，控制冲突优先级。
3. 通用请求进入同一观察、资源、排程、检查和交付，不能只返回建议或默认上新。

**正例与应观察结果**
- 商品视频加章节/角标等不命中六业务模板的请求实际生成可编辑版本；教程复合请求保留动作保护。

**反例／禁止通过的情况**
- 不具备的三维重建/暂停的视觉生成不能伪装已完成；复杂业务不能绕过事实和锁。

**最小证据**：general实际工程与MP4；复合合同继承表；能力缺失反例。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：该审查增量；不替代其他旧要求。

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M01-F05｜最小澄清、业务兜底与可恢复继续

**归属**：M01 / G1、G2；建议顺序 13。

**前置**：M01-F03

**代码导航**：`video-agent/web/commerce.js`；`video-agent/lib/creative/message-routing.mjs`；`video-agent/lib/creative/workflow-intent.mjs`；`video-agent/lib/creative/workflow-design.mjs`；`video-agent/lib/creative/service.mjs`；`video-agent/prompts/commerce/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 按缺事实、缺素材、对象歧义、能力不支持、资源失配分类，不把所有问题推给用户。
2. 可逆风格可用可见默认，非必要价格缺失不阻塞；必要动作/活动字段不编造。
3. 澄清回答关联原需求，保留已完成观察；批准的等价表达另记降级状态，并继续到可用候选或准确阻塞。

**正例与应观察结果**
- 缺非必要价格继续；补一个活动日期后从原检查点继续且不重问已知字段。

**反例／禁止通过的情况**
- 缺关键动作只形成明确不完整候选/缺项，不能算教程正例；指定效果不得静默替换。

**最小证据**：澄清前后同任务记录；兜底分支执行证据；降级/完整状态分离。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M01-4

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M01-F06｜路由回执、异常归属和按能力恢复

**归属**：M01 / G2、G4；建议顺序 14。

**前置**：M01-F04、M01-F05

**代码导航**：`video-agent/web/commerce.js`；`video-agent/lib/creative/message-routing.mjs`；`video-agent/lib/creative/workflow-intent.mjs`；`video-agent/lib/creative/workflow-design.mjs`；`video-agent/lib/creative/service.mjs`；`video-agent/prompts/commerce/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 失败记录必须对应实际采取的动作，不仅写nextAction；合并分类与运行状态避免二者相互矛盾。
2. 模型不可用使用已批准且视觉/工具能力兼容的备选；无备选保存现场，不无穷重试。
3. 分别验证暂时故障、鉴权、预算、用户取消；旧成果可看，恢复后可继续而不是只展示旧片。

**正例与应观察结果**
- 每种故障能查到原需求、基准、原因、实际动作、保持、恢复入口和质量状态。

**反例／禁止通过的情况**
- TTS模型不能代替视觉观察模型；失败回执的“保留”需有旧版本存在证据。

**最小证据**：故障注入回执；安全恢复结果；未触发错误供应商的调用记录。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：该审查增量；不替代其他旧要求。

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。
