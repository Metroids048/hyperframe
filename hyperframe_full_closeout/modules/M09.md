# M09｜自然语言多轮再编辑

本文件是总任务的一段，不是仅做本模块的范围变更。依赖以02_TASKS.json为准；当前模块没有就绪任务时查依赖卡，完成后返回。状态仅写项目绑定的唯一任务账。

## 本模块执行Prompt

读取01_EXECUTION_RULES.md和本模块当前未通过任务。沿真实入口定位具体功能，按“复现→根因→最小修改→正反例→真实集成→产物与保持检查”逐张执行。代码已具备就核实并复用，不只列清单；不要一次改本模块所有文件再统一找问题。
每项通过后接着下一项；遇依赖不足先修依赖，不把原项提前通过。模块卡全部完成也继续总队列。文档、替身、候选、真人认可分别记；日志和源配置不得泄露秘密。

## 任务索引

- M09-F01：真实基准、对象焦点与变更保持集合
- M09-F02：确定性编辑与复杂布局分流
- M09-F03：文字内容与已朗读事实的关联修改
- M09-F04：替换已有镜头与保持镜头邻居
- M09-F05：版本发布、撤销重做与失败回滚
- M09-F06：变体分支、开头限定和画幅重布局
- M09-F07：同工程八轮自然语言完整验收

### M09-F01｜真实基准、对象焦点与变更保持集合

**归属**：M09 / G4；建议顺序 43。

**前置**：M01-F02、M05-F01

**代码导航**：`video-agent/lib/creative/model-edit.mjs`；`video-agent/lib/creative/patch.mjs`；`video-agent/lib/creative/history.mjs`；`video-agent/lib/creative/branches.mjs`；`video-agent/lib/creative/edit-review.mjs`；`video-agent/lib/creative/service.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 复用原生对象ID和当前revision，不用标题文字模糊匹配所有同名对象。
2. 每次修改保存change/preserve及必要依赖；明确冲突只问最小问题。
3. 当前字幕值、焦点和跨轮引用来自工程与已执行记录，不依赖聊天记忆猜测。

**正例与应观察结果**
- “这一版第三段/字幕再上移”定位当前正确对象；同名两处按选择或澄清处理。

**反例／禁止通过的情况**
- 旧revision、未知对象、锁定冲突、多个同名候选不得误改。

**最小证据**：目标/保持集；冲突/同名对象用例；版本关联。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M09

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M09-F02｜确定性编辑与复杂布局分流

**归属**：M09 / G2、G4；建议顺序 55。

**前置**：M09-F01、M04-F04

**代码导航**：`video-agent/lib/creative/model-edit.mjs`；`video-agent/lib/creative/patch.mjs`；`video-agent/lib/creative/history.mjs`；`video-agent/lib/creative/branches.mjs`；`video-agent/lib/creative/edit-review.mjs`；`video-agent/lib/creative/service.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 字、字级、位移、音量、明确转场范围走受控patch；复杂需求才调用规划。
2. 复杂布局只在目标sourceBundle修改允许源码/参数，不创建任意工具权限。
3. 移除无谓重复规划和源观察，记录实际模型调用，不伪造速度收益。

**正例与应观察结果**
- 同一明确操作换说法行为一致，简单编辑不重走全片导演。

**反例／禁止通过的情况**
- 输入含引号/否定或复合目标不能被快捷正则吞半句；复杂修改不得重写其他场。

**最小证据**：分流回执；模型调用/缓存计数；局部源码范围审查。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F2-07

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M09-F03｜文字内容与已朗读事实的关联修改

**归属**：M09 / G3、G4；建议顺序 70。

**前置**：M09-F02、M07-F01

**代码导航**：`video-agent/lib/creative/model-edit.mjs`；`video-agent/lib/creative/patch.mjs`；`video-agent/lib/creative/history.mjs`；`video-agent/lib/creative/branches.mjs`；`video-agent/lib/creative/edit-review.mjs`；`video-agent/lib/creative/service.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 区分样式、纠错和改写原话；已朗读文字变更检查关联声轨。
2. 用户明确只改视觉且冲突时给清楚选择，不擅自生成；明确授权同步改旁白只重配相关段。
3. 保留活动条件/原义和其他内容，不以新摘要删除限制。

**正例与应观察结果**
- 片尾文字改对；活动日期在画面和相关音频最终一致或明确等待批准，不假交付。

**反例／禁止通过的情况**
- 只改屏幕日期保留旧播报、无授权重配、顺手删除限制均失败。

**最小证据**：文字/声音依赖diff；内容/样式分流；实际冲突处理。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F3-03

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M09-F04｜替换已有镜头与保持镜头邻居

**归属**：M09 / G4；建议顺序 71。

**前置**：M09-F02、M02-F03、M06-F03

**代码导航**：`video-agent/lib/creative/model-edit.mjs`；`video-agent/lib/creative/patch.mjs`；`video-agent/lib/creative/history.mjs`；`video-agent/lib/creative/branches.mjs`；`video-agent/lib/creative/edit-review.mjs`；`video-agent/lib/creative/service.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 先核对目标用途与同商品备选源段，选择真实区间；新视觉生成保持暂停。
2. 更新目标媒体、必要布局及对应原声，不改其他源选段、文字和声音。
3. 范围不足/身份不同/锁冲突准确拒绝或要求替代，不自动换整片策划。

**正例与应观察结果**
- 第三段换已有同部位镜头后1/2/4等邻居内容保持；实片不越界。

**反例／禁止通过的情况**
- 无备选不调用新镜头生成；假指另商品片、改全片镜头失败。

**最小证据**：源区间与邻居hash/对象diff；实际目标片段；视觉生成调用=0。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F3-04

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M09-F05｜版本发布、撤销重做与失败回滚

**归属**：M09 / G4；建议顺序 72。

**前置**：M09-F03、M09-F04

**代码导航**：`video-agent/lib/creative/model-edit.mjs`；`video-agent/lib/creative/patch.mjs`；`video-agent/lib/creative/history.mjs`；`video-agent/lib/creative/branches.mjs`；`video-agent/lib/creative/edit-review.mjs`；`video-agent/lib/creative/service.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 唯一revision和发布事务只在有效当前基准上更新，迟到/取消的候选不覆盖新修改。
2. 撤销最近一次换镜头保持先前字幕音色；选择性恢复受限定范围，不整条回到v0。
3. 发布/保存失败保留旧有效版本和新候选证据，实际导出与revision一致。

**正例与应观察结果**
- 连续撤销/重做正确恢复目标操作，声音和字幕不回退。

**反例／禁止通过的情况**
- 同revision不同导出、迟到结果覆盖、失败半发布、取消后继续发布均失败。

**最小证据**：版本DAG及事务记录；实际撤销/重做diff；失败与并发用例。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F3-01、F3-04

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M09-F06｜变体分支、开头限定和画幅重布局

**归属**：M09 / G1、G4；建议顺序 73。

**前置**：M09-F05、M05-F12

**代码导航**：`video-agent/lib/creative/model-edit.mjs`；`video-agent/lib/creative/patch.mjs`；`video-agent/lib/creative/history.mjs`；`video-agent/lib/creative/branches.mjs`；`video-agent/lib/creative/edit-review.mjs`；`video-agent/lib/creative/service.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 明确母版revision和变化/保持集；分别创建开头分支与画幅分支，非顺序覆盖。
2. 只开头时保持后文绝对时间/文字/声音合同；改比例按保护区重布局而非只改尺寸。
3. 母版/兄弟版hash和对象图保持，单分支再修改不影响其他分支。

**正例与应观察结果**
- 两个真实派生片可播放、原生可重开，母版/兄弟版仍一致。

**反例／禁止通过的情况**
- 中心裁切丢商品、只改width height、开头操作改后文、复制MP4假多版失败。

**最小证据**：母/兄弟/分支diff；两派生MP4；多比例实际检查。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：该审查增量；不替代其他旧要求。

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M09-F07｜同工程八轮自然语言完整验收

**归属**：M09 / G3、G4；建议顺序 74。

**前置**：M09-F06、M08-F03、M06-F05、M07-F04

**代码导航**：`video-agent/lib/creative/model-edit.mjs`；`video-agent/lib/creative/patch.mjs`；`video-agent/lib/creative/history.mjs`；`video-agent/lib/creative/branches.mjs`；`video-agent/lib/creative/edit-review.mjs`；`video-agent/lib/creative/service.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 选正确S02/MiniMax已有工程，不用米家作者片；通过真实WebUI依次改片尾、目标换声、音乐低/淡出、字幕再上移、替换已有镜头。
2. 继续撤销换镜头但保留前声字、创建竖屏分支；逐轮记录当前版本、目标和保持项。
3. 每轮需实际预览检查；完成链后导出覆盖关键变化的MP4，模型/API调用与文件结果真实对应。

**正例与应观察结果**
- 八轮来自同一版本链且每轮保持成立，普通后期无新商品图/视频生成。

**反例／禁止通过的情况**
- 用多个孤立工程、结构化直传替代自然语言全流程、只看最终差异不检查中间保持均失败。

**最小证据**：conversation-transcript（脱敏）；逐轮revision/diff/调用；实际浏览器及导出证据。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F3-01、F3-02、F3-03、F3-04、M09

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。
