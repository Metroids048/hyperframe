# M04｜HyperFrames资源发现与精准路由

本文件是总任务的一段，不是仅做本模块的范围变更。依赖以02_TASKS.json为准；当前模块没有就绪任务时查依赖卡，完成后返回。状态仅写项目绑定的唯一任务账。

## 本模块执行Prompt

读取01_EXECUTION_RULES.md和本模块当前未通过任务。沿真实入口定位具体功能，按“复现→根因→最小修改→正反例→真实集成→产物与保持检查”逐张执行。代码已具备就核实并复用，不只列清单；不要一次改本模块所有文件再统一找问题。
每项通过后接着下一项；遇依赖不足先修依赖，不把原项提前通过。模块卡全部完成也继续总队列。文档、替身、候选、真人认可分别记；日志和源配置不得泄露秘密。

## 任务索引

- M04-F01：全量资源索引、真实身份与状态分层
- M04-F02：中文功能意图、召回与候选解释
- M04-F03：输入、画幅、文字和动作硬过滤
- M04-F04：最后、倒数与并列省略的精确范围
- M04-F05：资源计划与实际执行一致性
- M04-F06：资源刷新、缓存和验证失效

### M04-F01｜全量资源索引、真实身份与状态分层

**归属**：M04 / G2；建议顺序 36。

**前置**：P00-04

**代码导航**：`video-agent/lib/creative/resource-discovery.mjs`；`video-agent/lib/creative/resource-catalog.mjs`；`video-agent/lib/creative/capabilities.mjs`；`video-agent/config/hyperframes/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 遍历已安装HyperFrames授权根，记录canonical/display/catalog/composition/runtime标识、路径和内容版本。
2. 功能十标签和来源类型分离，block/component/example/skill装配不同；相同镜像去重不丢可追溯来源。
3. 状态区分discovered/wired/validated_in_context；上下文包含版本、比例、输入与依赖，不做永久通用认证。

**正例与应观察结果**
- 用户指定名称可定位真实文件与执行标识；扫描范围和不可读项明确。

**反例／禁止通过的情况**
- 文件存在不标可用；只有读过skill不标渲染通过；未扫目录不报全量完整。

**最小证据**：资源索引/边界/状态统计；重复来源映射。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M04-6

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M04-F02｜中文功能意图、召回与候选解释

**归属**：M04 / G2；建议顺序 37。

**前置**：M04-F01、M01-F02

**代码导航**：`video-agent/lib/creative/resource-discovery.mjs`；`video-agent/lib/creative/resource-catalog.mjs`；`video-agent/lib/creative/capabilities.mjs`；`video-agent/config/hyperframes/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 将克制/突出细节/避免遮挡等转换为功能与硬限制，再召回真实资源。
2. 精确名称/别名/URL与自由功能请求分开；“HyperFrames资源”不是一个待匹配资源名。
3. 规则匹配和模型语义重排分别留记录，优先兼容且可执行候选，不盲加向量数据库。

**正例与应观察结果**
- 等价中文表达得到相同功能约束；候选不同但选择理由与实际契约一致。

**反例／禁止通过的情况**
- 标题名称命中但无执行器不能标resolve；虚构资源不猜一个名字替代。

**最小证据**：query→intent→候选/排除/选择回执。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M04-1、F2-04

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M04-F03｜输入、画幅、文字和动作硬过滤

**归属**：M04 / G2、G3；建议顺序 38。

**前置**：M04-F02、M02-F04

**代码导航**：`video-agent/lib/creative/resource-discovery.mjs`；`video-agent/lib/creative/resource-catalog.mjs`；`video-agent/lib/creative/capabilities.mjs`；`video-agent/config/hyperframes/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 先检查媒体数量/类型、画幅/尺寸、文字容量、依赖、版本、保护区和锁，再排序。
2. 记录每个排除原因和可恢复动作；相关分数不能覆盖硬冲突。
3. 版本改变和缺依赖在隔离资源副本模拟，已通过状态按条件失效。

**正例与应观察结果**
- 教程选到低遮挡资源；两个输入的效果在只有一个输入时不执行；长中文有可行布局。

**反例／禁止通过的情况**
- 超长文案无限缩字号、依赖缺失继续成功、保护区被强转场遮盖均失败。

**最小证据**：兼容矩阵；缺依赖/版本/文本/媒体反例。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M04-2、M04-4、M04-5、F2-03

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M04-F04｜最后、倒数与并列省略的精确范围

**归属**：M04 / G2、G4；建议顺序 39。

**前置**：M04-F02、M01-F02

**代码导航**：`video-agent/lib/creative/resource-discovery.mjs`；`video-agent/lib/creative/resource-catalog.mjs`；`video-agent/lib/creative/capabilities.mjs`；`video-agent/config/hyperframes/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 复现审查的“最后一处→全部”和“第二、第三处→只第三”；实现相对索引、并列省略与范围集合。
2. 解析结果绑定基准revision里的稳定转场ID；多资源分别拥有include/exclude，未决局部表达禁止默认all。
3. 保留已修的第二处/其余、多位置和全局否定；加入倒数第二、第2至第4处、引用/否定、跨轮更正。

**正例与应观察结果**
- 四处转场：最后→第4；第二、第三→2和3；只第二其余不要→仅2；第一第三→1和3。

**反例／禁止通过的情况**
- 不存在目标、仅局部未解析、锁定冲突返回未决/准确错误；不能默认为全部或最近对象。

**最小证据**：解析器before/after；对象ID/实际作用域diff；WebUI到导出定点片。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M04-3、F2-01、F2-02

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M04-F05｜资源计划与实际执行一致性

**归属**：M04 / G2、G3、G4；建议顺序 40。

**前置**：M04-F03、M04-F04

**代码导航**：`video-agent/lib/creative/resource-discovery.mjs`；`video-agent/lib/creative/resource-catalog.mjs`；`video-agent/lib/creative/capabilities.mjs`；`video-agent/config/hyperframes/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 选定资源的参数、对象、时段与来源写入实际工程绑定；编译器不能另选一个效果却沿用旧回执。
2. 被排除位置保持原样；源输入hash和运行时记录可追到渲染产物。
3. 显式指定资源失败需修适配/报告，装饰性资源按批准策略降级且保留质量状态。

**正例与应观察结果**
- 第二处的目标效果出现在第二处，其他转场/音轨不变；平台资源与自写适配来源不同标识。

**反例／禁止通过的情况**
- 回执说色散但画面是淡入、只示例SCENE A/B、覆盖全部绑定均失败。

**最小证据**：执行前后资源binding；转场前中后连续帧；真正调用回执。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F2-04、F2-06、M04-3

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M04-F06｜资源刷新、缓存和验证失效

**归属**：M04 / G2；建议顺序 41。

**前置**：M04-F05

**代码导航**：`video-agent/lib/creative/resource-discovery.mjs`；`video-agent/lib/creative/resource-catalog.mjs`；`video-agent/lib/creative/capabilities.mjs`；`video-agent/config/hyperframes/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 新本地资源纳入索引；变更源码、字体、运行版本、参数/输入契约影响的缓存按依赖失效。
2. 旧冻结工程保留实际源副本或内容寻址锁；不能目录更新使旧工程不可重开。
3. 统计真实模型调用和命中，复杂任务复用有效观察，普通参数修改不重检全库。

**正例与应观察结果**
- 新增资源可召回，改版本后旧认证不误用；旧工程仍能按锁定内容重开。

**反例／禁止通过的情况**
- 只改timestamp重验全库、错复用旧hash、宣称无证据提速均不通过。

**最小证据**：刷新/失效试验；资源锁与模型计数/耗时。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M04-6、F2-07

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。
