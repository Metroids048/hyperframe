# M12｜作品工作台、质量与发布回归

本文件是总任务的一段，不是仅做本模块的范围变更。依赖以02_TASKS.json为准；当前模块没有就绪任务时查依赖卡，完成后返回。状态仅写项目绑定的唯一任务账。

## 本模块执行Prompt

读取01_EXECUTION_RULES.md和本模块当前未通过任务。沿真实入口定位具体功能，按“复现→根因→最小修改→正反例→真实集成→产物与保持检查”逐张执行。代码已具备就核实并复用，不只列清单；不要一次改本模块所有文件再统一找问题。
每项通过后接着下一项；遇依赖不足先修依赖，不把原项提前通过。模块卡全部完成也继续总队列。文档、替身、候选、真人认可分别记；日志和源配置不得泄露秘密。

## 任务索引

- M12-F01：预设只读浏览与显式副本创建
- M12-F02：精选、候选、参考和历史归组
- M12-F03：准确文件路径和八入口产品一致性
- M12-F04：跨场景差异、迁移素材和全量能力回归
- M12-F05：最终源码、WebUI、媒体和各平台回归
- M12-F06：汇总四目标发布结论并提供结果入口

### M12-F01｜预设只读浏览与显式副本创建

**归属**：M12 / G4；建议顺序 6。

**前置**：P00-04

**代码导航**：`video-agent/web/commerce.js`；`video-agent/web/finished-work-options.mjs`；`video-agent/lib/creative/finished-works.mjs`；`video-agent/lib/creative/delivery-gate.mjs`；`video-agent/lib/creative/final-quality.mjs`；`video-agent/lib/creative/service.mjs`；`.github/workflows/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 复现选择preset触发loadPreset复制工程；拆成只读观看和明确编辑副本两个动作。
2. 进入观看不改变工程/版本；用户明确创建副本用幂等请求，保留来源谱系。
3. 切换作品前保存未提交输入/附件状态，不让浏览丢当前需求。

**正例与应观察结果**
- 连续观看同预设20次工程/版本/媒体数不增长；明确创建副本只创建一次。

**反例／禁止通过的情况**
- 以删除历史或隐藏全部作品修重复失败；连续双击不能复制两份。

**最小证据**：前后工程/版本/文件计数；浏览器20次+创建测试；草稿保持。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F1-02、F1-03、F1-07

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M12-F02｜精选、候选、参考和历史归组

**归属**：M12 / G1、G4；建议顺序 7。

**前置**：M12-F01

**代码导航**：`video-agent/web/commerce.js`；`video-agent/web/finished-work-options.mjs`；`video-agent/lib/creative/finished-works.mjs`；`video-agent/lib/creative/delivery-gate.mjs`；`video-agent/lib/creative/final-quality.mjs`；`video-agent/lib/creative/service.mjs`；`.github/workflows/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 精选显式锁定作品/版本，候选与完整历史另有可达入口；米家保留参考身份。
2. 按来源谱系归组未修改副本，同名不同作品保留；用户已改历史副本不可删除。
3. 复验已修ZIP缺失不隐藏视频；缺视频或坏hash单条报错不拖垮列表。

**正例与应观察结果**
- 米家等精选可找，最新候选/历史可看，测试导出不能顶掉精选版。

**反例／禁止通过的情况**
- 按标题粗删历史、参考标Agent、缺ZIP拖垮视频列表失败。

**最小证据**：作品来源/版本清单；同名/同源/缺ZIP反例；真实UI路径。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F1-01、F1-04、F1-05

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M12-F03｜准确文件路径和八入口产品一致性

**归属**：M12 / G1、G2、G4；建议顺序 9。

**前置**：M12-F02、M01-F01

**代码导航**：`video-agent/web/commerce.js`；`video-agent/web/finished-work-options.mjs`；`video-agent/lib/creative/finished-works.mjs`；`video-agent/lib/creative/delivery-gate.mjs`；`video-agent/lib/creative/final-quality.mjs`；`video-agent/lib/creative/service.mjs`；`.github/workflows/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 路径由运行数据目录和revision解析，不固定3020/3024或猜Users路径；下载与复制路径指同一文件。
2. 八场景的下拉/快捷/URL和普通消息语义一致，未完成工作流不写已通过。
3. MiniMax和字幕/动效/修改入口回到现有工作台，不另建测试专页作为交付。

**正例与应观察结果**
- 刷新/切版后路径、下载、hash一致；每个入口都有正确任务与状态。

**反例／禁止通过的情况**
- 可点按钮却进不同业务或旧端口、历史revision下载当前文件失败。

**最小证据**：八入口对照；路径/HTTP/hash验证；状态UI检查。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F1-06、F3-07

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M12-F04｜跨场景差异、迁移素材和全量能力回归

**归属**：M12 / G1、G2、G3、G4；建议顺序 114。

**前置**：M03-S02-06、M03-S01-06、M03-S03-06、M03-S04-06、M03-S05-06、M03-S06-06、M03-S07-06、M03-S08-06、M09-F07、M10-F04、M11-F03

**代码导航**：`video-agent/web/commerce.js`；`video-agent/web/finished-work-options.mjs`；`video-agent/lib/creative/finished-works.mjs`；`video-agent/lib/creative/delivery-gate.mjs`；`video-agent/lib/creative/final-quality.mjs`；`video-agent/lib/creative/service.mjs`；`.github/workflows/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 八场景逐项核实正例/反例/变化/续改；同一组支持两种目的的素材分别走不同业务，结果不能只换标题。
2. 选择一组未参与修复调优的授权素材，检验意图、选材、资源和排版；固定商品名/路径判失败。
3. 复查十类资源的上下文证据及每一旧义务，消除由共用改动引入的跨场景回归；记录真实调用/成本，不设虚构提速。

**正例与应观察结果**
- 八类均有各自业务闭环且至少一组跨场景对照/未调优输入通过；无遗漏旧ID。

**反例／禁止通过的情况**
- 以2个场景替8个、以版本数替场景数、只模仿米家片架构失败。

**最小证据**：四目标/八场景/十资源总矩阵；跨场景/未调优素材结果；旧账证据引用。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M12

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M12-F05｜最终源码、WebUI、媒体和各平台回归

**归属**：M12 / G1、G2、G3、G4；建议顺序 115。

**前置**：M12-F03、M12-F04、P00-03、P00-05

**代码导航**：`video-agent/web/commerce.js`；`video-agent/web/finished-work-options.mjs`；`video-agent/lib/creative/finished-works.mjs`；`video-agent/lib/creative/delivery-gate.mjs`；`video-agent/lib/creative/final-quality.mjs`；`video-agent/lib/creative/service.mjs`；`.github/workflows/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 在最终源码SHA或明确dirty tree指纹重跑core、浏览器、FFmpeg/HyperFrames、受支持平台CI；若仅媒体批次变记录应用子树证明，不混用旧代码报告。
2. 冷启动、实际首屏、上传/新建/编辑/分支/导出/下载/重新打开完整验证；预设只读和准确路径复验。
3. 慢/并发测试失败查实际争用与预算，不随意skip；执行后又改依赖代码则受影响结果失效重新跑。

**正例与应观察结果**
- 最终应用版本有完整回归证据；技术和视觉/声音状态对应真实同一revision。

**反例／禁止通过的情况**
- 旧SHA通过抵消新失败、Ubuntu替Windows、skip计pass、人工听感不曾发生却签字失败。

**最小证据**：最终SHA/构建/数据目录；全套真实报告和CI状态；媒体完整解码/实际浏览器。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F3-08、F3-09

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M12-F06｜汇总四目标发布结论并提供结果入口

**归属**：M12 / G1、G2、G3、G4；建议顺序 116。

**前置**：P00-01、P00-02、P00-03、P00-04、P00-05、M01-F01、M01-F02、M01-F03、M01-F04、M01-F05、M01-F06、M02-F01、M02-F02、M02-F03、M02-F04、M02-F05、M04-F01、M04-F02、M04-F03、M04-F04、M04-F05、M04-F06、M05-F01、M05-F02、M05-F03、M05-F04、M05-F05、M05-F06、M05-F07、M05-F08、M05-F09、M05-F10、M05-F11、M05-F12、M08-F01、M08-F02、M08-F03、M08-F04、M08-F05、M06-F01、M06-F02、M06-F03、M06-F04、M06-F05、M07-F01、M07-F02、M07-F03、M07-F04、M09-F01、M09-F02、M09-F03、M09-F04、M09-F05、M09-F06、M09-F07、M10-F01、M10-F02、M10-F03、M10-F04、M11-F01、M11-F02、M11-F03、M12-F01、M12-F02、M12-F03、M03-S02-01、M03-S02-02、M03-S02-03、M03-S02-04、M03-S02-05、M03-S02-06、M03-S01-01、M03-S01-02、M03-S01-03、M03-S01-04、M03-S01-05、M03-S01-06、M03-S03-01、M03-S03-02、M03-S03-03、M03-S03-04、M03-S03-05、M03-S03-06、M03-S04-01、M03-S04-02、M03-S04-03、M03-S04-04、M03-S04-05、M03-S04-06、M03-S05-01、M03-S05-02、M03-S05-03、M03-S05-04、M03-S05-05、M03-S05-06、M03-S06-01、M03-S06-02、M03-S06-03、M03-S06-04、M03-S06-05、M03-S06-06、M03-S07-01、M03-S07-02、M03-S07-03、M03-S07-04、M03-S07-05、M03-S07-06、M03-S08-01、M03-S08-02、M03-S08-03、M03-S08-04、M03-S08-05、M03-S08-06、M12-F04、M12-F05

**代码导航**：`video-agent/web/commerce.js`；`video-agent/web/finished-work-options.mjs`；`video-agent/lib/creative/finished-works.mjs`；`video-agent/lib/creative/delivery-gate.mjs`；`video-agent/lib/creative/final-quality.mjs`；`video-agent/lib/creative/service.mjs`；`.github/workflows/`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 读取全部任务有效回执及旧义务映射，而不是让模型凭印象填写“全部完成”。
2. 按四目标、八场景、十资源、旧M/F分别生成结果与未决项；提供每组准确MP4/原生包/封面/版本/来源。
3. 工程完成、候选交付、用户认可、外部能力受阻分别列出；只有实际全部条件满足才用对应完成语句。未解决内部项继续执行，不将其外部化。

**正例与应观察结果**
- 用户能从现有工作台找到每组真实作品并继续改；总账与原始报告一致。

**反例／禁止通过的情况**
- 隐去一个blocked任务提高完成率、以供应商音乐受限宣称所有音频做完、Agent自签用户认可失败。

**最小证据**：FINAL_REPORT.md；四目标结果清单；原旧义务逐项结论与所有产物引用。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：该审查增量；不替代其他旧要求。

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。
