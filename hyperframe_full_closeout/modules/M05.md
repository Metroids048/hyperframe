# M05｜十类资源装配与原生制作

本文件是总任务的一段，不是仅做本模块的范围变更。依赖以02_TASKS.json为准；当前模块没有就绪任务时查依赖卡，完成后返回。状态仅写项目绑定的唯一任务账。

## 本模块执行Prompt

读取01_EXECUTION_RULES.md和本模块当前未通过任务。沿真实入口定位具体功能，按“复现→根因→最小修改→正反例→真实集成→产物与保持检查”逐张执行。代码已具备就核实并复用，不只列清单；不要一次改本模块所有文件再统一找问题。
每项通过后接着下一项；遇依赖不足先修依赖，不把原项提前通过。模块卡全部完成也继续总队列。文档、替身、候选、真人认可分别记；日志和源配置不得泄露秘密。

## 任务索引

- M05-F01：统一原生对象与受控参数装配入口
- M05-F02：模板跨商品复用
- M05-F03：子合成尺寸与时间嵌入
- M05-F04：组件绑定真实部位
- M05-F05：局部效果与参数变化
- M05-F06：指定边界的真实转场
- M05-F07：指导规则消费与产物对应
- M05-F08：字幕资源输入和独立样式
- M05-F09：中文排版与字形保证
- M05-F10：信息条保护商品与动作
- M05-F11：真实纹理与预览导出一致
- M05-F12：跨资源风格一致与复杂布局局部修复

### M05-F01｜统一原生对象与受控参数装配入口

**归属**：M05 / G2、G3、G4；建议顺序 42。

**前置**：M04-F05

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 复用native-recipes/compiler/custom-source：稳定对象ID、真实素材槽、布局参数、动画参数和音轨职责清晰。
2. 先验证静态最可见布局再加进出运动；位置/字号编辑不能被旧GSAP覆盖。
3. 成熟需求参数化；确需新资源时隔离适配再注册，不每幕重新自由编整片源码。

**正例与应观察结果**
- 真实S02备份工程在现有服务里能修改参数并导出，保留媒体身份与时间线。

**反例／禁止通过的情况**
- 修改一个字引发换全场模板或替换其他媒体失败；任意网络/脚本不能经资源说明进入执行。

**最小证据**：原生对象/参数契约；实际服务集成版本；越权/受控源码反例。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：该审查增量；不替代其他旧要求。

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F02｜模板跨商品复用

**归属**：M05 / G2、G3；建议顺序 44。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 可替换素材、事实、标题和CTA，删除不适用槽位，不虚构价格。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- 换一组已有同类型商品素材和文案，完成实例化与一处续改。

**反例／禁止通过的情况**
- 固定商品名/绝对素材路径、为了填槽编价格。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-template、F2-05

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F03｜子合成尺寸与时间嵌入

**归属**：M05 / G2、G3；建议顺序 45。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 解析内部composition ID、宿主起点、源时间、尺寸和总时长，按锁定运行版本执行。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- 真实子合成在主片非零起点出现与退出，seek和导出都正确。

**反例／禁止通过的情况**
- 子片漂移、缺幕、媒体源偏移错或主体被裁掉。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-composition

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F04｜组件绑定真实部位

**归属**：M05 / G2、G3；建议顺序 46。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 细节卡/callout并入宿主，以真实对象和稳定/冻结/可靠跟踪之一绑定。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- 修改标注位置或文字只改变目标组件且指向正确。

**反例／禁止通过的情况**
- 示例数据残留或箭头无法跟踪却假装跟踪。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-component

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F05｜局部效果与参数变化

**归属**：M05 / G2、G3；建议顺序 47。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 区分对象效果和跨镜头转场；范围、颜色与商品真实性受约束。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- 对一处细节施加局部强调并改参数，其他镜头不变。

**反例／禁止通过的情况**
- 全片受影响、商品颜色或结构被无依据改变。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-effect

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F06｜指定边界的真实转场

**归属**：M05 / G2、G3；建议顺序 48。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 使用两段真实商品素材，验证输入、重叠、边界帧及总时长。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- Chromatic Radial Split至少一次经Agent指定位置使用，前/中/后及离线结果一致。

**反例／禁止通过的情况**
- SCENE A/B、黑场、错误素材、静默替换或时長累计错误。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-transition、F2-06

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F07｜指导规则消费与产物对应

**归属**：M05 / G2、G3；建议顺序 49。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 记录实际加载的Skill版本，具体规则怎样影响策划/对象/检查，不要求文本自己渲染。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- 例如动作保护规则使原片必要动作保留且步骤说明避开手部，有源区间和对象依据。

**反例／禁止通过的情况**
- 只记录“已读skill”却无适用决策及工程对应。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-skill

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F08｜字幕资源输入和独立样式

**归属**：M05 / G2、G3；建议顺序 50。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 使用真实导入cue/原声识别/供应商时间，字幕作为独立可编辑对象。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- 移动/换样式后字幕仍对应声音，无新增TTS调用。

**反例／禁止通过的情况**
- 均分字数假造逐字时间、截断中文、把营销标题当ASR字幕。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-caption

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F09｜中文排版与字形保证

**归属**：M05 / G2、G3；建议顺序 51。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 用实际字体/短长中文、型号和标点验证层级、字宽和语义断行。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- 改标题字级后清楚可读且不越界，横竖比例分别检查。

**反例／禁止通过的情况**
- 缺字、孤字行、把关键条件缩成极小字隐藏。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-typography

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F10｜信息条保护商品与动作

**归属**：M05 / G2、G3；建议顺序 52。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 根据主体/手部保护区选择位置、容器和时段，不机械固定底部。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- 教程画面中信息条显示并按自然语言移开关键区域。

**反例／禁止通过的情况**
- 遮住手或接口、示例文字、超出安全区域。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-lower-third

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F11｜真实纹理与预览导出一致

**归属**：M05 / G2、G3；建议顺序 53。

**前置**：M05-F01

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 核验本地WebGL/shader执行路径、纹理输入、时间和释放，记录环境条件。
2. 用当前真实商品工程经现有服务验证；独立测试候选可复用，但不得算成新业务场景。
3. 记录原生对象、源hash、参数、版本、适用比例；未覆盖环境不升级为全局认证。

**正例与应观察结果**
- 实际商品纹理在预览和离线导出中相同效果，短片完整解码。

**反例／禁止通过的情况**
- 示例纹理、预览好但导出黑、依赖缺失伪装通过。

**最小证据**：该类别实片/原生绑定；参数续改与实际帧；适用条件与来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M05-shader、F2-06

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M05-F12｜跨资源风格一致与复杂布局局部修复

**归属**：M05 / G1、G2、G3；建议顺序 54。

**前置**：M05-F02、M05-F03、M05-F04、M05-F05、M05-F06、M05-F09

**代码导航**：`video-agent/lib/creative/native-recipes.mjs`；`video-agent/lib/creative/compiler.mjs`；`video-agent/lib/creative/custom-source.mjs`；`video-agent/lib/creative/resource-receipts.mjs`；`video-agent/lib/creative/chromatic-split.mjs`；`video-agent/lib/creative/isolation.mjs`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 同片资源共享已定义字体层级、留白和运动节奏；装饰不掩盖信息任务。
2. 针对实际S02关系表达，整体定位→局部→回到整体，避免重复框+文字替代解释。
3. 布局问题只使对应资源/镜头失效，必要重新渲染与重新生成商品素材区分。

**正例与应观察结果**
- 不同资源组成完整视觉语言，局部修复不毁已通过场景；真实成片支持定位具体增值。

**反例／禁止通过的情况**
- 七场景换标题同皮肤、全片每切点强特效、无必要的自由源码重写不通过。

**最小证据**：同片设计与对象约束；组合渲染检查；最小失效/修复diff。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：该审查增量；不替代其他旧要求。

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。
