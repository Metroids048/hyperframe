# M08｜MiniMax真实接入与供应商边界

本文件是总任务的一段，不是仅做本模块的范围变更。依赖以02_TASKS.json为准；当前模块没有就绪任务时查依赖卡，完成后返回。状态仅写项目绑定的唯一任务账。

## 本模块执行Prompt

读取01_EXECUTION_RULES.md和本模块当前未通过任务。沿真实入口定位具体功能，按“复现→根因→最小修改→正反例→真实集成→产物与保持检查”逐张执行。代码已具备就核实并复用，不只列清单；不要一次改本模块所有文件再统一找问题。
每项通过后接着下一项；遇依赖不足先修依赖，不把原项提前通过。模块卡全部完成也继续总队列。文档、替身、候选、真人认可分别记；日志和源配置不得泄露秘密。

## 任务索引

- M08-F01：复核真实接入与统一音色身份
- M08-F02：真实TTS结果、文案和字幕资产落地
- M08-F03：指定旁白段换音色与局部文本更新
- M08-F04：付费请求幂等、缓存与未知结果保护
- M08-F05：MiniMax音乐410诊断与能力状态

### M08-F01｜复核真实接入与统一音色身份

**归属**：M08 / G3；建议顺序 56。

**前置**：P00-02、P00-03

**代码导航**：`video-agent/lib/edit/adapters/minimax-client.mjs`；`video-agent/lib/edit/adapters/minimax.mjs`；`video-agent/lib/edit/codex-provider.mjs`；`video-agent/lib/creative/voice.mjs`；`video-agent/lib/creative/audio-assets.mjs`；`video-agent/web/commerce.js`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 复用MiniMaxClient和已成功记录；核对当前本地Key区域/权限状态，仅最小授权调用，绝不回显密钥。
2. 新建、试听、换声、状态UI都经实际供应商选择；voiceId按provider命名空间，不套本地zf/zm规则。
3. 已选历史声音和成功文件保持；配置状态与实际verified状态分离。

**正例与应观察结果**
- 现有声音入口能查真实音色并读选声，已完成片尾换声证据在匹配hash时可复用。

**反例／禁止通过的情况**
- 配置Key存在不等于live成功；目录成功不等于TTS/音乐权限；无配置不禁用本地音乐。

**最小证据**：脱敏能力状态；音色与试听路径回执；旧工程兼容。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M08-live-1、M08-local-1

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M08-F02｜真实TTS结果、文案和字幕资产落地

**归属**：M08 / G3；建议顺序 57。

**前置**：M08-F01

**代码导航**：`video-agent/lib/edit/adapters/minimax-client.mjs`；`video-agent/lib/edit/adapters/minimax.mjs`；`video-agent/lib/edit/codex-provider.mjs`；`video-agent/lib/creative/voice.mjs`；`video-agent/lib/creative/audio-assets.mjs`；`video-agent/web/commerce.js`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 按当前官方账户文档核对请求、业务码、媒体返回和字幕单位；不冻结旧模型或擅自升级。
2. 以批准文案作为内容源，保存可解码音频、真实时长、供应商/音色/脚本关系；临时URL及时落地。
3. 兼容旧speak返回契约，实际入库路径不能只存在孤立测试脚本。

**正例与应观察结果**
- 短句及包含商品术语的实际合成可解码并带真实时间；旧音频不被覆盖。

**反例／禁止通过的情况**
- HTTP200但业务失败、损坏音频、缺字幕/无时间不能伪造成功或均分时间。

**最小证据**：脱敏请求/响应摘要；实际音频hash/时长/cues；业务错误反例。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M08-live-2、M08-local-2

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M08-F03｜指定旁白段换音色与局部文本更新

**归属**：M08 / G3、G4；建议顺序 58。

**前置**：M08-F02、M09-F01

**代码导航**：`video-agent/lib/edit/adapters/minimax-client.mjs`；`video-agent/lib/edit/adapters/minimax.mjs`；`video-agent/lib/edit/codex-provider.mjs`；`video-agent/lib/creative/voice.mjs`；`video-agent/lib/creative/audio-assets.mjs`；`video-agent/web/commerce.js`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 选当前revision目标音轨，恢复该源区间的真实批准文案，不误用整条长稿。
2. 换音色/语速只生成对应段并实测；保存现有字幕样式，重排必要时间。
3. 超出允许画面窗口时处理精简或延长合同，不能截断声音、加速关键动作或发布半改版本。

**正例与应观察结果**
- 两种实际音色替换目标段，其他声音/镜头保持，字幕已改样式不回退。

**反例／禁止通过的情况**
- 取消/越窗/旧基准时新音频可保留但不错误发布；共享音源另一区间字幕不被删除。

**最小证据**：换声前后对象/音频diff；TTS实际调用记录；越窗/共享源反例。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M08-live-3、M08-local-3

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M08-F04｜付费请求幂等、缓存与未知结果保护

**归属**：M08 / G3、G4；建议顺序 59。

**前置**：M08-F02

**代码导航**：`video-agent/lib/edit/adapters/minimax-client.mjs`；`video-agent/lib/edit/adapters/minimax.mjs`；`video-agent/lib/edit/codex-provider.mjs`；`video-agent/lib/creative/voice.mjs`；`video-agent/lib/creative/audio-assets.mjs`；`video-agent/web/commerce.js`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 缓存包含非秘密账户命名空间、区域、模型、音色、文案、读音、语速、格式和字幕策略。
2. 区分明确未提交、确定失败、提交结果未知；后者只允许对账或明确新授权，不能把trace当可轮询task。
3. 同幂等重复/取消/重启保留有效输出，日志不含秘密；协议注入只替代传输边界且不算live。

**正例与应观察结果**
- 相同请求成功缓存复用；字幕样式不触发TTS；跨账户不会错误复用私有声音。

**反例／禁止通过的情况**
- 401/429/超时/业务失败/损坏文件/迟到返回分别处理；UNKNOWN不能盲目重发。

**最小证据**：协议故障全组；请求状态/重复计数；安全日志检查。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M08-local-2、M08-local-3、M08-local-4

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### M08-F05｜MiniMax音乐410诊断与能力状态

**归属**：M08 / G3；建议顺序 60。

**前置**：M08-F01

**代码导航**：`video-agent/lib/edit/adapters/minimax-client.mjs`；`video-agent/lib/edit/adapters/minimax.mjs`；`video-agent/lib/edit/codex-provider.mjs`；`video-agent/lib/creative/voice.mjs`；`video-agent/lib/creative/audio-assets.mjs`；`video-agent/web/commerce.js`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 复用已有失败证据，核对官方当前区域/路径/请求/账户支持；先排配置错误再判供应商限制，禁止反复收费探测。
2. 若账户实际支持且已授权，生成真实纯伴奏并落地；若确有外部限制，记录其范围/复核依据/后续动作，保留未完成能力。
3. 把生成音乐与使用已有本地音乐分开；允许使用用户批准本地音乐保证业务可用，不自动切换外部供应商。

**正例与应观察结果**
- 客户端支持路径和错误映射可验证；live成功需真实音频；不可用时UI及回执准确且本地混音仍能工作。

**反例／禁止通过的情况**
- 本地音乐不得标MiniMax生成；单次410不得推断全部地区关闭；不能偷偷缩减原验收。

**最小证据**：音乐能力诊断；实际结果或外部阻塞凭据（脱敏）；本地替代来源。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：该审查增量；不替代其他旧要求。

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。
