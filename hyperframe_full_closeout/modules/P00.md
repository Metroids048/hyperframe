# P00｜安全、基线与现有验收器接入

本文件是总任务的一段，不是仅做本模块的范围变更。依赖以02_TASKS.json为准；当前模块没有就绪任务时查依赖卡，完成后返回。状态仅写项目绑定的唯一任务账。

## 本模块执行Prompt

读取01_EXECUTION_RULES.md和本模块当前未通过任务。沿真实入口定位具体功能，按“复现→根因→最小修改→正反例→真实集成→产物与保持检查”逐张执行。代码已具备就核实并复用，不只列清单；不要一次改本模块所有文件再统一找问题。
每项通过后接着下一项；遇依赖不足先修依赖，不把原项提前通过。模块卡全部完成也继续总队列。文档、替身、候选、真人认可分别记；日志和源配置不得泄露秘密。

## 任务索引

- P00-01：绑定唯一仓库、当前源码和真实运行服务
- P00-02：排查私密配置跟踪并修安全边界
- P00-03：修当前旁白重测、恢复与音色CI失败
- P00-04：将本任务队列接到既有状态与验收器
- P00-05：分块恢复与干净目录依赖验证

### P00-01｜绑定唯一仓库、当前源码和真实运行服务

**归属**：P00 / G1、G2、G3、G4；建议顺序 1。

**前置**：无；先核对当前工作区。

**代码导航**：`AGENTS.md`；`video-agent/AGENTS.md`；`video-agent/.gitignore`；`video-agent/scripts/workspace-content.py`；`.github/workflows/commerce-native.yml`；`.github/workflows/conversation-checks.yml`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 核对origin、当前HEAD、工作树差异；将审查快照与本地实际版本逐文件对照，不以旧SHA覆盖新修改。
2. 核对服务进程的工作目录、端口、运行构建、数据目录、活动job；记录S02/MiniMax母工程与参考米家各自真实revision及哈希。
3. 只对本轮涉及的文件建立定位表；未找到路径先搜当前实现，不新建平行模块。

**正例与应观察结果**
- 定位现有WebUI正在使用的构建和数据；基线媒体、工程、历史清单可核查。

**反例／禁止通过的情况**
- 切换到错误origin或错误服务工作目录应阻断写入；活动任务不得被重启打断。

**最小证据**：baseline.json：repo/head/dirty-diff摘要/运行构建/活动任务/资产与工程清单（无密钥）。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F1-07

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### P00-02｜排查私密配置跟踪并修安全边界

**归属**：P00 / G3；建议顺序 2。

**前置**：P00-01

**代码导航**：`AGENTS.md`；`video-agent/AGENTS.md`；`video-agent/.gitignore`；`video-agent/scripts/workspace-content.py`；`.github/workflows/commerce-native.yml`；`.github/workflows/conversation-checks.yml`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 仅在本地检查审查点名的minimax.local.env和ignore例外；输出路径、风险类型与是否含疑似凭据，不输出值或可还原摘要。
2. 保留本机有效私密文件，修Git跟踪、同步和归档排除；只提交无秘密example。确认含真实密钥时立即提示撤销/轮换，在授权内执行；历史重写/强推另行确认。
3. 扫描拟提交差异及工作区快照/包的秘密边界，使用合成秘密canary验证拒收；不复制真实秘密到测试报告。

**正例与应观察结果**
- 新提交和新包不含私密配置或秘密；原本机配置仍可用；已暴露项的处置状态明确。

**反例／禁止通过的情况**
- 合成secret进入归档、日志、前端或例外目录时扫描失败；删除工作树文件不能当移除Git跟踪。

**最小证据**：security-check.json（无秘密）；配置canary回归与提交清单。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：M08-local-4

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### P00-03｜修当前旁白重测、恢复与音色CI失败

**归属**：P00 / G3、G4；建议顺序 3。

**前置**：P00-01

**代码导航**：`AGENTS.md`；`video-agent/AGENTS.md`；`video-agent/.gitignore`；`video-agent/scripts/workspace-content.py`；`.github/workflows/commerce-native.yml`；`.github/workflows/conversation-checks.yml`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 在隔离配置重跑审查列出的test-commerce-next中两项及失败套件；分别记录真实异常链，禁止加载生产Key。
2. 核对新音色目录、注入provider能力、旁白重测、状态指纹和步骤预算；修真正不一致，保留原先要验证的中断恢复语义。
3. 复跑原失败组及受影响套件；确认正例到达原预期检查点，恢复不重复TTS；另保留预算耗尽和非法音色的独立反例。

**正例与应观察结果**
- 原两项测试验证旁白真实测量/恢复契约，不仅换断言；实际结束码通过。

**反例／禁止通过的情况**
- 未知音色仍被拒；预算耗尽仍安全暂停；不能删测试、关闭目录校验或单纯扩预算。

**最小证据**：failure-before.log；root-cause.md（短）；完整after-suite.log。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：F3-08

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### P00-04｜将本任务队列接到既有状态与验收器

**归属**：P00 / G1、G2、G3、G4；建议顺序 4。

**前置**：P00-01

**代码导航**：`AGENTS.md`；`video-agent/AGENTS.md`；`video-agent/.gitignore`；`video-agent/scripts/workspace-content.py`；`.github/workflows/commerce-native.yml`；`.github/workflows/conversation-checks.yml`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 核对已生效的项目指令及现有执行状态、验收脚本；把本包任务ID映射进去，禁止再建监督平台或无限循环。
2. 采用本文证据契约，给已有状态扩少量必需字段；单一权威状态保存，EXECUTION_STATUS等只作投影视图。
3. 验证缺报告、旧revision、skip、失效哈希、伪造pass不能计过；只读查询不得生成项目/音频；未决项继续保留。

**正例与应观察结果**
- 从现有执行入口查出当前任务、已验证、待集成、受阻和下一就绪项；所有旧ID已映射。

**反例／禁止通过的情况**
- 删除任务或只改status=passed但无产物应失败；正常阶段记录不导致全局提前完成。

**最小证据**：existing-state-binding.json；验收器反例日志；当前队列只读投影。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：该审查增量；不替代其他旧要求。

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。

---

### P00-05｜分块恢复与干净目录依赖验证

**归属**：P00 / G1、G4；建议顺序 5。

**前置**：P00-01、P00-02

**代码导航**：`AGENTS.md`；`video-agent/AGENTS.md`；`video-agent/.gitignore`；`video-agent/scripts/workspace-content.py`；`.github/workflows/commerce-native.yml`；`.github/workflows/conversation-checks.yml`。实际不存在则定位现有等价实现，不能为文件名另建架构。

**实现动作**
1. 核验workspace-content manifest与objects及恢复脚本；依清单验证可恢复性，不用上传批次数字推测完成。
2. 在独立目录只补缺失文件，核对哈希；使用锁文件恢复依赖，记录本地运行版本，不盲升级HyperFrames。
3. 在隔离副本去一块、损坏一块、放置同路径已修改文件；分别验证可诊断、不覆盖用户改动；已含的工程整组保持。

**正例与应观察结果**
- 选定工作工程及必要素材可独立恢复并启动；全量manifest的完整性有实际检查结果。

**反例／禁止通过的情况**
- 缺块/坏块/部分上传不能报告恢复完整；不得删除原目录证明隔离。

**最小证据**：snapshot-restore-check.json；独立目录启动/解码日志；坏块和冲突记录。同时满足01_EXECUTION_RULES第5节通用回执字段。

**旧要求映射**：该审查增量；不替代其他旧要求。

**完成后**：验证本卡证据并更新唯一任务状态，立即选择下一依赖就绪卡；并非在此结束全部任务。
