# HyperFrames 项目架构说明

> 这是一份面向产品、业务和协作人员的简版说明。它描述当前系统怎么工作，不展开代码实现。
>
> 当前运行基线：HyperFrames `0.8.33`。项目工作树存在未提交改动，以下内容以本地实际行为为准。

## 先用一句话理解

用户提供需求和真实素材，系统先理解“要做什么”，再决定“怎么讲、怎么剪”，最后交给 HyperFrames 生成可预览、可修改、可导出的视频。

系统有三个主要角色：

| 角色 | 负责什么 |
|---|---|
| **video-agent** | 理解需求、识别场景、分析素材、规划分镜、管理项目和版本 |
| **HyperFrames** | 把分镜和素材变成画面、字幕、动效、声音和视频文件 |
| **OpenClaw** | 提供另一种对话入口，并控制任务权限、进度和结果返回 |

最重要的分工是：**Agent 决定内容和叙事，HyperFrames 负责呈现和渲染，服务端负责权限、版本和交付判断。**

## 项目不是一条脚本，而是五层协作

为了理解各个文件为什么存在，可以把项目分成五层：

| 层次 | 主要问题 | 对应内容 |
|---|---|---|
| 用户入口 | 用户从哪里提出需求、上传素材和查看结果 | Web 工作台、`server.mjs`、OpenClaw 对话入口 |
| 理解与编排 | 用户到底想做什么，应该走哪条流程 | 需求准备、意图识别、路由和生产服务 |
| 内容与素材 | 商品事实是什么，哪些素材能用，镜头如何安排 | 素材观察、商品理解、导演和场景规则 |
| 画面与渲染 | 如何把镜头变成字幕、动效、声音和视频 | HyperFrames 适配层与运行时 |
| 项目与交付 | 如何保存版本、反馈、证据并判断能否交付 | 项目存储、版本历史、质量检查和交付门禁 |

所以用户看到的是一个视频工作台，系统内部实际维护的是一组相互关联的对象：任务目标、商品事实、素材证据、分镜、可编辑工程、导出文件和审查结果。MP4 只是其中一个输出，不是全部项目状态。

## 两条主要工作流

### 新建电商视频

从商品需求和真实素材开始，适合单品上新、商品介绍、卖点展示、使用演示、促销和 FAQ。系统会先判断素材是否真的能支撑目标，例如教程必须有关键动作证据，不能只靠字幕“写出”没有拍到的步骤。

### 已有视频对话式精剪

从一条已有视频或已有工程开始，用户可以要求删减片段、调整节奏、修改字幕、改变声音或重新导出。这条流程更强调时间轴和局部修改：只改字幕时，应尽量保留画面、素材、声音和其他没有被要求改变的内容。

两条工作流共享项目、版本和 HyperFrames 能力，但新建视频更依赖场景和分镜规划，已有视频精剪更依赖时间轴、对象定位和撤销恢复。

## 核心模块与代表文件

下面只列最值得先看的文件，完整路径清单另见 [`PROJECT_CODE_INVENTORY.md`](./PROJECT_CODE_INVENTORY.md)。

| 模块 | 主要作用 | 代表文件或目录 |
|---|---|---|
| 入口与项目管理 | 接收请求、创建项目、保存任务和版本 | `video-agent/server.mjs`、`video-agent/lib/creative/service.mjs` |
| 需求与生产编排 | 把需求变成制作计划，并推动各阶段执行 | `video-agent/lib/creative/commerce-preparation.mjs`、`production.mjs` |
| 素材与分镜 | 观察素材、确认可用片段、安排镜头顺序 | `source-inspection.mjs`、`director.mjs`、`story-validation.mjs` |
| HyperFrames 适配 | 把分镜和素材变成可编辑工程并渲染 | `document.mjs`、`compiler.mjs`、`runner.mjs` |
| OpenClaw 控制层 | 提供对话入口、权限控制、进度和结果返回 | `lib/openclaw/`、`openclaw-plugin/index.mjs` |
| 已有视频精剪 | 对导入视频做时间轴、字幕和声音修改 | `video-agent/lib/edit/` |
| 场景与配置 | 定义电商场景规则、资源目录和制作约束 | `commerce/scenes/`、`config/`、`prompts/commerce/` |

## 一张图看懂整体结构

架构图见 [`PROJECT_ARCHITECTURE.mmd`](./PROJECT_ARCHITECTURE.mmd)。核心关系可以概括为：

```text
用户需求与素材
    ↓
理解需求、确认商品和场景
    ↓
观察素材、查找事实、确定可用内容
    ↓
规划分镜和表达方式
    ↓
选择 HyperFrames 资源并制作视频
    ↓
预览、检查、修改
    ↓
候选视频 → 人工确认 → 正式交付
```

## 从输入到成片的八个步骤

### 1. 优化输入

把一句自然语言需求整理成可执行的制作目标：商品是什么、给谁看、用于什么平台、希望多长、需要什么声音和画幅。

信息不完整时，系统会提出问题或标记缺口，不会把猜测当成商品事实。

### 2. 识别意图和场景

系统判断用户是要新做一条视频、修改已有视频、导出结果，还是恢复之前的任务。

对电商视频，还会判断更接近哪类目标，例如：

- **单品上新**：快速建立商品印象，突出可信卖点。
- **使用演示**：让第一次使用的人看懂步骤，并能照着操作。

场景不同，素材要求、叙事节奏和检查标准也不同。

### 3. 看素材和查事实

系统会观察用户提供的图片和视频，确认商品、动作、细节和可用片段；必要时查找公开信息，帮助核对名称、规格和使用背景。

搜索结果只是参考。只有和用户素材、商品身份及使用权一致的内容，才会进入视频。

### 4. 规划表达

系统先确定每个镜头要传达什么，再安排素材顺序、字幕、声音、转场和结尾。

这一步的重点不是堆效果，而是让观众按正确顺序获得有效信息。使用演示尤其要保留关键动作，不能用字幕代替没有拍到的步骤。

### 5. 调用 HyperFrames 资源

系统根据镜头目的选择合适的版式、字幕样式、动效和转场，再把它们绑定到具体镜头。

资源选择会记录来源和实际使用情况，因此可以知道某个效果来自哪里、用在了哪一幕，也能在后续修改时保持一致。

### 6. 制作候选视频

素材、字幕、声音和动效被组合成一个可编辑工程，再由 HyperFrames 生成预览和视频文件。

视频文件是输出结果，工程才是后续修改的基础。修改字幕、镜头顺序、时长或声音时，系统应尽量保留其他没有被要求改变的内容。

### 7. 持续检查和修复

系统会检查画面、字幕、动作、声音和事实是否符合目标；发现问题后，优先修复影响理解和业务准确性的内容，再处理节奏和视觉细节。

检查发现问题并不等于任务失败。系统会保留当前版本，支持局部修改、撤销、恢复和重新导出。

### 8. 候选和正式交付

视频先以“候选”状态提供预览，方便检查和反馈。只有当前版本、素材、质量结果、使用权和人工确认都对应一致，才进入正式交付。

“渲染成功”只代表文件生成成功，不代表视频已经通过业务审查或真人确认。

### 每一步之间传递什么

把八个步骤连起来看，中间并不是一堆聊天记录，而是逐步形成的项目材料：

| 阶段 | 主要留下的内容 | 它解决什么问题 |
|---|---|---|
| 需求准备 | 任务目标、商品身份、输出要求和待确认信息 | 明确这条视频要为谁解决什么问题 |
| 场景判断 | 新建/精剪模式和适用场景 | 决定后面使用哪套规则和检查标准 |
| 素材观察 | 素材清单、可用片段、动作和声音证据 | 确认“素材里实际有什么” |
| 内容策划 | 商品理解、卖点顺序和表达策略 | 决定观众应该先看到和理解什么 |
| 分镜规划 | 镜头目的、素材区间、字幕、声音和保护区域 | 把创意变成可以执行的镜头安排 |
| 工程制作 | 可编辑工程、资源绑定和时间线 | 让画面可以预览、修改和渲染 |
| 质量修复 | 当前版本的问题、反馈和修复记录 | 保证新版本没有悄悄破坏旧内容 |
| 交付判断 | 候选/正式状态、文件和证据绑定 | 判断结果是否真的可以交付 |

这张表是理解项目的关键：每一层都为下一层提供依据，也都留下可追溯的结果，所以后续修改不必从一段自然语言重新开始。

## HyperFrames 在项目中的位置

HyperFrames 是视频制作和渲染底座，主要负责：

- 组织图片、视频、字幕、图形和声音；
- 控制时间、动画和镜头衔接；
- 提供预览、检查和最终导出；
- 保留可继续编辑的原生工程。

项目并没有把 HyperFrames 改造成另一套视频引擎。主要工作是在它上面增加商品场景、资源选择、素材证据、版本管理和交付门禁，让通用能力适合真实的电商视频流程。

当前应用使用 `0.8.33`；仓库里的其他 HyperFrames 版本属于参考材料，不能直接视为当前运行版本。

### HyperFrames 资源如何被调度

项目不会先随意套一个模板，再让内容去适应模板。资源使用通常经过四步：

1. **发现**：从官方参考、项目本地资源、场景包和已有组合中建立可用目录；
2. **提出需求**：把“突出局部”“引导步骤”“强调卖点”等镜头意图转换成资源条件；
3. **筛选并绑定**：结合场景、素材类型、画面空间和运行版本，选择资源并绑定到具体镜头；
4. **记录回执**：保存资源来源、版本、使用镜头和实际渲染结果，方便复查和后续修改。

因此，“搜到一个资源名称”不等于“视频使用了这个资源”。资源必须真正进入工程并参与当前版本的编译和渲染，才会成为成片的一部分。

## OpenClaw 在项目中的位置

OpenClaw 是外部对话入口和控制层。它可以接收用户请求、调用视频任务、查询进度和读取结果，但真正的项目创建、素材写入、任务执行和交付判断仍由 video-agent 服务端完成。

这样做有三个好处：

1. 外部 Agent 不能随意写入本机文件或绕过项目权限。
2. 同一个项目可以从 Web 工作台或 OpenClaw 继续修改。
3. 任务状态、版本和失败原因来自真实项目，而不是对话中的口头描述。

## 项目保存的核心内容

读者不需要记住所有文件名，但可以用下面几类内容理解项目为什么能够继续修改：

| 内容 | 说明 |
|---|---|
| 任务合同 | 场景、目标、商品身份、已知事实、未知信息、输出要求和禁止项 |
| 素材证据 | 来源、文件、可用区间、商品核对、动作和声音证据 |
| 分镜与工程 | 镜头目的、素材绑定、字幕、声音、时间和可编辑对象 |
| 资源回执 | 实际使用的 HyperFrames 资源、版本和镜头位置 |
| 运行与版本 | 任务进度、检查点、失败原因、当前版本和历史版本 |
| 质量与交付 | 技术检查、问题修复、人工确认和候选/正式状态 |

常见的对应文件包括 `native-project.json`、`business-contract.json`、`document.json`、`production-run.json`、`hyperframes-resource-receipt.json`、`quality-report.json` 和最终的 `commerce-final.mp4`。这些文件共同描述一个项目，不能只看 MP4 判断任务是否完成。

## 三道质量门

| 阶段 | 要回答的问题 | 结果 |
|---|---|---|
| **制作准入** | 素材、商品事实、使用权和目标是否足够明确？ | 可以制作，或先补充信息 |
| **候选检查** | 当前导出的视频是否能看、能听、能理解？ | 候选、需要修改或等待人工确认 |
| **正式交付** | 当前视频和所有证据是否属于同一个版本，并得到必要确认？ | 正式交付或阻止交付 |

这三道门把“能生成文件”和“可以交付给用户”区分开，避免用技术成功掩盖事实错误、动作缺失或审查不足。

## 需要深入时再看

- [架构图源文件](./PROJECT_ARCHITECTURE.mmd)：查看组件之间的关系。
- [代码与配置清单](./PROJECT_CODE_INVENTORY.md)：按目录查找具体文件，已排除依赖、缓存、运行数据和交付产物。
- [video-agent 执行规则](./video-agent/AGENTS.md)：了解制作、恢复和验收边界。
- [HyperFrames 适配策略](./video-agent/DIRECTOR_TO_HYPERFRAMES_STRATEGY.md)：了解分镜如何落到具体表现。
