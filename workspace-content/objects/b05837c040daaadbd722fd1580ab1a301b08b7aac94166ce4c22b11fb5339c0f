◆  Capturing 11 frames at [1.8s, 5s, 11.8s, 18s, 26s, 35s, 43s, 50s, 56s, 62s, 68.5s] from v2
[StaticGuard] Invalid HyperFrame contract: <video> with data-start is nested inside <section id="s0"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s1"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s2"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s2"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s2"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s3"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s3"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s5"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s6"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s7"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s8"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.; <video> with data-start is nested inside <section id="s10"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
[INFO] [Compiler] Fetched 11 font face(s) for "Inter" from Google Fonts (cached to C:\Users\admin\.cache\hyperframes\fonts\inter)
[INFO] [Compiler] Injected deterministic @font-face rules for 1 requested font families
[hyperframes] browserGpuMode probe → hardware (WebGL renderer vendor="Google Inc. (Intel)" renderer="ANGLE (Intel, Intel(R) UHD Graphics (0x0000A721) Direct3D11 vs_5_0 ps_5_0, D3D11)")

   Fonts: 3 loaded, 3 unused
   Note: added an end-of-timeline frame at 69.84s. Short beats between your --at times may still be skipped — pass them explicitly.

◇  12 snapshots saved to C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\frames
   frames\frame-00-at-1.8s.png
   frames\frame-01-at-5s.png
   frames\frame-02-at-11.8s.png
   frames\frame-03-at-18s.png
   frames\frame-04-at-26s.png
   frames\frame-05-at-35s.png
   frames\frame-06-at-43s.png
   frames\frame-07-at-50s.png
   frames\frame-08-at-56s.png
   frames\frame-09-at-62s.png
   frames\frame-10-at-68.5s.png
   frames\frame-11-at-69.84s.png
   contact-sheet-1..2.jpg (grid view for AI review)
