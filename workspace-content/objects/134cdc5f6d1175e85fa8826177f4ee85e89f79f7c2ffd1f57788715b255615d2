# R1 收口：一个商品，从真实生成到可连续修改的成片

基线仓库：Metroids048/hyperframe；审查提交：46c6ff6066fba063f18b0fd057180eece65994fc。
本文件是下一轮执行合同，不是完成报告。以当前本地源码和未提交改动为准；若 HEAD 已前进，先核对差异，保留已修复项并复测，不强制退回审查提交。

## 1. 这轮只交什么

在现有 WebUI 中，以一款具有合法、清晰商品原图和可核对资料的商品，完成：

**商品输入 → Agent 确定镜头计划 → RunningHub 生成图片/原始镜头 → 本地资产登记 → HyperFrames 精确路由与编排 → 30秒竖屏候选MP4 → 同一工程连续修改三次 → 保存重开与导出。**

默认验收规格1080×1920、30fps、30秒。画面结构含商品亮相、可依据卖点/细节、场景展示与结尾。可规划约4—5段有效原始镜头并留裁切余量，实际数量依时长、模型能力和内容确定，不在运行时写死角色数组或循环填时长。生成关键帧与镜头先检查，不用转场遮盖商品变形。

保留图片、原始视频、营销成片三种目标以及场景下拉，已有工程继续聊天默认修改。单独图/视频路径用本轮素材与真实记录验证；不再增加供应商、另造Agent框架、重做首页、另开六个Demo或再建监督平台。

第二业务场景保留和回归，但第二条全新教程、复杂人手UGC、全量Catalog每项逐帧验收与完整Pippit竞品复刻不在这轮新增样片范围。它们仍记未完成/后续，不准变成已验收。缩小样本数量，不取消四项产品目标。

## 2. 冻结可以复用的部分

复用现有 composer、native document、patch、revision、undo/redo、history package、渲染队列、Agent阶段、CapabilityCatalog、全量发现目录和RunningHub provider。已通过当前本地复测的部分直接封存。

不要 reset、clean、覆盖未提交工作或清空素材/任务。先检查 live jobs，不中断正在取得进展的付费任务。不要把参考成片或预置revision作为本轮新生成输出。修复用最小项目侧改动，不为了本片大改上游核心。

## 3. 依次修五个收口项；中间不等待“继续”

### R1-A：干净检出可用，先清除当前CI根因

检查当前Git索引里的 third_party/hyperframes、third_party/hyperframes-launches、.gitmodules、项目内 hyperframes 源目录以及上游锁文件。

两条最新CI的问题已见 evidence/CI_FINDINGS.md：一条在checkout清理认证时遇到缺失submodule URL；另一条在import chromatic-split.mjs时找不到官方HTML。

选择一种与当前项目一致的依赖交付方式：完整且固定提交的submodule，或可复现的锁定源码/资源分发。统一真正使用的资源根，不保留多个无说明、版本不一致的候选路径。

保留用户已有下载文件。不要靠打开persist-credentials、跳过测试、把不存在资源标成可用来隐藏故障。LFS指针/实体警告审查实际影响，不擅自重写历史。

必要效果资源由构建或明确的能力加载阶段校验；未使用某效果时，其缺失不应在顶层import崩掉所有任务。用户明确要求该效果时，资源缺失则准确阻断并修复，不偷偷换成普通淡化。

在独立临时检出目录验证必要依赖、构建、已有回归；本机可用环境按 start.py 和实际配置解析，不再次停在裸 node 找不到。禁止更改全局安全策略或删除用户目录。

**通过条件：干净环境不再出现已知checkout/import错误，相关回归真实执行；最终推送时两条CI必须重跑，未有权限推送则提供实际本地等价结果和未验证远端状态，不伪造CI通过。**

### R1-B：生成由目标和镜头缺口决定，恢复同一组镜头

修改 service.mjs 的create链路：先形成可持久化的镜头计划，再按每个shot的缺口调用生成。用户选marketing并说“基于这张商品图，做一条30秒新品广告”，不需要补写“生成视频”关键词。

有现成视频也只说明部分素材已具备，不能代表全部镜头已齐。逐镜头保留输入、用途、构图、时长、画幅、后期留白、派生prompt、sourceAssetId、生成状态和验收结果。原始prompt不变。

复用现有父任务、provider记录和检查点，不建第二套竞争状态。已完成镜头照原sha复用，失败/未开始镜头继续；第一段成功后中断，恢复必须继续剩余镜头，而不是因为已存在video而绕过生成。

将生成阶段纳入任务恢复，不以Agent runId是否已创建作为唯一可恢复条件。重启后扫描已提交provider任务并继续查询；状态未知先对账，不重新付款。取消意图必须持久化，用户取消不能被自动恢复。

RunningHub调用身份按实际请求语义形成，包含输出画幅、模型/工作流版本、输入内容hash、时长、seed及实际参数；不包含密钥。1:1须正确映射或在提交前明确报不支持，不能静默发9:16。

同请求在锁内重新读取持久化记录；预算预留和provider提交互斥、可恢复。复用现有锁/事务设施，至少保护同任务并发恢复，跨进程部署有明确单worker/租约限制。不要将“文件数量”无条件当真实尝试次数。补并发与失败注入回归。

付费只在已有有效授权内进行。安全读取本地忽略配置；没有推到GitHub不代表没有Key。不打印密钥，不新设金额消耗余额。缺Key或授权只暂停新增付费分支，继续其余开发、无付费回归及已有素材剪辑验证。

**通过条件：普通营销需求实际调用所需生成；部分失败恢复不丢镜头；同请求不重复提交；画幅与缓存一致；真实输出下载、解码、来源与费用状态可追溯。**

### R1-C：精确路由不假成功，本片效果真正生效

保留全量目录；修通用canonicalId/显示名/已索引URL/别名精确解析，而不是再为两个名称写if。明确指定的资源不能被普通候选重排替换；无兼容执行器返回具体未适配/未解决，不允许resolved+空列表。

正确识别中文/英文否定、作用范围、镜头位置和“其他保持”。例如“不要全片用色散，只在第一个转场用一次”，不能整句误判为禁用，也不能全片启用。

本轮只对本片真正依赖的约5—6项能力做深度绑定/渲染复验：商品展示结构、细节放大/标注、字幕文字、结尾、常规转场以及Chromatic Radial Split。是否复用已有适配器以现场实现为准，不为凑数量新增效果。

Chromatic Radial Split按真实官方实现和本地兼容接口作用于前后商品镜头，去除SCENE A/B等样例界面。验证转场前/中/后、视频推进、前后seek以及相同时间重复seek，输出不得只拍到静止纹理或丢失主体。

记录请求到canonical/runtime/参数/场景/对象/资产/资源哈希及产物的关联。不要把“读取了Skill说明”写成“效果执行成功”。目录总量、匹配覆盖、适配覆盖和真实渲染覆盖分别报告。

**通过条件：本轮作用范围和指定效果符合原话；未匹配请求可见，不被静默改成其他效果；当前成片有真实资源调用和画面证据。**

### R1-D：真实状态与结果卡，打通用户最后一步

修 `/api/commerce-execution-status`：从现有task/shot/revision/export状态推导；区分idle、running、blocked、failed、review_ready；不能固定RUNNING/blocker:null。recentProgressAt仅在实际阶段/产物变化时更新，另设检查时间；项目创建数不能当成片完成数，删除或改造旧六场景/固定8任务占位统计。

在当前生成结果卡上补明确资产选择及“做成视频/用于成片”动作，传assetId，不默认总取第一张原图。保留现有页面布局，不重建资产平台。

营销成片任务发布原生工程后，自动走现有固定revision的候选导出步骤；候选MP4可下载且基础媒体校验通过后才将本次制作标完成。不得把人工accepted作为自动结束条件；保留正式交付人工门禁。已有编辑后的手动导出行为可以保持。

真实浏览器完成上传、模式/场景/画幅选择、提交、进度、结果、修改和下载。刷新不会丢失当前任务，生成暂时失败不会清空工程或草稿。

**通过条件：普通用户从同一入口得到本次结果；无后台任务显示idle，有错误显示实际阻断；只刷新页面不伪造进展；预览就绪与MP4完成明确区分。**

### R1-E：交出一条达到目标的片，而不是更多报告

从本地官方Examples中选一项实际可读取/观看的参考，冻结来源、版本与具体设计特点，不只写“参考官网”。沿用可用品牌规范，制作静态关键布局后再做动效。强调主体、留白、字号、叙事、节奏和声音，不堆滤镜掩盖坏素材。

先技术检查，再看实际MP4。至少检查每幕最充分展示时刻、转场中间帧、字幕变化、商品一致性、首尾、完整解码与声音。工具不能完整观片/审听就注明覆盖限制，不伪造人评；仍交可供用户检查的候选，不把等待主观确认变成继续停工借口。

输出同素材基础版与精剪版：相同素材池、商品事实、字幕文字、声音和输出规格，基础版也要正常完整，不能故意做差。

同一工程连续三次用户级指令：
1. “字幕精简并移到不挡商品的位置，声音和镜头不变。”显示文本编辑不能自动改旁白；零生图/生视频/TTS。
2. “细节镜头提前，只在第一个转场使用色散，其他内容保持。”零素材生成；检查时间和作用范围。
3. “最后一句改为这段已确认文案，只改画面文字。”零素材生成/配音；支持撤销、恢复，保存重开并再次导出。

若另需验证单坏镜头重做，用隔离故障测试或已授权的真实局部重做补充，不为演示故意多消费。

**通过条件：当前UI可播放/下载30秒候选、原生工程与三次新版本；无错商品、黑帧、遮挡、错误声音或明显重复凑时长；保留基于实际画面的修复前后证据。自评分不替代用户最终认可。**

## 4. 只维护12项收口检查

|编号|验收|
|---|---|
|C01|依赖固定且干净检出可恢复；两条CI或有明确范围的本地等价检查|
|C02|普通营销用语与image/video模式走正确路径，输入框和下拉接真实请求|
|C03|1:1、9:16、16:9参数和缓存身份正确，不支持时提交前说明|
|C04|同请求并发/重复点击/恢复不会重复提交；未知提交先对账|
|C05|第一镜头成功后故障/重启，原taskId查询、缺失镜头续作|
|C06|进度来源真实；idle/错误/完成不伪装；刷新不刷新实际进展时间|
|C07|确切名称、自然语言、否定、范围、无匹配不假成功|
|C08|指定shader与本片资源真实绑定，前中后及seek检查|
|C09|RunningHub真实图片/视频结果有效、来源和成本状态准确|
|C10|现有WebUI端到端产生候选MP4而非只有预览或taskId|
|C11|三次续改、零多余生成、撤销/重开/工程再导出|
|C12|同素材基础/精剪对照、当前revision技术和视觉检查证据|

测试复用现有框架。`evidence/probes`中的测试是旧缺陷复现，不是正确行为验收；实现修复后应在项目测试中断言正确行为，不能改为期望旧Bug还在。

## 5. 执行、停止与最终交付

每完成一项保存进展、版本、真实命令结果和下一动作，自动进入下一项。内部代码、映射、依赖、样式问题自行诊断修复；重复相同失败两次后改变诊断方式，不无限重复也不自动结束全部任务。

当付费分支等待配置时，继续C01/C03—C08及已有素材的浏览器和编辑验证；这些必须标明非本轮真实生成，不顶替C09/C10最终证据。无需每阶段向用户要“继续”。

不新建后台监督平台。使用现有任务恢复和开发检查点；没有实际进程/运行记录，不承诺对话断开后后台继续改代码。不能凭提示词保证断电或上游容量问题不发生。

最终只交：真实WebUI入口、主片与同素材对照、三次修改版及原生工程、12项检查真实状态、CI结果、生成与资源来源、费用/耗时及未通过项。所有链接先核实存在。密钥和字体文件不进入交付包。

本轮通过的准确名称是“单品上新生成—剪辑闭环可验收”，不是“所有电商场景/所有官方资源全部验收”。剩余范围单列，不能隐藏。
