◆  Checking v2

Lint
  ⚠ duplicate_media_discovery_risk: Detected 3 matching video entries with the same source/start/duration.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html [data-composition-id] t=0s
    Fix: Avoid duplicated media nodes that can be discovered twice during compilation.
  ⚠ duplicate_media_discovery_risk: Detected 2 matching video entries with the same source/start/duration.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html [data-composition-id] t=0s
    Fix: Avoid duplicated media nodes that can be discovered twice during compilation.
  ⚠ duplicate_media_discovery_risk: Detected 2 matching img entries with the same source/start/duration.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html [data-composition-id] t=0s
    Fix: Avoid duplicated media nodes that can be discovered twice during compilation.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s0"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #v0 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s1"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #v1 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s2"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #u0 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s2"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #u1 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s2"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #u2 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s3"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #lens0 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s3"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #lens1 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s5"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #base0 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s6"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #screen0 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s7"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #life0 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s8"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #life1 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  ✗ video_nested_in_timed_element: <video> with data-start is nested inside <section id="s10"> which also has data-start. The frame extractor resolves the video's start from its own data-start without the wrapper's offset, while visibility uses the wrapper's window, so the two disagree: the clip shows the wrong source frames and then disappears partway through its slot.
    C:\Users\admin\Desktop\hyperframe-main\video-agent\outputs\mijia-brand-test\v2\index.html #end0 t=0s
    Fix: Time the wrapper OR the video, never both: remove data-start from the wrapper (use it as a non-timed visual container), or move the <video> up to be a direct child of the stage.
  12 error(s), 3 warning(s), 0 info(s)

Runtime
  ◇ 0 errors, 0 warnings

Layout
  ◇ 0 issues across 0 sample(s)

Motion
  ◇ 0 errors, 0 warnings

Contrast
  ◇ 0/0 text checks pass WCAG AA

Snapshots
  ◇ disabled

◇  Check failed
