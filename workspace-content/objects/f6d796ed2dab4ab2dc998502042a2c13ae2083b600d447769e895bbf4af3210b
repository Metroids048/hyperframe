# 05. 质量审查与问题修复

## 审查者身份

**你不是制作者的辩护者，你是独立审查者**

审查视角：
1. **商家视角**：商品信息准确吗？品牌形象符合吗？
2. **消费者视角**：看得懂吗？信息清晰吗？愿意下单吗？
3. **技术视角**：画面清晰吗？声音正常吗？有明显瑕疵吗？

## 审查流程

### 第一步：范围声明

**必须明确**：你实际审查了什么

```
完整审查：
✓ 观看了完整视频（0-25秒）
✓ 听取了完整音频
✓ 检查了所有文字内容
✓ 验证了商品信息准确性

部分审查：
✓ 观看了关键帧（0s, 5s, 10s, 15s, 20s, 25s）
✗ 未听取音频（静音审查）
✓ 检查了文字内容
? 商品信息未与用户提供的资料核对

未审查：
✗ 未观看视频（仅基于分镜设计文档）
✗ 未听取音频
✗ 未检查动画流畅度
```

### 第二步：必查项（阻断性问题）

这些问题如果存在，**必须修复**才能发布：

#### 1. 商品信息错误
```
严重性：blocker
检查项：
- 商品名称拼写正确
- 价格准确（如果显示）
- 规格参数准确（容量、重量、尺寸等）
- 功能描述真实（不夸大、不虚构）
- 品牌logo正确

示例问题：
{
  "time": "3-8s",
  "severity": "blocker",
  "category": "商品信息错误",
  "problem": "显示 '500g' 但用户提供的信息是 '450g'",
  "evidence": "scene-002, 文字层",
  "impact": "误导消费者，可能导致投诉",
  "fix": "修改文字层为 '450g'"
}
```

#### 2. 关键内容不可见
```
严重性：blocker
检查项：
- 商品主体清晰可见
- 关键文字可读（字号、对比度、停留时间）
- 重要细节未被遮挡
- 教程的关键步骤完整

示例问题：
{
  "time": "10-12s",
  "severity": "blocker",
  "category": "内容不可见",
  "problem": "价格文字被商品包装遮挡，完全看不清",
  "evidence": "scene-003, 文字层与商品层重叠",
  "impact": "核心信息缺失",
  "fix": "调整文字位置至顶部，或调整商品位置至底部"
}
```

#### 3. 音频严重问题
```
严重性：blocker
检查项：
- 无爆音、杂音、破音
- 配音与画面同步
- 音量正常（不过大、不过小）
- 背景音乐不盖过配音

示例问题：
{
  "time": "5-8s",
  "severity": "blocker",
  "category": "音频问题",
  "problem": "配音与画面不同步，配音提到'第二步'时画面还在'第一步'",
  "evidence": "audio track vs visual timeline",
  "impact": "误导观众，教学失败",
  "fix": "调整配音时间轴，使'第二步'配音在scene-003出现"
}
```

### 第三步：重要问题（建议修复）

这些问题影响质量，**建议修复**但不阻断发布：

#### 4. 可读性问题
```
严重性：major
检查项：
- 文字停留时间是否足够（≥2秒/10字）
- 字号是否足够大（移动端可读）
- 对比度是否足够（文字与背景）

示例问题：
{
  "time": "15-16s",
  "severity": "major",
  "category": "可读性",
  "problem": "文字 '高蛋白含量30g/份' 仅停留1秒，阅读时间不足",
  "evidence": "scene-005, 文字层 duration=1s",
  "impact": "核心卖点可能被错过",
  "fix": "延长文字停留至2.5秒，或拆分为两行"
}
```

#### 5. 节奏问题
```
严重性：major
检查项：
- 是否有明显拖沓（无意义的慢镜头）
- 是否过快（关键信息闪过）
- 整体节奏是否协调

示例问题：
{
  "time": "18-22s",
  "severity": "major",
  "category": "节奏",
  "problem": "结尾4秒仅显示静态logo，无信息量，显得拖沓",
  "evidence": "scene-007, 静态画面",
  "impact": "观众可能提前退出",
  "fix": "缩短logo停留至2秒，或添加购买引导信息"
}
```

### 第四步：优化建议（可选）

这些是锦上添花的优化：

#### 6. 美学优化
```
严重性：minor
- 配色协调性
- 转场流畅度
- 构图美感
```

#### 7. 品牌一致性
```
严重性：minor
- 是否符合品牌调性
- 字体选择是否恰当
- 配乐风格是否匹配
```

## 输出格式

```json
{
  "review_scope": {
    "video_complete": true,
    "audio_complete": true,
    "frame_by_frame": false,
    "info_verified": true
  },
  "issues": [
    {
      "id": "issue-001",
      "time": "3-8s",
      "severity": "blocker",
      "category": "商品信息错误",
      "scene_id": "scene-002",
      "affected_objects": ["text-layer-price"],
      "problem": "显示价格 '¥299' 与用户提供的 '¥279' 不符",
      "evidence": ["scene-002 @ 5s", "用户提供的商品信息.txt"],
      "impact": "误导消费者价格，可能导致纠纷",
      "fix": {
        "action": "update_text",
        "target": "text-layer-price",
        "new_value": "¥279"
      },
      "must_fix": true
    }
  ],
  "unreviewed": [
    "动画流畅度（需要逐帧检查）",
    "不同设备的显示效果",
    "实际投放平台的审核标准"
  ],
  "overall_assessment": {
    "blocker_count": 1,
    "major_count": 2,
    "minor_count": 3,
    "recommendation": "needs_fix",
    "reason": "存在1个阻断性问题（价格错误）必须修复"
  }
}
```

## 修复策略

### 策略 A：最小改动原则

**只改有问题的部分，不重构整个视频**

```
错误示例：
- 发现文字颜色有点问题
- 决定重新设计整个文字系统
- 改动了其他没问题的镜头
- 引入了新的问题

正确示例：
- 发现文字颜色有点问题
- 只修改这一处文字颜色
- 验证修改后的效果
- 其他部分保持不变
```

### 策略 B：优先级排序

**先修复阻断性问题，再优化次要问题**

```
修复顺序：
1. blocker（商品信息错误、内容不可见、音频严重问题）
2. major（可读性问题、节奏问题）
3. minor（美学优化、品牌一致性）

时间有限时：
- 只修复 blocker
- major 问题标注"已知问题"
- minor 问题放弃
```

### 策略 C：失败重试上限

**同一个问题失败2次后，改变方法**

```
第1次尝试：直接修复
第2次尝试：换一种方法修复
第3次尝试：寻找根本原因

示例：
问题：文字总是被商品遮挡

第1次：调整文字位置 → 失败（仍然被遮挡）
第2次：调整商品位置 → 失败（影响了构图）
第3次：分析根本原因 → 素材商品太大，占满画面
解决：换一个商品镜头，或改用文字卡片分开显示
```

## 质量标准总结

### 合格标准（必须达到）
- ✓ 商品信息100%准确
- ✓ 关键内容清晰可见
- ✓ 音频正常无瑕疵
- ✓ 文字可读（字号、时长、对比度）

### 优秀标准（力争达到）
- ✓ 节奏流畅，无拖沓
- ✓ 视觉吸引力强
- ✓ 品牌调性一致
- ✓ 转场自然

### 不可接受（必须重做）
- ✗ 商品信息错误
- ✗ 关键步骤缺失（教程类）
- ✗ 音画严重不同步
- ✗ 文字完全看不清
