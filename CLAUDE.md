# Repository Claude Bootstrap

Work in `video-agent/` by first running `node scripts/workspace-context.mjs --fetch-soft`, then reading `outputs/workspace-context.json`, `prompts/workbench-agent.md`, `CLAUDE.md`, and `AGENTS.md` inside that directory. Preserve local uncommitted files, use `origin/main` only as upstream reference, and never force-push `main`.
